# PROGRESS.md — Castle Run (Browser)
*Last synchronized: August 15, 2026 (design synced through July 25, 2026 session; repository state through July 23, 2026)*
*Platform: HTML/CSS/JS — browser-based, mobile-first*
*Separate from Castle Run: Ascent (Roblox project)*

---

## Current Active Architecture

- The active game is the split build deployed through the repository-root `index.html`.
- `index.html` loads `css/styles.css` and, in order, `js/data.js`, `js/game.js`, `js/combat.js`, `js/ui.js`, and `js/main.js`.
- `castle-run.html` is a legacy/reference snapshot only. It is not the deployed entry point and should not receive routine runtime changes.
- The split build contains the latest mobile combat layout, dice controls, dynamic card preview, status UI, Power-card hooks, and enemy-intent work.
- Implementation means code is present. It does not mean the feature has been fully verified against descriptions, previews, timing rules, or all combat paths.

### Status vocabulary

- **Designed** — intended behavior is documented.
- **Implemented** — relevant runtime code exists, but may not have complete testing.
- **Partially verified** — some code paths or play scenarios have been checked.
- **Verified** — behavior has been deliberately tested against the agreed design.
- **Known issue** — a reproducible defect or direct code contradiction is documented.
- **Deferred** — intentionally postponed.

### Current high-level status

| Area | Current status | Notes |
|---|---|---|
| Story and world | Designed | Ending details still contain documented conflicts. |
| Core combat loop | Implemented / Partially verified | Requires systematic card/combat consistency testing. Exhaust/deck-integrity path fixed and Verified Aug 15, 2026 — see Session Log. |
| Five heroes and starter decks | Implemented | Do not infer every card interaction is verified. |
| Hero reward pools and upgrades | Implemented | Substantially populated; GDD completeness conflicts remain. |
| Status and Power systems | Implemented / Partially verified | Timing, descriptions, previews, and Exhaust need consistency tests. |
| Enemy intent | Implemented / Partially verified | Compare every displayed intent with the action branch that resolves. |
| Relic system | Implemented / Partially verified | Several hooks exist; some relics and reward flows remain Deferred. Start-of-combat Block relics were dead until Aug 15, 2026 — see the Block bug fix in the Session Log. |
| Mobile landscape combat | Implemented / Partially verified | April 16 responsive work landed; short-height/browser-chrome testing remains. |
| Sir Crimson encounter | Designed / Deferred | Not established as active runtime flow. |
| Consumables and meta-progression | Designed / Partially implemented | Batch 1 (inventory model + 2 items) Implemented/Verified Aug 18, 2026. Remaining items and acquisition path still Deferred. |

---

## Current Restart Point

- Hero reward pools are substantially implemented.
- Resume work with card/combat consistency testing, not new gameplay expansion.
- Test displayed card cost versus actual Energy spent.
- Test preview damage/effect values versus actual resolved damage/effects.
- Test status application, timing, decrement, tooltip wording, and cleanup.
- Test Power-card activation, ongoing behavior, and Exhaust/return-after-combat behavior.
- Test enemy intent against the exact enemy action that occurs, including special and boss turns.
- After combat verification, continue mobile combat layout polish, especially the hand, selected-card preview, short phone heights, and browser chrome.
- Do not mark these systems Verified merely because their code exists.

See `DESIGN_DISCREPANCIES.md` before changing disputed rules.

---

## Quick Status Overview

> Historical planning snapshot. Labels such as “Complete,” “Built,” and “In prototype” below predate the status vocabulary above and must not be read as verification. The current high-level status and restart point take precedence.

| Area | Design | Built |
|---|---|---|
| Story & World | ✅ Complete | ✅ In prototype |
| Characters (5) | ✅ Complete | ✅ In prototype |
| Floor & Path System | ✅ Complete | ✅ In prototype |
| Combat System | ✅ Complete | ✅ In prototype |
| Cards — Barbarian | ✅ Complete | ✅ All 24 reward cards coded. Pool updated. |
| Cards — Mage | ✅ Complete | ✅ All 27 reward cards coded. Pool updated. |
| Cards — Thief | ✅ Complete | ✅ All 26 reward cards coded. Pool updated. |
| Cards — Vampire | ✅ Complete | ✅ All 28 reward cards coded. Pool updated. |
| Cards — Gambler | ✅ Complete | ✅ All 26 reward cards coded. Pool updated. |
| Card Upgrades | ✅ Complete | ✅ All upgrades coded for all 5 heroes + shared pool |
| Shared Card Pool | ✅ Complete | ✅ In prototype |
| Enemy Roster (all floors) | ✅ Complete | ✅ In prototype. `GDD.md`'s roster tables were re-synced to `js/data.js` on Aug 16, 2026 (stale-doc correction, no code change) and now match exactly — verified by a script that parses the doc back and diffs it against the live data. |
| Boss Debuff System | ✅ Complete | ✅ In prototype |
| Events | 🟡 18 designed, 16 of 18 named events built | 🟡 21 total entries exist in `js/data.js`, but only 16 match a named GDD event by title — the other 5 (Ancient Tome, The Offering, Hidden Cache, The Gambler's Curse, The Blood Pool) are the original pre-July-25 generic placeholders, still present alongside the newer named batches. 2 named events remain entirely unbuilt: The Cracked Mirror and The Soul Market. See Session Log / Events section. |
| Economy (Gold/Souls) | ✅ Complete (Gold) / ⚠️ Souls redesigned | ✅ Gold in prototype. Souls redesigned July 25 — see Soul system section below, no longer a permanent currency. |
| King Aldric Final Boss | ✅ Complete | ✅ In prototype |
| Floor Boss Hint System | ✅ Complete | ✅ In prototype |
| Sir Crimson — Story Arc | ✅ Complete | ✅ **Verified** (Aug 17, 2026) — the Floor 1-2 shadow beat, the Floor 2-3 interruption (smoke transition, confrontation, shadow-themed Rest-stop variant), the real fight including the Echo mimic, and the post-fight outro/reward-reveal are all built and Verified end to end. Dialogue matches GDD.md word-for-word. Nothing from GDD §5 remains unbuilt. |
| Sir Crimson — Fight | ✅ Complete (today) | ✅ **Verified** (Aug 17, 2026) — HP 160, his 4-move rotation (Crimson Strike/Iron Guard/Shatter Step/Studied Blow) wired through the 5b-i multi-move engine, plus the Echo mimic (every 3rd turn, pulls an audited-full-effect card from the player's own deck). Verified with a real, losable fight: correct numbers, rotation order (including the Echo pause-and-resume), 2-turn-early Studied Blow telegraph, Shatter Step's strip, Echo's telegraph-matches-resolution guarantee, a genuine win (no reward/gold/heal) and a genuine loss (clean game-over hand-off). |
| Relics — Common (10) | ✅ Complete | ✅ Built |
| Relics — Uncommon (10) | ✅ Complete | ✅ 9 of 10 built. **`bone_key` has no implementation** (no hook anywhere) and is excluded from both offer pools until it lands. |
| Relics — Rare (10) | ✅ Complete | ✅ 9 of 10 built — **`void_compass` IS implemented** (drives the 1-of-3 elite relic screen); the old "void_compass deferred" note here was stale. **`shattered_mirror` has no implementation** (needs an enemy card-copy mechanic that does not exist) and is excluded from both offer pools until it lands. |
| Relics — Character (15) | ✅ Complete | ✅ **15 of 15 built — COMPLETE** (Aug 17, 2026) — Barbarian (3/3), Mage (3/3), Thief (3/3), Vampire (3/3), Gambler (3/3: `devils_ledger`, `house_always_wins`, `loaded_coat`). Hero-locked via a `hero` field, Floor 3+, boss-reward-only. |
| Relic offer gating (rarity + floor) | ✅ Complete | ✅ Built and Verified (Aug 16, 2026) — `offerableRelics()` in `js/ui.js`, shared by the shop and the Void Compass elite reward. |
| Relics — True Ending (old 4, floor-reward) | ⚠️ Superseded | ❌ Not built. Replaced July 25, 2026 by the 5 per-hero Challenge relics — see `DESIGN_DISCREPANCIES.md`. |
| Relics — Challenge (5, per-hero) | ✅ Complete (design) | ✅ Built and Verified (Aug 16, 2026). All 5 Challenges enforced and earnable; relics persist in `META.challengeRelicsEarned`. **In-fight effects landed Aug 17, 2026** — each hero's relic now fires a distinct Phase 3 beat effect on Aldric via `ALDRIC_HERO_BEAT_EFFECTS`, assigned to thresholds by `computeAldricRelicAssignment()`; the 50 HP beat additionally offers the mercy choice gating the True Ending. |
| Boss Reward Relic Screen | ✅ Complete | ✅ Built and Verified (Aug 16, 2026) — `showBossRelicReward()` (js/ui.js), pick 1 of 3 after Floors 1-3 bosses, between the card reward and the Soul screen. **Character slot is backfilled** until Character relics exist; see Session Log. |
| Consumables (10) | ✅ Complete (design) | 🟡 Batch 1 built and Verified (Aug 18, 2026) — 2 of 10 items (Health Potion, Block Stone). `G.consumables` inventory (cap 3), in-combat slot UI, debug grant. See Session Log. The remaining 8 items and any real (non-debug) acquisition path are still Not built. |
| Soul In-Run Stat Menu | ✅ Complete | ✅ Implemented (Aug 15, 2026). 8 upgrades, offer 3 of 8 after each Floor 1-3 boss. Replaces the old permanent cross-run Soul tree entirely — see Soul system section below. |
| Core Lore / Challenge-Unlock System | ✅ Complete (design) | 🟡 Partly built (Aug 16, 2026). The **cross-run persistence layer exists** (`js/meta.js`) and permanently records which companions' Cores have been collected — the prerequisite for "unlocks their Challenge in a later run". Core *lore* display and Challenge-mode fight logic are still not built. |
| Cross-Run Persistence (`META` / localStorage) | ✅ Complete (design, minimal) | ✅ Built and Verified (Aug 16, 2026). Records first-ever Core collection per companion. `challengeRelicsEarned` is a reserved, unwritten slot — see Session Log. |
| Magic Door Exclusive Pool | ✅ Complete | ✅ Built |
| Card Rarity / Reward Odds | ✅ Complete | ✅ Built |
| Hand Size Decision | ✅ Complete | ✅ Built (5 draw / 8 cap) |

---

## Story — ✅ Locked

- **Setting:** Castle Ashborne — ancient, alive, feeds on grief. The true villain.
- **King Aldric Ashborne** — once a just ruler. Corrupted by grief after betrayal. Victim, not villain.
- **Sir Crimson** — the knight whose desperate act caused everything. Consumed by the castle.
- **The Soldier** — mystery figure. Referenced in Core fragments. Identity revealed across runs.
- **Castle is the villain.** Aldric is its prisoner.

### The Run
- 5 heroes travel together toward the castle
- Castle captures 4 before they arrive — one per floor, corrupted into boss form
- You play as the 5th hero who made it through
- Beat all 4 corrupted companions → face King Aldric

### Two Endings
- **Normal Ending** — defeat Aldric without holding 4 of the 5 Challenge relics. Castle endures. Cycle continues.
- **True Ending** — hold 4 of the 5 per-hero Challenge relics, trigger at 50 HP in Phase 2/3. Aldric freed. Castle destroyed. Sir Crimson appears in true form.
- *(Redesigned July 25, 2026 — see Soul/Core/Challenge system section below and `DESIGN_DISCREPANCIES.md` for why the old floor-reward relic gate was replaced.)*

---

## Characters — ✅ Locked

| Character | Dice Affinity | HP | Playstyle |
|---|---|---|---|
| Barbarian | Even rolls (2, 4, 6...) | 80 | Heavy attacks, Rage stacking, consistent damage |
| Mage | High rolls (6+) | 70 | Spell scaling, Burn, threshold-based power |
| Thief | Odd rolls (1, 3, 5...) | 72 | Poison chains, card cycling, odd-roll combos |
| Vampire | Extreme rolls (1 or max face) | 78 | Lifesteal, high risk/reward, feast or famine |
| Gambler | d6 specialist (min 2, max = Lucky Streak) | 72 | Dice manipulation, high variance, Gold scaling |

Each card has a weak base mode and a strong dice-affinity mode. No global affinity bonus — each card handles it individually.

---

## Floor & Boss Structure — ✅ Locked

- **4 floors** total
- **3 paths per floor** — shown as icons only, no room preview, commit on pick
- **13–15 rooms per path** (random each run)
- **Mirror mechanic** at 60% of path — forced rest/upgrade/remove choice
- Floor boss is a **corrupted version of one of the 4 captured companions**
- Which companion appears on which floor is **random each run**
- **Affinity hints** appear in rooms near the boss — environmental particles matching the boss's character (e.g. magic vapor for Mage, blood mist for Vampire)

### Floor Themes
| Floor | Theme | Boss |
|---|---|---|
| 1 | Castle Dungeon — entrance, guards, creatures | Corrupted companion (random) |
| 2 | Catacombs — undead, decay, darkness | Corrupted companion (random) |
| 3 | Shadow Realm / Void — corrupted magic | Corrupted companion (random) |
| 4 | Throne Room | King Aldric (fixed) |

### Boss Debuff System (Balatro-inspired)
Each corrupted companion boss has a fixed debuff shown before the fight. Debuffs scale per floor:
- **Floor 1** — disables your dice affinity bonus for the fight
- **Floor 2** — card type costs +1 energy
- **Floor 3** — severe mechanical restriction (block resets, draw reduction, etc.)
- **Floor 4** — brutal restriction per character kit

---

## Sir Crimson — ✅ Fully Locked (designed today)

### Story Arc Across the Run
| Point | What Happens |
|---|---|
| Between Floor 1–2 | Appears as a shadowy presence. One line of dialogue. Watching. No fight. |
| Between Floor 2–3 | Confronts you. **The fight happens here.** |
| After the fight | Castle's grip breaks. He's lucid. Gives True Ending hint. Disappears. |
| True Ending cutscene | Appears freed completely. |

### The Fight
- **Type:** Surprise encounter — no reward room beforehand
- **Difficulty:** Full boss-level
- **HP:** 160 | **Base DMG:** 12

**His Moves (rotation):**
| Move | Effect |
|---|---|
| Crimson Strike | Deal 12 damage |
| Iron Guard | Gain 14 block |
| Shatter Step | Deal 8 damage, remove 8 player block |
| Studied Blow | Deal 15 damage + Weak 2 (telegraphed one turn early) |

**Mimic Move — every 3rd turn:**
| Move | Effect |
|---|---|
| Echo | Pulls a random card from your deck and uses it against you at full effect. Always telegraphed. |

Echo mechanic: block cards give him block, damage cards hit you, status cards apply to you. Larger decks are riskier — rewards tight deck building.

**Post-fight dialogue (True Ending hint):**
*"The king... he didn't choose this. None of us did. The castle took everything from him — his grief was the door it walked through. If you want to free him... find what he lost. Four pieces. You'll know them when you see them."*

⚠️ **Needs revisiting (flagged Aug 15, 2026):** this line was written for the old floor-reward relic framing (4 physical royal artifacts). Under the July 25 redesign, the 4 (of 5) relics are earned by out-dueling corrupted companions under Challenge conditions, not found as objects. The hint's wording ("find what he lost... you'll know them when you see them") may need to shift toward something like "prove yourself against the ones who fell" — not rewritten yet, just flagged.

---

## Cards — ✅ Complete

### Universal Starter Cards (all characters)
- Strike (6 dmg, cost 1) / Strike+ (9 dmg)
- Defend (5 Block, cost 1) / Defend+ (8 Block)

### Per-Character Pools
All 5 characters have full 30-card pools designed including upgrades. Each character starts with 10 cards (2 Strike, 2 Defend, 6 class-specific). Reward pools are per-character — only class-appropriate cards appear as rewards.

**Card upgrade rule:** Every card has exactly one upgrade (base → +). Available at rest stops (free), shops (Gold), events (RNG).

---

---

## Card Build Status — Detailed

### What's in the game right now
Every hero has an implemented starter deck and a substantially populated rarity-bucketed reward pool. Card presence and pool membership have not yet been systematically verified against every GDD entry, upgrade description, preview, or combat effect.

### Card Build Status — Implemented, verification pending
All five heroes have implemented starters, reward cards, and upgrades, along with a shared pool. Missing-GDD-card and pool-completeness conflicts remain documented in `DESIGN_DISCREPANCIES.md`.

### Implementation note
Use targeted search before editing. Card definitions and upgrades are in `js/data.js`; active rarity-bucketed `CHAR_REWARD_POOLS` are in `js/ui.js`. Verify relevant hooks in `js/combat.js` and rendering/previews in `js/ui.js` rather than treating any one file as sufficient.

### Card rarity distribution in reward pools
- Common reward cards — appear most frequently
- Uncommon — appear from Floor 2+
- Rare — appear from Floor 3+ or boss rewards only
*(Exact odds not yet designed — see Open Design Items)*

---

## Relics — Designed; partially implemented and partially verified

### ⚠️ SUPERSEDED — True Ending Relics (4, floor-reward)
| Relic | Effect in Aldric fight |
|---|---|
| The Fractured Crown | Aldric loses all Strength permanently |
| The King's Sword | Aldric's damage halved for the fight |
| The Royal Sigil | Fading Light disabled — stop losing HP each turn |
| The Knight's Vow | Aldric stops attacking entirely. Fight ends without killing blow. |

Replaced July 25, 2026 — floor bosses 1-3 are the only relic sources and floor 4 is Aldric himself, so no run could ever hold all 4. See Soul/Core/Challenge system below and `DESIGN_DISCREPANCIES.md`. Aldric's Phase 3 HP-threshold effects (this table) may still inform how the new Challenge relics manifest in the fight, but the specific relic-to-effect mapping needs redesigning against the new 5-relic (need 4 of 5) pool.

### Challenge Relics (5, per-hero) — earned across runs, not picked mid-run
| Hero | Challenge | Type |
|---|---|---|
| Barbarian | +1 Strength every 2 turns, starting turn 2 | Escalation |
| Vampire | Drains 8 HP from you to heal itself, every 3 turns starting turn 3 | Escalation |
| Mage | May never play a card that draws extra cards | Denial |
| Thief | May never gain any Block | Denial |
| Gambler | May never use a reroll | Denial |

Need 4 of 5 to trigger Aldric Phase 3 / True Ending (never your own chosen hero's, since you can't fight yourself). Unlocked by first beating that hero as a corrupted floor boss (Normal Ending run), then re-attempting the fight under Challenge conditions in a later run.

### Common Relics (10) — Any floor, multiple sources
| Relic | Effect |
|---|---|
| Bloodsoaked Rag | Heal 3 HP after each combat win |
| Iron Vambrace | Start every combat with 6 Block |
| Rusted Chain | Enemies start combat with 1 Vulnerable |
| Phantom Blade | First attack each combat deals +8 damage |
| Ivory Die | Add one d8 to your dice pool |
| Ash Pendant | Gain 1 Soul after every battle |
| Cracked Hourglass | Reroll restored at start of every combat |
| Iron Ration | Heal 5 HP after elite fights |
| Lucky Rabbit Foot | Once per run, survive a killing blow at 1 HP |
| Tarnished Coin | Gain 5 bonus gold after every combat |

### Uncommon Relics (10) — Floor 2+
| Relic | Effect |
|---|---|
| Torn Page | Draw 1 extra card at start of each turn |
| Loaded Gauntlet | Minimum dice roll is always 2 |
| Lucky Coin | Roll affinity number exactly → draw 1 card |
| Bone Dice | Reroll result can never be lower than original |
| Grave Robber | Gain 8 Gold after each elite fight |
| Gilded Quill | Every 10th card played deals double damage |
| Scholar's Lens | See 1 extra card option on every reward screen |
| Bone Key | Every 4th room has a chance to contain a hidden chest |
| Twinned Die | Roll twice, take the higher result |
| Soulbound Tome | Gain 1 Energy when you play 3+ cards in one turn |

### Rare Relics (10) — Floor 3+, mix of pure and trade-off
**Pure upside:**
| Relic | Effect |
|---|---|
| Soulbound Gauntlet | First card each turn costs 0 energy |
| Ashen Crown | Gain 1 extra energy at start of every combat |
| Shattered Mirror | When enemy copies/mirrors you, they take 10 damage |
| Void Compass | After every elite, choose 1 of 3 relics instead of 1 |
| Crimson Phylactery | Survive a killing blow at 1 HP once per run |

**Trade-offs:**
| Relic | Effect | Cost |
|---|---|---|
| Cursed Hourglass | Draw 2 extra cards per turn | Hand limit drops from 5 to 4 |
| Hollow Throne | Start every combat with 20 Block | Lose 8 max HP permanently |
| The Pale Contract | All cards deal +4 damage | Healing is 50% less effective |
| Fractured Die | Roll twice, take higher result | Lose reroll for the rest of the run |
| King's Debt | Gain 60 gold immediately | Every shop costs 25% more all run |

### Character Relics (15 total, 3 per character) — Floor 3+, Boss reward only
*Design philosophy: give each character something they're missing*

**Barbarian** (missing: healing, card draw, odd-roll value)
| Relic | Effect |
|---|---|
| Warlord's Bandage | Heal 4 HP every time you play an Attack on an odd roll |
| Battle Drum | Draw 1 extra card at turn start if last roll was odd |
| Berserker's Scar | Taking damage grants 1 Rage stack |

**Mage** (missing: block, low-roll value, survivability)
| Relic | Effect |
|---|---|
| Stone Grimoire | Gain 4 Block every time you cast a spell, regardless of roll |
| Frost Seal | Low rolls (3 or under) apply Chill 1 to enemy instead of nothing |
| Ley Line Crystal | Once per combat, set your dice roll to 6 |

**Thief** (missing: burst damage, block generation)
| Relic | Effect |
|---|---|
| Assassin's Edge | Every 4th card played in a turn deals double damage |
| Shadow Wrap | Start every combat with 5 Block |
| Venomfang | Poison stacks deal 1 extra damage per tick |

**Vampire** (missing: middle-roll consistency)
| Relic | Effect |
|---|---|
| Crimson Lens | Middle rolls (2–5) count as half-affinity — lifesteal at 50% value |
| Blood Pact | Spending HP to play cards counts as a drain — triggers lifesteal |
| Midnight Hunger | If you didn't hit affinity this turn, next roll gets +2 added |

**Gambler** (missing: damage scaling, non-d6 options)
| Relic | Effect |
|---|---|
| Loaded Coat | Once per combat, swap active die for any die type |
| Devil's Ledger | Every 20 gold spent adds +1 damage this run, cap at +8 |
| The House Always Wins | Roll max 2 turns in a row → next card costs 0. Shows streak tracker. |

### Boss Reward Flow
After each floor boss: choose 1 of 3 relics — 1 Common, 1 Rare, 1 Character-specific.

---

## Enemies — ✅ Complete

Full roster across all 4 floors designed. Mix of classic fantasy and corrupted castle aesthetics. Floor 1 enemies are exclusive; some mid-tier enemies carry into Floor 2. Each enemy has HP, damage, and one special ability.

---

## Events — 🟡 18 designed, 16 of 18 named events built (batches 1, 3, 4 + Sleeping Shade Aug 18; Knight/Wolf/Chest Aug 19, 2026)

Categories: Gold events, HP-for-Gold trades, Curse card rewards, Risk events, Classic reworked.

**Count correction (Aug 18, 2026):** this header previously read "17 built," counting `EVENTS.length`
directly. `js/data.js` does hold 18 entries as of this session, but title-matching each one against
GDD's 18-item event table (§12) shows only **13 actually correspond to a named GDD event** — the
other 5 (Ancient Tome, The Offering, Hidden Cache, The Gambler's Curse, The Blood Pool) are the
original pre-July-25 generic placeholders the July 25 correction below already flagged as non-GDD,
never removed once the named batches started landing alongside them. **2 named GDD events remain
completely unbuilt** (as of Aug 19, 2026): The Cracked Mirror and The Soul Market. The Trapped Knight,
The Starving Wolf and The Locked Chest landed Aug 19 — see their section below.

**Correction (July 25, 2026):** this section previously read "✅ Complete (18 events) — all implemented in prototype." That was false — confirmed by direct code inspection: only **5 generic events** existed in `js/data.js`, and none of them matched the 18 named events actually designed in `GDD.md`. This was a design-only gap, not a migration loss — the legacy monolith `castle-run.html` never had the 18 events either.

Building toward all 18 over time is the intent (larger pool keeps outcomes unpredictable; comeback tools should exist by chance, not by the system detecting player struggle).

**`bone_key`'s pause is NOT resolved by The Locked Chest landing (Aug 19, 2026).** This section
previously said the relic was paused "because it depends on 'The Locked Chest' event, which doesn't
exist yet". That event now exists, and `bone_key` is still correctly paused: its text is *"Every 4th
room has a chance to contain a hidden chest"*, which needs a **room-generation hook** that still does
not exist — it is not about opening the chest in this event. The two are thematically adjacent and
mechanically unrelated. Whoever signs off on this batch's reward tier should decide whether `bone_key`
gets a role at The Locked Chest (e.g. unlocking it for free instead of sacrificing a relic), which
would be a genuine design change rather than the unpause the old wording implied.

### The Trapped Knight / The Starving Wolf / The Locked Chest — ✅ Built Aug 19, 2026

`EVENTS` now holds 21 entries: 16 of the 18 GDD-named events plus the 5 surviving pre-July-25 generic
placeholders. All 21 titles and all 21 icons are unique (⚔️ / 🐺 / 🔒 added), asserted.

**Infra built first, per the brief's sequencing, because both pickers' events depend on it:**

`chooseRelicToSacrifice(g, onChosen, onNone)` (`js/ui.js`) — one shared picker for The Trapped Knight
and The Locked Chest, following the `offerableRelics()` chokepoint convention rather than two
one-offs. It owns the modal and the `g.relics.splice()` and **nothing else**: it never calls
`proceedDoors()` and never grants a reward, which is what lets one event trade the relic for a tip and
the other trade it for loot without the picker knowing either. With no relics held it calls `onNone()`
and deliberately does not open an empty picker, so the caller can message and return without
proceeding — the same "event stays open" UX as an unaffordable Gold choice. New
`#relic-sacrifice-modal` in `index.html` mirrors the shop's card modals and reuses
`.rest-deck-grid`/`.rest-deck-card`.

Two details not in the brief, both deliberate: a **Cancel** button (the modal it is modelled on has
one, and without it a player who opens the picker would be trapped unless they gave something up —
cancelling spends nothing and fires no callback), and the selected relic is **re-found by key at click
time** rather than trusting an index captured at render time, since the grid is built from a filtered
copy and a stale index could splice the wrong relic.

`removeRandomCurseFromDeck(g)` (`js/ui.js`) — returns the removed Curse key, or `null`. Verified over
500 removals that it never touches a non-Curse card and that all four curse keys are reachable.

> **The brief's stated rationale for this helper was wrong, and the code comment says so.** It held
> that curses "can currently be granted but never removed — despite the `js/data.js` comment claiming
> removal exists 'at rest/shop.' It doesn't." It does. The Rest stop's remove mode
> (`js/ui.js`, `mode === 'remove'`) splices any selected card with no type filter, and the shop's
> Remove Card service filters only `k !== 'strike' && k !== 'defend'` — neither excludes Curse type, so
> a curse has always been removable both ways. The comment was accurate and has been expanded rather
> than corrected. What genuinely did not exist is **free, random** removal: rest costs the rest, shop
> costs `shopCost(75)`, and both let the player choose. The Starving Wolf is neither. The helper is
> still the right thing to build; only the justification changed.

**Reward tiers are the brief's placeholder, flagged not decided.** All three relic grants use plain
`giveReward(g,'relic')`, which floor-gates via `offerableRelics()` but does **not** rarity-filter the
way The Sleeping Shade's own win does (rare-preferred with a fallback) or its offering does
(common-only). Verified that a Floor 1 win therefore yields a common, never a rare or an unimplemented
relic, and that an all-commons-owned Floor 1 still proceeds without crashing. The Locked Chest's
key-unlock and fight-win pay identically, per the brief — asserted over 150 runs of each.

**Enemy stat blocks are proposed, not signed off**, marked as such in-source, each a single named
constant so re-tuning is one line. None is in any `FLOOR_ENEMIES`/`ELITES`/`EASY_ENEMIES` pool, so they
can only appear through their own event (asserted by slicing the actual roster blocks).

| Constant | hp / block / dmg | Anchored to |
|---|---|---|
| `TRAPPED_KNIGHT_ENEMY` "The Jailer" | 85 / 5 / 13 | The Sleeping Shade's own 85/0/13 — the existing relic-for-a-fight event enemy, since this fight pays the same way. Between Blood Cultist (85/0/14) and Cursed Knight (75/8/13). |
| `STARVING_WOLF_ENEMY` | 55 / 0 / 14 | Cursed Hound (50/0/10), the roster's actual canine, pushed toward "starving": frailest of the three, hitting above its weight. The fight is the *alternative* to feeding it. |
| `CHEST_TRAP_ENEMY` "Chest Mimic" | 60 / 8 / 15 | A mechanism, not a creature: Cursed Knight's 8 Block for the lid, lowest HP, highest damage of the three because a trap is a burst. |

All three are deliberately **special-less**, unlike The Sleeping Shade. Its turn-3 burst was added
because a special-less enemy at Floor 3-4 weight was the only exception in that roster; these three sit
below that weight, where plenty of real rows carry no special either. Adding one is a follow-up, not a
silent change.

### Curse cards — ✅ all 4 functional (Aug 18, 2026)

The four `type:'Curse'` entries in `CARDS` were stubs whose printed downsides were unimplemented —
`js/data.js` recorded them as "do nothing or self-only". All four now behave as printed. **Batch 3's
three events are the real source** (see below) — The Merchant's Ghost and The Crying Statue each roll
one of the three real curses, The Bloodied Altar grants Weakness by name. `js/debug.js`'s `curses=`
option remains for testing.

| Curse | Behaviour | Where |
|---|---|---|
| Weakness | Unchanged — playable, wastes 1 Energy, does nothing. Deliberately **not** in `CARD_PLAY_CONDITIONS`. | — |
| Debt | 3 damage per copy in `G.deck`, at every fight start including Aldric. Floored at 1 HP. | `applyCurseOfDebt()` |
| Confusion | While in hand, one other card in hand costs +2 for that turn only. | `applyCurseOfConfusion()` |
| Binding | One card **key** permanently costs +2, accumulating if two copies pick the same key. | `resolveCurseOfBinding()` |

**`CARD_PLAY_CONDITIONS` gained a `hard: true` flag**, enforced by `playCard()` via
`isHardBlocked()`. It was needed because that table is documented as a *warning* system — the card
stays tappable and `effect()` is the authority — which is fine for Backstab but not for a Curse with
no effect worth reaching: the tap would spend the Energy and the play, then print flavour text. The
refusal **must stay above** `playCard()`'s `getCardEnergyCost(..., { consume: true })` call, or a
mis-tap on an unplayable Curse would silently burn a queued Mana Surge / Soulbound Gauntlet / House
Always Wins free card. Every entry without `hard` keeps the old contract, asserted.

**Binding is key-level, and that is forced rather than chosen.** `G.deck` holds bare key strings with
no per-copy identity, so "one card" is unimplementable — only "one card key" is. Cursing a key you
hold four copies of taxes all four. The card's description was reworded to "one card type" so the
tile does not promise a narrower effect than the data model can deliver. Candidates are the
**distinct** non-Curse base keys, so a 4-copy starter is not four times likelier to be picked than a
singleton on top of already being four times worse to lose (verified over 2000 rolls).

Binding is **counter-driven** (`G.cursedBindingsResolved`) rather than hooked to deck entry: cards
enter `G.deck` at the reward screen, the shop and events with no shared chokepoint, so a per-site
hook would be three places to forget. The counter makes `resolveCurseOfBinding()` safe to call from
anywhere, any number of times — the debug grant calls it immediately, and every fight start calls it
as a safety net.

**Both surcharges are read in `getCardEnergyCost()` against the base cost, above every existing
modifier.** Position is load-bearing: Soulbound Gauntlet, The House Always Wins and Shadow Artist are
hard overrides to **0**, not discounts, so taxing afterwards would let a curse resurrect a card the
game had just promised was free. Taxing first also stops Mana Surge's floored `-1` from being wasted
on a cursed 0-cost card (0+2−1 = 1, where the other order gives 2). Keys are normalized with the same
trailing-`+` strip `CARD_PLAY_CONDITIONS` uses, so the player cannot upgrade a permanent curse off.

**Debt is floored at 1 HP** via `loseHP()`'s own `floorAt`, and therefore never consumes a
survive-killing-blow relic. The printed text promises damage, not death; the player has had no chance
to act, has no counterplay, and `checkCombatEnd()` at fight-start time would run before the combat
screen exists. Flagged as a reading of the text rather than a silent narrowing — if curses are meant
to be able to end a run, this is the line to change.

### Batch 1 — five events from existing mechanics (Aug 18, 2026)

`EVENTS` (`js/data.js`) now holds 10. The five new ones introduce no new mechanic: The Shrine of Ash,
The Gambler's Dice, The Broken Clock, The Cellar of Bottles, Abandoned Laboratory.

**Conventions every event choice follows** — the five originals already did, and this is now written
down because batch 1 doubled the number of places that can get it wrong:

- Each effect ends the event **exactly once**, either by calling `proceedDoors()` itself or by handing
  off to `giveReward()`/`showReward()`, which own the transition. Never both.
- A choice the player cannot afford **messages and returns without proceeding**, leaving the event
  screen live so they can pick something else. Event choices have no render-time affordability
  gating, so the check must live in the effect.
- Gold the player agrees to pay goes through `spendGold()`; Gold gained is a plain `g.gold +=`.

**`G.pendingExtraDraw`** (`js/game.js`, beside `mapBlind`) banks extra card draws owed to the next
fight. `G.extraDraw` cannot hold this — every fight-start function resets it to 0.
`spendPendingExtraDraw()` (`js/combat.js`) is called from `startCombat()`, `startBossFight()` and
`startSirCrimsonFight()` immediately after each reset, and zeroes the bank on spend, which is what
makes it exactly-once. **Deliberately not called from `startAldricFight()`** — that is the one
fight-start that never resets `extraDraw`, because Aldric inherits the Floor 3 boss fight's value by
design; calling it there would add the bank a second time on top of what it inherited. A bank cannot
survive unspent into Aldric anyway, since the Floor 3 boss always sits between an event room and the
final fight. Consequence, inherited rather than new: like every other `extraDraw` source, a bonus
spent on the Floor 3 boss carries into Aldric through that same non-reset.

**`EVENT_POTION_POOL`** (`js/ui.js`) is the shared "drink the unlabelled bottle" pool behind both
The Cellar of Bottles and Abandoned Laboratory — 8 entries, 5 good / 3 bad.

> **Hard constraint: no entry may apply a status.** All four fight-start functions assign
> `G.statuses = { player: [], enemy: [] }` fresh, so a Weak/Chill/Rage/Vulnerable applied at an event
> is wiped before any fight can read it — a **guaranteed** no-op, not an edge case. Curse cards are
> excluded for the same class of reason: `js/data.js` already records that all four "do nothing or
> self-only", so their printed downsides ("Unplayable", "3 damage at combat start") are unimplemented
> and granting one would add a near-blank card. Entries are therefore restricted to state that
> genuinely survives a room transition: HP, Max HP, Gold, the `pendingExtraDraw` bank, and the
> consumable inventory.

Each entry takes `apply(g, done)` and **must call `done()` exactly once on every path** — the
consumable entry resolves asynchronously when the inventory is full, which is why the whole pool is
written to a callback contract rather than returning. The damaging entries are clamped so a random
potion can never end a run: HP floors at 1 (via `loseHP()`'s own `floorAt`), Max HP never drops below
10, Gold never goes negative. The Gold-loss entry deliberately does **not** use `spendGold()` — that
chokepoint feeds Devil's Ledger's "Gold spent this run", and a potion stealing Gold is a loss, not a
purchase, so it must not score toward a relic that rewards spending.

**`giveReward()` gained a `'relic'` branch.** Without it, `giveReward(g,'relic')` fell through to
`showReward()` and would have silently offered a **card** — the wrong reward type. It grants one
random relic directly via `acquireRelic()` (so Ivory Die / Hollow Throne / King's Debt pickup side
effects fire), drawn from `offerableRelics()` for ownership, unimplemented-relic and GDD §9 floor
gating. Deliberately a direct grant rather than a 1-of-3 picker: the elite and boss screens own that
presentation, and an event choice the player already committed to should resolve, not open another
chooser.

**Other shared helpers added** (`js/ui.js`): `grantConsumablesSequentially()` chains grants through
`onDone` so The Cellar's "take three" gets one swap prompt per bottle — a plain loop would hit
`grantConsumable()`'s re-entrancy guard and silently drop bottles two and three at a partly-full
inventory. `upgradeRandomDeckCard()` routes through the shared `upgradeCard()`. `nextRoomTip()` reads
one room past `currentRoomIndex()` (event effects run *before* `proceedDoors()` advances the index)
and calls `getMagicHint(type, 1)` rather than the live floor, because that function returns null for
floor 0 by design and a paid tip must say something on every floor. `getMagicHint()` gained a
`'boss'` entry for the end-of-path case; Magic Doors never produce that type, so nothing else is
affected.

### Batch 4 — four scouting events (Aug 18, 2026)

`EVENTS` now holds 17. The Whispering Walls, The Prisoner's Letter, The Portrait Gallery, The Old
Prisoner. All 17 titles and all 17 icons remain unique, asserted.

**`getBossHint(g)`** (`js/ui.js`) names the boss actually assigned to the current floor and states its
real damage. It **must** read `G.map[G.currentFloor].boss`: floor bosses come from a per-run *shuffled*
copy of `BOSSES` (`G.floorBosses`, stamped onto each floor by the map builder in `js/game.js`), so
`BOSSES[G.currentFloor]` would be wrong four runs in five. Verified across all 4 floors × all 5
bosses, plus a case that deliberately assigns a boss the fixed-index version would have got wrong.
Returns `null` with no crash when a floor has no boss.

Distinct from each boss's own `.hint` field, which is the atmospheric line the boss-intro screen
shows — this is the concrete scouting version. Three of the four events surface it (Listen, Read,
Study), each with a long-lived toast so the information stays readable on the door screen the player
transitions to.

**The Prisoner's Letter's deferred payout** is the one piece of real plumbing here.
`G.pendingEventGold` (run-scoped, in the `newGame()` literal beside `pendingExtraDraw`) plus a
separate `G._pendingEventGoldArmed` flag:

| Step | State |
|---|---|
| "Promise to deliver" clicked | amount set, **not armed** |
| that same event's resolution | armed is false → pays nothing |
| the NEXT event opens (`showEvent`) | armed becomes true |
| that event's resolution | pays, clears both fields |
| any later event | bank empty → nothing |

**Two fields, not one, and that is the whole trick.** The payout check runs immediately after the very
click that creates the bank, so a single "pending amount" could not tell "banked during the event now
resolving" from "banked earlier" — it would pay itself instantly. Verified: nothing at the banking
event, exactly +20 at the next one (measured as the *difference* against an unbanked control run, since
some choices pay Gold themselves), nothing at a third, and the bank simply expires unpaid if the run
ends first — which is the risk the choice advertises.

One accepted imprecision, documented at the call site: a choice that refuses itself (an unaffordable
price messaging and returning) still reaches the payout line, so a banked payout can arrive on a
refused click. The player is standing in the correct *event* either way, so the bank is not held back
for a cleaner moment that may never come.

**The Portrait Gallery's "take the cursed portrait"** draws from the same `TRADEOFF_RELICS` pool as
The Merchant's Ghost, but free rather than 90 Gold. Verified over 600 takes that it yields only the
four downside-carrying Rares and never one of the clean ones, and that the two events' pools are
identical. Owning all four refuses and leaves the event open.

Both HP-cost choices (Shout, Break him out) are floored at 1 HP via `loseHP()`'s `floorAt`, matching
every other event cost.

### Event-triggered combat — ✅ Verified, first real user landed (The Sleeping Shade, Aug 18, 2026)

`startEventCombat(enemyDef, onVictory)` (`js/combat.js`) is the fifth fight-start function: a fight
begun by an event choice, taking an explicit enemy object rather than drawing from a floor pool.
Built as shared plumbing ahead of the event designs that would need it. **The Sleeping Shade is now
the first real event to call it** — its "Wake it" choice starts a genuine, playable fight
(`SLEEPING_SHADE_ENEMY`) rather than the proof-of-concept dummy. `DUMMY_EVENT_ENEMY`,
`startDummyEventCombat()` and `js/debug.js`'s `eventcombat` target were deleted (Aug 18, 2026) per
their own explicit "delete once a real event supplies its own enemy" marker — `CLAUDE.md`'s debug
target list updated to match. Confirmed nothing else in the repo referenced them before removal.

On victory `checkCombatEnd()` short-circuits on `G.enemy.isEventCombat`, mirroring the `isSirCrimson`
branch: it skips the entire gold/soul/relic-hook/elite-drop/Cores chain — all of which assume the
fight came from a floor room — plays the death VFX, then invokes `G._eventCombatVictoryCallback`.
**Loss has no special case at all**, by design: it falls through the same `G.hp <= 0` branch every
other fight uses.

Details worth keeping:

- **It shows the combat screen itself**, unlike `startCombat()` (whose two callers do it). Its real
  callers will be event choices running with the event screen up, and `inCombatScreen()` reads
  `G._activeScreen` — skipping it would leave the consumable row and the out-of-combat use gate wrong
  for the entire fight.
- **It sets neither `G.inBoss` nor `G.lastFightWasElite`.** Sir Crimson sets `inBoss` for boss-level
  difficulty and only gets away with it because his short-circuit returns before every `G.inBoss`-gated
  read; an event fight has no reason to claim boss status, so the reward-window helpers stay correctly
  false.
- **All the ordinary start-of-combat hooks do run** — relics, soul upgrades, curses, and the banked
  Broken Clock draw. An event fight is a real fight: a player holding Iron Vambrace starts it with
  Block, and a Curse of Debt in the deck bleeds them.
- **The callback is captured and cleared before the death-VFX timeout**, so a re-entrant
  `checkCombatEnd()` cannot fire it twice, and it is assigned unconditionally at fight start (to
  `null` when absent) so a previous fight's callback can never fire for a later one.
- **The engine navigates nowhere on its own** — where to go after the win is entirely the callback's
  decision. Asserted, so a future caller that forgets to navigate fails loudly rather than stranding
  the player on a corpse.
- It calls `restoreLoadedCoatDie()`, which Sir Crimson's branch omits. That omission is pre-existing
  and self-healing (every fight start restores it), but a swapped die would still show on the map
  screen in between, so this branch does it properly.

~~**Throwaway, delete when a real event lands:** `DUMMY_EVENT_ENEMY` ("Practice Dummy", 30 HP, 6
damage, no `special`, no `moves`) and `js/debug.js`'s `eventcombat` target.~~ ✅ Deleted Aug 18, 2026
— see The Sleeping Shade section below.

### The Sleeping Shade — ✅ Built and Verified (Aug 18, 2026)

`EVENTS` now holds 18 — the 18th GDD-named event, and the first to use `startEventCombat()` for
real. `SLEEPING_SHADE_ENEMY` (`js/data.js`, event-only — not in any `FLOOR_ENEMIES` floor pool):
HP 85 (matches Blood Cultist, Floor 4), damage 13, plus a one-time **Night Terror** special (turn 3
only, +15 bonus damage) added after comparing against every Floor 3-4 enemy in the data and finding
every single one of them carries a special — a no-special guard at this weight, guarding a real
fight gated behind a guaranteed relic reward, would have been the only exception.

Three choices: **Wake it** (fight; on victory, grants one relic filtered to `rarity==='rare'` from
`offerableRelics()` — not the narrower `offerableTradeoffRelics()` subset — falling back to whatever
`offerableRelics()` does offer if no rare is available, either because the floor is too early
(`RELIC_RARITY_MIN_FLOOR.rare` is 2) or every rare is already owned, so a real win is never wasted).
**Sneak past** (true no-op). **Leave an offering** (-20 Gold via `spendGold()`, guaranteed relic
filtered to `rarity==='common'`, refusing without proceeding if unaffordable or if every common is
already owned — same affordability-refusal convention as every other event choice).

Icon 🌙, unique against all 17 other events (asserted). Enemy loss falls through the same ordinary
`G.hp <= 0` branch every other fight uses — no special-casing needed.

**Verified** via a dedicated Playwright suite, run 5 times for stability: icon/title/desc/choice
rendering; the fight is real and playable (normal hand, dice, Energy, card damage resolve exactly
like any other fight); the Night Terror burst fires only on turn 3 (13 base → 28 with burst → back
to 13); victory grants exactly one relic and skips the standard gold/soul reward chain entirely
(confirmed unchanged before/after); the rare-relic grant and both fallback paths (floor-gated,
fully-owned) each verified independently; the absolute-empty-pool case (every offerable relic
already owned) correctly grants nothing rather than crashing or double-granting; a loss shows the
game-over screen normally; Sneak past is a true no-op; the offering's happy path, insufficient-gold
refusal, and all-commons-owned refusal are all correct. One real test-authoring pitfall caught and
fixed along the way: a first draft of the Night Terror timing test assumed the enemy attacks every
turn, but generic (non-scripted, non-`moves`) enemies randomize their next intent 65% attack / 35%
defend (`js/combat.js` STEP 10) — pre-existing, unrelated behavior. Fixed by forcing
`G.enemy.intent = 'attack'` before each simulated turn so the test isolates the burst deterministically.

Full cross-batch regression sweep re-run clean after this batch, including after the dummy-plumbing
deletion above (confirmed via direct source grep that nothing else referenced the deleted names
before removing them). Two pre-existing failures persist unchanged and are unrelated to this batch:
`sircrimson_restructure2.js`/`sircrimson_fight.js` (already documented as retired assertions from the
Echo batch) and `sircrimson_fight2.js`, newly identified this session — its reachability check counts
literal-text occurrences of `startSirCrimsonFight()` across all script files, including two comment
mentions in `js/ui.js` and `js/combat.js` that were never part of its "definition + 1 real call = 2"
assumption; confirmed via direct grep that this predates and is unrelated to any change in this
session (neither file was touched by The Sleeping Shade). Not fixed here — flagged for whoever next
touches that suite. **Update:** actually fixed the same day — see the Session Log entry below
("Diagnosing the recurring Sir Crimson test failures").

Not built as part of this session, since a floor-gating concept for events doesn't exist anywhere in
the codebase and adding one was out of scope: 5 named GDD events remain entirely unbuilt (The
Trapped Knight, The Starving Wolf, The Locked Chest, The Cracked Mirror, The Soul Market), and the 5
original pre-July-25 generic placeholders (Ancient Tome, The Offering, Hidden Cache, The Gambler's
Curse, The Blood Pool) still occupy slots in `EVENTS` alongside the 13 GDD-named ones now built.

### Batch 3 — the three Curse-granting events (Aug 18, 2026)

`EVENTS` now holds 13. The Merchant's Ghost, The Crying Statue, The Bloodied Altar. These are the
first real source of Curse cards — until now `js/debug.js`'s `curses=` was the only way to get one.
Same choice conventions as batch 1 above.

**`CURSE_EVENT_POOL`** (`js/ui.js`) is the three curses a random roll can inflict:
`curse_debt`, `curse_confusion`, `curse_binding`. **Curse of Weakness is deliberately excluded** and
granted by name only, by the Altar's smash — it is the one curse that is merely dead weight, so it is
the mild outcome of the choice with no upside rather than something a random roll can substitute for
a real curse. If Weakness ever enters that pool, smashing the Altar stops being distinguishable from
robbing the Ghost. Verified across 600 rolls per event.

**`grantCurse(g, key)`** is the shared deck write. It calls `resolveCurseOfBinding()` after the push,
which matters: a Curse of Binding granted without it sits inert until the next fight start happens to
sweep it up, so the surcharge would appear a room later than the card. Verified that a Binding grant
surcharges a key immediately and that `getCardEnergyCost()` reads it on the spot.

**`TRADEOFF_RELICS`** is the four Rares with built-in downsides — King's Debt, Cursed Hourglass, The
Pale Contract, Fractured Die. The Merchant's Ghost sells only from this list, verified over 600
purchases to never yield one of the clean Rares. It deliberately **does not** use `offerableRelics()`:
that applies GDD §9 floor gating, under which Rares are Floor 3+ only, and this event's premise is a
guaranteed tradeoff Rare at any floor. Ownership filtering is kept; the floor gate is not.

**Price: 90 Gold, flat.** Inside the 80-150 relic band this document records, and a fair premium over
the shop's `shopCost(80)` given it bypasses the rare floor gate and guarantees a downside-carrying
Rare. Two things worth knowing rather than discovering later:

- **Flat, not `shopCost(90)`** — so King's Debt does not inflate it. Every price in `EVENTS` is flat
  and routing this one through the shop markup would make it the only exception. Defensible either
  way (it *is* a merchant); one line to change if a ghost's price should count as a shop price.
- **Buying King's Debt nets −30, not −90**, because its own pickup grants +60 Gold immediately. It is
  by far the cheapest of the four, and there is no way to pick which one you get. Asserted, so the
  interaction is recorded rather than surprising.

Availability is checked **before** charging, matching the shop's contract: owning all four refuses
and leaves the event open rather than taking 90 Gold for nothing.

**The Bloodied Altar** grants 2 Power cards from the player's own hero pool, straight into the deck.
Every hero has **exactly 3** Power cards, all in their rare tier — so the pool is only one deeper
than the grant. `grantHeroPowerCards()` therefore prefers unowned keys but falls back to allowing a
duplicate rather than granting fewer than promised: a player holding 2 of their 3 Powers would
otherwise get a short reward from a choice that already cost them 15 HP. Verified for all five heroes
that both grants are real `type:'Power'` cards from that hero's own pool and never another hero's,
across 400 sacrifices covering all 15 Powers. HP cost is floored at 1 like The Shrine of Ash's.

**Balance note, not a defect:** two rare Powers for 15 HP is a strong rate, and Power cards are the
top tier of every hero's pool. Flagged rather than quietly adjusted — the numbers are as specified.

**Known non-issue, verified:** `giveReward(g, type, rarity)`'s `rarity` argument has never been read
— `showReward()` rolls its own rarity. So "gain a rare card" on Ancient Tome, The Offering and now The
Shrine of Ash is aspirational rather than guaranteed. Pre-existing, unchanged here, and flagged
rather than silently fixed since making it binding would change three events' payouts.

---

## Economy — ✅ Design Complete, ✅ Built

**Two currencies:**
- 🪙 **Gold** — in-run only, starts at 30, resets each run
- 💀 **Souls** — permanent, earned every battle, spent on meta-progression

**Gold income:** 12–40 per regular battle (scales per floor), 40–90 elites, 80 per boss
**Gold sinks:** Shop items 50–120 Gold, card removal 75–100, relics 80–150

---

## Consumables — ✅ Complete (Batches 1–6, Aug 18, 2026)

10 consumables. Carry up to 3 at a time. Found in shops, events, magic doors, chests.

`G.consumables` (js/game.js) is the per-run inventory array, cap 3, reset only by `newGame()` —
persists across every combat within a run exactly like `G.relics`. `CONSUMABLES` (js/ui.js) holds
the item data; `grantConsumable()`/`useConsumable()`/`renderConsumableSlots()` implement
grant/use/render. A new in-combat slot row (`#consumable-slots`, floated above `.dice-panel`,
opposite side from `#soul-die-controls`) shows each held item; click-to-use, no
Energy cost, single-use (spliced out of the array on use). Items are obtainable from the shop,
elite rewards and events (see "Acquisition and availability" below), plus `js/debug.js`'s
`consumables=` option for testing.

**In-combat row overlap — ✅ Fixed and Verified August 21, 2026.** The row was a
`flex-direction: column` growing upward from the dice panel with no cap, so its height scaled with
inventory size. Measured in a real browser rather than read: each row is **42px** tall on any
viewport ≤1100px wide — `.btn { min-height: 42px }` (the tap-target rule inside
`@media (max-width: 1100px)`) beats `.soul-die-btn`'s `height: 15px`, being a later rule of equal
specificity — so a full 3/3 inventory stood **136.5px** tall on a landscape phone and climbed out of
the dice panel into the character/HP band. Desktop was 64.7px and reached into the arena at 2 items.

Fixed by making the footprint bounded by the layout instead of by inventory: `flex-direction: row`
with `flex-wrap: nowrap`, so height is exactly one button at 1, 2, or 3 items (19.8px desktop /
44.1px landscape) and the empty state still has zero footprint. The buttons became **emoji-only** to
keep the row narrow — full names would make 3 slots ~290px wide and push it across the player's
sprite and HP bar, trading a vertical overlap for a horizontal one. The name and description moved
to `title` plus a new `aria-label`; `renderFieldInventory()` still labels items in full out of
combat. Verified at 0/1/2/3 items across 6 viewports (1280×800 down to 568×320), plus a functional
check that the *n*th cell still uses the *n*th held item and fires its effect.

**Out-of-combat box desktop sizing — ✅ Added and Verified August 21, 2026.** `#field-inventory` had
one fixed size for every viewport, and the only breakpoint touching it made it *smaller* for mobile —
so on a wide desktop window it rendered at phone size while `.door-title`/`.door-content` around it
scaled up. Added a `@media (min-width: 901px)` block (901px sits just above the mobile rule's 820px
ceiling): label `0.5rem → 0.68rem`, slot and placeholder text `0.45rem → 0.58rem`, rows `15px → 22px`,
padding `0.3/0.4rem → 0.5/0.6rem`, and `max-width: min(320px, 46vw) → a flat 280px` so the box does
not stretch on ultrawide monitors.

Deliberately scoped to `.field-inv-slots .consumable-slot`, never the bare
`.consumable-slot`/`.soul-die-btn` classes — those are shared with the in-combat `#consumable-slots`
and `#soul-die-controls`, whose sizing pass above exists to keep them small. Both suites were re-run
together to prove the in-combat row is byte-identical after this change.

Two things the sizing table alone would have missed, found by measuring instead:
- **`.btn { min-height: 42px }` still applies from 901–1100px** (it lives in `@media (max-width: 1100px)`),
  so populated slots are 42px there, not the declared 22px, while `.field-inv-empty` — a plain div —
  would have stayed 22px and left a partly-filled box with mismatched rows. A paired
  `@media (min-width: 901px) and (max-width: 1100px)` rule holds the placeholders at 42px to match.
  Above 1100px both settle at 22px.
- **`.field-inv-why` did need the bump** (the deferred judgment call). Left at `0.4rem` it fell to 69%
  of the new slot text and read as a smudge; at `0.58rem` it matched the item name and the hierarchy
  collapsed. Set to `0.52rem`, which reproduces the base design's own 89% ratio at 90%. Judged from
  rendered 3× screenshots of all four candidates, not from arithmetic.

Verified at 8 viewports (780×400 → 2560×1080) including every breakpoint boundary (860/901/1000/1100/1101),
at 0/1/2/3 items, asserting row-height agreement between filled and empty slots at each.

⚠️ **Known issue, pre-existing and NOT introduced by the above:** `#soul-die-controls` is still an
uncapped upward column with the identical problem, and the original CSS comment's claim that the two
rows "can never collide even when both are populated" is false at **every** size — the dice panel is
only 76px wide (58.8px on mobile), so two content-sized stacks anchored to its left and right edges
always overlap. Measured on landscape 740×360: the soul stack is 44.1px tall at 1 row, 90.3px at 2
(Second Die + Gambler's Edge), 136.5px at 3 (+ Ley Line Crystal on a Mage), reaching into the arena
from 1 row up, and overlapping the consumable row in all three cases. The consumable fix *shrinks*
that overlap (the shared band went from ~56×90px to ~56×44px) but cannot remove it. Separately,
`#aldric-curse-indicator` uses the byte-identical `left: 0; bottom: calc(100% + 0.2rem)` anchor as
`#consumable-slots` and so draws directly on top of it during Aldric Phase 2+ — confirmed by
measurement on both the original and fixed code, so also pre-existing. Both need the same
bounded-footprint treatment plus a call on which element yields.

Nine of the ten items' `effect` reuses an existing combat function verbatim
(`healPlayer`/`gainBlock`/`applyStatus`/`drawCards`) — there is no per-item plumbing. Batch 2
added one shared fix: `useConsumable()` now ends in `renderAll()` instead of
`renderConsumableSlots()`, because `drawCards()` and the raw `g.energy`/`g.gold` writes do not
render themselves the way `healPlayer()`/`gainBlock()` do. `renderAll()` is a superset, so the
slot row still updates. Dice Stabilizer is the one item that needed new mechanism — see below.

| Item | Effect | Floor | Status |
|---|---|---|---|
| Health Potion | Heal 20 HP | Any | ✅ Built and Verified (Aug 18, 2026) |
| Smoke Vial | Apply Weak 2 to enemy | Any | ✅ Built and Verified (Aug 18, 2026) |
| Fire Flask | Apply 4 Burn | Any | ✅ Built and Verified (Aug 18, 2026) |
| Poison Vial | Apply 5 Poison | Any | ✅ Built and Verified (Aug 18, 2026) |
| Energy Crystal | Gain 2 Energy this turn | Floor 2+ | ✅ Built and Verified (Aug 18, 2026) |
| Scroll of Draw | Draw 3 cards immediately | Any | ✅ Built and Verified (Aug 18, 2026) |
| Dice Stabilizer | Lock die at current result for 2 turns | Floor 2+ | ✅ Built and Verified (Aug 18, 2026) |
| Gold Pouch | Gain 40 gold | Any | ✅ Built and Verified (Aug 18, 2026) |
| Block Stone | Gain 15 Block immediately | Any | ✅ Built and Verified (Aug 18, 2026) |
| Chaos Potion | Random status on enemy | Floor 3+ | ✅ Built and Verified (Aug 18, 2026) |

**Chaos Potion pool (`CHAOS_POTION_STATUSES`, js/combat.js).** `GDD.md`'s §12 table abbreviates
the effect to "Apply random status to enemy", but the raw GDD source (`gdd_text.txt:741`) spells
out the pool: **Poison, Burn, Weak, or Vulnerable**. Built against the source — Chill is
deliberately *not* in it, and enemy `💢Rage` cannot come up (it is the enemy's Strength buff, so
rolling it would help the enemy). Stacks match the dedicated single-status items (Poison 5,
Burn 4, Weak 2) so the potion is a random one of those; Vulnerable 2 is the one number with no
GDD figure, chosen for duration parity with Smoke Vial's Weak 2 since both debuffs decay 1/turn.

**Dice Stabilizer (batch 4) — the one item with real mechanism.** Two fields in `js/combat.js`,
and the distinction between them is the whole implementation:

- `G._diceLockTurnsRemaining` — how many **future** turns skip their roll. `startTurn()` branches
  on this field *only* (never on `dieLockActive()`), decrements it, and skips `rollDice()`, leaving
  `G.currentDie` untouched. Set to `2` by `lockDice()`; re-using while locked resets to 2 rather
  than stacking.
- `G._dieLockedThisTurn` — whether **the current** turn is a held one. Turn-scoped, rewritten by
  every `startTurn()`.

`dieLockActive()` is the OR of the two and is what every in-turn gate must call. The counter alone
is off by one for "is the die locked right now?": on the **last** locked turn it has already been
decremented to 0, so a counter-only check reported unlocked on a turn whose roll had in fact been
suppressed — the reroll button re-enabled itself one turn early and the player could reroll the
held value away on the final turn of a lock they paid for. Conversely `startTurn()` must *not* use
`dieLockActive()`, since `_dieLockedThisTurn` still holds last turn's value at that point and the
lock would re-arm every turn and never expire. Both directions are covered by tests.

Blocked while locked, each at its own trigger point plus a UI disable: `useReroll()` (button shows
`🔒 LOCKED (n)`), `openGamblersEdge()`/`applyGamblersEdge()`, and `useLeyLineCrystal()`. Blocked
attempts do not consume the once-per-combat charges. A locked turn still sets `G.diceRolled = true`
— the die *has* a value, it simply was not re-rolled — without which `useSecondDie()` would refuse
with a misleading "Roll first!".

`G._naturalDieValue` is deliberately **not** touched by a locked turn: not re-rolling does not make
the held value a forced one, so a locked natural max stays natural for `naturalMaxSuppressed()` and
The House Always Wins, unlike Ley Line Crystal / Gambler's Edge / Second Die which overwrite
`G.currentDie` and break the stamp on purpose.

Roll-keyed effects therefore re-fire on every locked turn, which is the point of the item rather
than a bug: affinity bonuses, the Arcane Die's even-roll +1 Energy, Titan's Die's max-roll draw
(6 instead of 5, so +1 card), Battle Drum's odd-roll +1 card, and The House Always Wins' max-roll
streak. Both fields reset to 0/false in all four fight-start functions, so a lock never crosses a
combat boundary. `?debug=combat&dicelock=2` starts a fight already locked.

**Die-mutating paths deliberately left unblocked** (batch 4's block list covers rerolling and the
two forced-set actions only): Second Die's +d2, and card effects that reroll or set the die (Risk
Taker, Wild Combo, Loaded Die, Safe Pull, Loaded House's guaranteed max). These cost a card and
Energy, which is the same line the Gambler Challenge already draws for rerolls. They are coherent
rather than broken — whatever value they leave in `G.currentDie` is what the remaining locked turns
hold — but if the lock is meant to be absolute, these are the sites to revisit.

**Acquisition and availability (batch 5).** `CONSUMABLE_AVAILABILITY` (js/ui.js) transcribes GDD
§12's "Available From" and "Floor" columns, and `offerableConsumables(source, g)` is the single
helper every source calls — modelled on `offerableRelics()`, for the same reason: the relic pools
had already drifted apart before that helper consolidated them. `minFloor` values are zero-based
`G.currentFloor` indices, matching `RELIC_RARITY_MIN_FLOOR`.

| Source | Items | Where |
|---|---|---|
| Shop | 8 (all but Gold Pouch / Chaos Potion) | `showShop()` draws `CONSUMABLE_SHOP_SLOTS` (2) rotating tiles per visit, thrown in with the static card/die stock and sliced to 4 |
| Elite reward | Fire Flask, Poison Vial | `checkCombatEnd()`, beside the `iron_ration`/`grave_robber` hooks; unconditional on an elite win |
| Event | Health Potion, Smoke Vial, Scroll of Draw, Gold Pouch, Block Stone, + Chaos Potion on Floor 3+ | `giveReward(g,'consumable')`; Hidden Cache is the first caller |
| Magic Door | — | **No direct grant, by decision (Aug 18, 2026).** Doors route into Shop/Event/Elite rooms, which already cover it. The `'door'` entries in `CONSUMABLE_AVAILABILITY` are unread GDD transcription. |

The shop pool is 8, not 10: GDD §12 lists Gold Pouch and Chaos Potion as Event/Magic-Door only, so
both carry `cost: null` and `offerableConsumables('shop')` filters them out twice over (by source,
then by the missing price). Paying Gold for a Gold Pouch is a wash by construction.

The retired `SHOP_ITEMS` "Healing Potion" entry healed 20 HP on purchase and never entered the
inventory — a different thing from the Health Potion consumable, which now shares its 50 Gold price.
**Per-item prices are not in the GDD** (§12 has no price column); they are set inside the 50-120
band this document records for shop items. The consumable tile is the one stock type that checks the
3-slot cap *before* spending Gold — every other tile can always deliver, and taking payment for an
undeliverable item would be theft.

**Out-of-combat use (batch 5).** `#field-inventory` is one body-level fixed element, not a copy in
each screen, toggled by `showScreen()` for the five screens in `FIELD_INVENTORY_SCREENS`
(path/door/rest/shop/event) and filled by `renderFieldInventory()`. It renders all 3 slots including
empties, so the carry limit is legible while shopping. `OUT_OF_COMBAT_CONSUMABLES` is Health Potion
and Gold Pouch — the two whose effect is pure run state; the other 8 render disabled with a "Combat
only" label rather than hidden, since a hidden item looks lost. `useConsumable()` enforces the same
rule as a backstop, and that check is load-bearing rather than decorative: an Energy Crystal used
between rooms would be destroyed for nothing.

`inCombatScreen()` reads `G._activeScreen`, stamped by `showScreen()`. It deliberately does **not**
read `G.enemy`: despite CLAUDE.md describing that field as "null outside combat", nothing ever sets
it back to null, so it stays truthy for the rest of the run after the first fight. All five fight
entries were checked to route through `showScreen('combat-screen')`.

**Inventory-full swap prompt (batch 6).** An Event or Elite grant arriving at 3/3 used to be lost
silently. It now opens `#consumable-swap-overlay` (`showConsumableSwapPrompt()`), listing the 3 held
items plus "Leave it behind". Choosing discards that item and completes the grant; declining leaves
the inventory untouched and the new item is lost — the same outcome as before, but as the player's
decision rather than something that happened to them. Held items are listed **by index**, so holding
three of the same item still offers three distinct buttons.

Three details carry the weight:

- **`grantConsumable(key, options)` gained `onDone(granted)`.** The prompt is asynchronous, so the
  synchronous return value cannot describe it — it is `false` both for an immediate refusal and for
  "a prompt is now open". Anything sequencing on the outcome must use `onDone`. The event grant does:
  `giveReward(g,'consumable')` defers its `proceedDoors()` until the choice resolves, because firing
  the screen change 800ms later regardless would leave the player choosing what to discard on top of
  the door screen. Declining releases the transition too, so an event can never get stuck.
- **The elite grant deliberately passes no continuation.** The post-combat chain it sits in (death
  VFX, core/Challenge records, then a `setTimeout` into the whole reward flow) is load-bearing and
  must not wait on a player choice. The overlay is `z-index: 400`, above `#deck-overlay`, so it
  stays on top of whatever reward screen the chain reaches; resolving it only touches
  `G.consumables`, which is safe from any screen. Cosmetically it can appear over a transitioning
  screen — functionally the swap is independent of what is underneath.
- **`allowSwapPrompt: false` at two call sites.** The shop passes it because Gold is already spent
  by that line: a decline would take payment for nothing, which is exactly why the pre-charge cap
  check above it cannot be deleted in favour of the prompt. `js/debug.js` passes it because a modal
  mid-setup is wrong for a dev tool — a 4th key still warns and is skipped.

`addConsumable()` was split out as the shared inventory write so the normal grant and the post-swap
grant cannot drift on the acquire message or either render call. `G._pendingConsumableSwap` holds the
open choice; it is cleared *before* the grant completes, so a double-click cannot resolve one prompt
twice and a second grant cannot clobber a pending one.

**Known gaps:**
- **No Magic Door grant site — closed as won't-build (Aug 18, 2026).** Magic Doors route into
  Shop/Event/Elite rooms, so those three sources already cover the door path; no direct grant is
  needed. The `'door'` entries in `CONSUMABLE_AVAILABILITY` are therefore transcription of GDD §12
  that nothing reads. Consequence worth knowing: Chaos Potion is obtainable only from Floor 3+
  events, and Energy Crystal / Dice Stabilizer only from Floor 2+ shops.
- **No drop rate for the elite reward.** GDD §12 says only that elites are a source. Implemented as
  guaranteed, matching the shape of the relic hooks it sits beside; a percentage would be a design
  call.
- **No Gold Pouch price**, by design — but it means an event/door-only item has no shop tile path
  if it is ever added to shop stock.

---

## Soul / Core / Challenge System — ✅ Redesigned July 25, 2026; Souls Implemented Aug 15, 2026, Cores/Challenges ❌ Not Built

This entire section replaces the old permanent cross-run Soul meta-progression tree below it (kept struck-through for history — see `DESIGN_DISCREPANCIES.md` for the full resolution).

**Souls (redesigned — now in-run only):**
- Earned per floor, no longer a permanent cross-run currency.
- Spent at a stat-upgrade screen that appears right after each floor boss's relic-reward pick (Floors 1-3 only).
- Carries forward to the next floor checkpoint if saved; resets to 0 at the start of every run.
- No cross-run banking — this replaces the old Power/Knowledge/Fortune branches entirely.

**Soul-spend menu — ✅ Finalized August 15, 2026, ✅ Implemented August 15, 2026:** 8 options, offer 3 at random per spend window. Built around tradeoffs (stronger effects cost more or carry a downside) with two dice-native options rather than pure stat boosts throughout.

| Upgrade | Effect | Cost | Repeatable? |
|---|---|---|---|
| Vitality | +6 Max HP, heals to new max | 3 Souls (rises +1 each rebuy) | Yes |
| Grit | +5 Block at the start of every combat, rest of the run | 5 Souls | No |
| Steady Hand | +1 reroll charge per combat, rest of the run | 6 Souls | No |
| Second Die | Once per combat: after your roll, optionally add a d2 to it | 6 Souls | No |
| Momentum | +1 Energy per turn, rest of the run | 8 Souls | No |
| Overdraw | Draw +1 card at turn start, rest of the run | 8 Souls | No |
| Reckless Surge | +1 Energy per turn, rest of the run — but −5 Max HP permanently this run | 4 Souls | No |
| Gambler's Edge | Once per combat, force your die to any value — but die can never roll its own affinity max on a natural roll for the rest of the run | 6 Souls | No |

Soul income per floor: 1 (regular win), 2 (elite), 3 (boss — triggers the spend screen), +1 per battle if Ash Pendant is held.

Cut during design: **True Roll** (min-roll floor — conflicted with Mage's Frost Seal, Gambler's low-roll cards, and Thief's odd affinity all caring about natural 1s) and **Twinned Roll** (roll twice, sum or take higher — trivialized Mage's high-roll threshold, dead for Vampire's exact-value check, and overlapped with the existing Twinned Die/Fractured Die relics). Second Die replaced them — bounded, optional, ~50% parity-flip odds instead of a guarantee. Full reasoning in `GDD.md` §15.

**Cores (redesigned — now have a defined job):**
- Beating a corrupted companion for the first time drops their Core.
- This reveals their lore in the menu/hero-select AND unlocks that hero's Challenge details simultaneously.
- Cores are not spent or consumed — they're a permanent lore/unlock record, viewable anytime.

**Core lore reveal — ✅ Written and ✅ Implemented August 20, 2026; Verified for the flow paths listed below.**

Lore text for all five companions now lives on the `BOSSES` entries in `js/data.js` as a `lore` field, deliberately separate from the existing pre-fight `hint`. The reveal is a `#core-lore-overlay` popup reusing the `.path-confirm-box` shell (the same shape as Sir Crimson's shadow beat), not a screen and not a toast — the existing "Core of X — collected!" toast is unchanged and still fires regardless.

`recordCoreCollected()`'s first-time-ever return value — previously discarded at its only call site — is now captured in `checkCombatEnd()` into `G._pendingCoreLore`. The overlay is **not** shown there: that block sits inside the timing-sensitive post-combat `setTimeout` chain (Aldric phase guard, the floor≥3 branch, the normal boss → `showReward()` branch). It is gated at **two** later points instead, both in `js/ui.js`:

| Gate | Covers | Continues into |
|---|---|---|
| `proceedOrPath()` | Floor 0/1/2 boss clears — everything upstream in the reward chain has already had its turn, so this only delays the final step | `showPathSelect()` |
| `launchFinalBoss()` | The Floor-3 boss clear, which returns straight out of `checkCombatEnd()` and **never reaches `proceedOrPath()` at all** | `showScreen('combat-screen')` + `startAldricFight()` |

The second gate is the non-obvious half. Without it exactly one reveal in four would be dropped silently and **permanently** — the `META` write has already happened by then, so `recordCoreCollected()` returns `false` in every later run and the lore could never be offered again.

On a Floor-1 clear both the Sir Crimson shadow beat and a Core reveal can be pending at once (the first floor boss is itself a corrupted companion — traced, not assumed). The shadow shows first and `dismissSirCrimsonShadow()` re-enters `proceedOrPath()`, which then lands on the Core gate, so the sequence ends on the companion just defeated rather than opening with them. Missing markup or missing lore text falls straight through to the continuation — a story beat never hard-blocks a run.

**Verified** (39 assertions, traced against the real `js/*.js` files with a stubbed DOM rather than read statically): lore present/distinct/non-`hint` on all 5 entries; first-ever collection stashes, a later run's repeat collection does not; the Floor-1 both-pending ordering and its hand-off; correct title and lore text on the overlay; the Floor-3 gate delays Aldric and its self-recursion terminates; both degraded-markup fall-throughs; and the `!G.isFinalBoss` guard blocking any second stash on Aldric's death.

⚠ Still open: PROGRESS's own bullet above (and `GDD.md` §9) also calls the lore "viewable anytime" from the menu/hero-select. Only the first-defeat reveal moment is built — there is no re-viewer, so the lore is currently readable exactly once, ever, per companion. See `DESIGN_DISCREPANCIES.md`.

**Challenges (new system):**
- See the Challenge Relics table above for all 5 designed challenges.
- A Challenge is only attemptable once its Core has been collected (i.e., you've beaten that hero once as a corrupted boss).
- Clearing a Challenge fight earns that hero's True Ending relic.

✅ Resolved Aug 15, 2026: costs/numbers finalized and built; the spend screen reuses the shared reward screen and fires after the floor boss's card reward. Souls income is now flat per fight type (1/2/3, +1 Ash Pendant) — the per-enemy `souls` values still sitting in `js/data.js` belong to the superseded permanent-Soul design and are no longer read.

⚠️ Still open: the boss relic-choice screen (1 of 3) is still unbuilt, so the Soul screen currently follows the boss **card** reward. When the relic screen lands it belongs between the two, leaving the Soul screen last as designed.

---

### ⚠️ SUPERSEDED — old permanent Soul Meta-Progression Tree

3 branches. Shared across all characters. **Replaced entirely July 25, 2026 by the in-run system above — kept here for history only, do not implement.**

**Power Branch**
- Start each run with +5 max HP (2 Souls)
- Start each run with +1 Energy on turn 1 (3 Souls)
- Starter deck includes 1 additional class card (4 Souls)

**Knowledge Branch**
- See room types 1 room ahead on path (2 Souls)
- Shop shows 1 extra item per visit (3 Souls)
- Card rewards show 4 options instead of 3 (5 Souls)

**Fortune Branch**
- Start each run with 30 bonus gold (2 Souls)
- Elite fights drop 1 consumable in addition to normal reward (3 Souls)
- Once per run, reroll a relic choice for free (4 Souls)

---

## King Aldric Final Boss — Designed and implemented; verification incomplete

3 phases. Stone Heart mechanic. Dice corruption in Phase 2. Phase 3 only fires if 4 of 5 Challenge relics are held — redesigned July 25, 2026, see Soul/Core/Challenge System section above. **The True Ending trigger is no longer automatic on the killing blow with the gate held** — as of Aug 17, 2026 it requires actively accepting the mercy choice offered at the 50 HP beat (GDD §1); declining it, or killing him without ever reaching that beat, now correctly gives the normal ending. Each of the 4 held relics also has its own distinct beat effect (Aug 17, 2026) instead of the four previously-unattributed generic placeholders.

---

## Open Design Items — ❌ Still Needed

| Item | Priority | Notes |
|---|---|---|
| Soul in-run stat menu | Done | ✅ Design finalized Aug 15, 2026 and ✅ Implemented the same day — see Soul/Core/Challenge section. |
| 2 remaining named events (of 18 designed) | Medium | The Cracked Mirror and The Soul Market. 16 of 18 named events now built (The Trapped Knight, The Starving Wolf and The Locked Chest landed Aug 19, 2026); see Events section. |
| Magic Door exclusive event pool | Low | Currently pulls from normal room pool |
| Card rarity system (Common/Uncommon/Rare reward odds) | Low | Can tune late |

---

## Open Build Items — ❌ Not Yet Implemented

| Item | Notes |
|---|---|
| Sir Crimson encounter | Full fight + dialogue + story beats between floors. Post-fight dialogue needs a rewrite pass — flagged above, written for the old relic framing. |
| Boss reward relic choice screen | Pick 1 of 3 after each floor boss |
| ~~Consumable system (batches 1–6)~~ | ✅ Implemented Aug 18, 2026 — `G.consumables` inventory (js/game.js), `CONSUMABLES`/`CONSUMABLE_AVAILABILITY`/`offerableConsumables`/`grantConsumable`/`addConsumable`/`renderConsumableSlots`/`renderFieldInventory`/`inCombatScreen` + the swap prompt (js/ui.js), `useConsumable`/`CHAOS_POTION_STATUSES`/`applyChaosPotion`/`lockDice`/`dieLockActive` + the elite drop hook (js/combat.js), `#consumable-slots` / `#field-inventory` / `#consumable-swap-overlay` UI (index.html/styles.css), `consumables=` + `dicelock=` debug options. **Feature-complete: all 10 designed items, shop / elite / event acquisition, GDD floor gating, out-of-combat use for Health Potion + Gold Pouch, and a player-facing choice when a grant arrives at 3/3.** 335 headless checks. Open by design, not omission: no elite drop rate in the GDD, no Magic Door grant (doors route into the three real sources). |
| ~~Soul in-run stat-upgrade screen~~ | ✅ Implemented Aug 15, 2026 — `SOUL_UPGRADES` (js/data.js), `buySoulUpgrade`/`soulUpgradeOffer`/`soulUpgradeCost` (js/game.js), `showSoulSpend` (js/ui.js), combat hooks (js/combat.js). Headless-tested; in-app device/browser verification still outstanding. |
| ~~Challenge-mode fight logic~~ | ✅ Implemented Aug 16, 2026 — `CHALLENGES` (js/data.js), `isChallengeEligible`/`challengeActive`/`tickChallengeEscalation` + enforcement (js/combat.js), boss-intro opt-in (js/ui.js), `recordChallengeRelicEarned` (js/meta.js). All 5 heroes verified end-to-end across two real runs. |
| ~~Core-drop "first time" gating~~ | ✅ Implemented Aug 16, 2026 — `recordCoreCollected()` (js/meta.js) writes only on first-ever defeat. Lore *reveal* UI still unbuilt. |
| ~~Aldric Phase 3 gate rewire~~ | ✅ Implemented Aug 16, 2026 — `hasTrueEndingRelics()` (js/meta.js) read once at Aldric fight start. **The True Ending path is now wired end to end.** |
| ~~Challenge relics' in-fight effects~~ | ✅ Implemented Aug 17, 2026 — `ALDRIC_HERO_BEAT_EFFECTS` (js/combat.js). Each of the 5 heroes now has a distinct Phase 3 beat effect (Barbarian/Gambler unchanged, Vampire/Mage/Thief new), assigned to the four HP thresholds by `computeAldricRelicAssignment()`'s fixed-priority compaction. |
| ~~Aldric 50 HP "Use the Relics" choice~~ | ✅ Implemented Aug 17, 2026 — `showAldricMercyChoice()`/`acceptAldricMercy()`/`declineAldricMercy()` (js/combat.js). `showAldricEnding()` now gates on `G.aldricMercyChosen`, set only by actively accepting — holding the relic gate is necessary but no longer sufficient for the True Ending. |
| 1 remaining event | Build out the 18-event pool designed in GDD.md; 17 exist as of Aug 18, 2026 (5 generic + batch 1's 5 + batch 3's 3 + batch 4's 4) |
| Enemy intent consistency testing | Intent logic received April 16 improvements; verify displayed intent against actual actions before recording a Known issue. |
| Mobile UI polish | April 16 responsive, dice, preview, and overlap work landed; continue short-height and browser-chrome verification. |

---

## Session Log

| Date | Work Done |
|---|---|
| August 21, 2026 (bug fix — same-tick messages overwrote each other) | **`showMsg()` was a single slot with an unconditional `el.textContent = txt`, so two calls in one synchronous execution lost the first before the browser painted.** Reported case: Mage Challenge active + Pick Pocket on an Odd roll — `drawCards(g,2)` hits the Challenge denial chokepoint and messages, then the Odd branch immediately messages its 5 Gold, so the denial genuinely fired, genuinely worked (draw blocked, Gold granted) and was never visible. Not card-specific: any Denial Challenge (mage/thief/gambler) paired with any card that also messages. **Fix:** calls made in the same synchronous run now COMBINE with a `'  •  '` separator instead of overwriting; the batch is closed by a **microtask**, which is precisely what scopes it to one player action (microtasks drain after the call stack unwinds, before the next render). Single-message behaviour, the `ms` argument, and the existing `clearTimeout`/re-arm are all unchanged — a lone call is byte-identical to before. **Deliberately narrower than the brief's proposed condition**, which appended while the toast was still `visible`: that window is the full `ms` (2500ms default), so two unrelated cards played a second apart would have run together — a new visible problem rather than a fix, and the box wraps at `max-width: min(90vw,440px)` anchored `bottom: 180px`, so it grows upward over the hand area on short landscape screens. Measured a realistic 4-action turn chaining to 135 characters. To adopt the wider behaviour anyway it is one token: swap `el._batch` for `el.classList.contains('visible')`. Also noted while reading: the brief's `&& el._t` guard would have been a no-op, since `el._t` is set but never nulled and so is permanently truthy after the first message the game ever shows. **Verified** with a 49-check suite driving the real Pick Pocket effect through the real `drawCards()` denial and the real `showMsg()`, including a `textContent` setter that records every value ever assigned (so an overwritten-before-paint value is detectable), a replay of the pre-fix single-slot behaviour proving the denial was lost, all four Challenge×roll combinations, tick-vs-visible-window scoping, timeout re-arm on a combined message (full duration, not cut short), and 3+ messages in one tick preserving fire order. **Status: Verified.** |
| August 21, 2026 (⚠️ Known issue found, NOT fixed — event icon collision) | **`'💀'` is used by two different events: The Offering (`js/data.js` ~960) and The Soul Market (~1424).** Every previous event batch treated a shared icon as a bug on the stated grounds that two events with the same icon read as the same event recurring — the Gambler's Dice took 🦴 instead of 🎲 and the Bloodied Altar took 🔪 instead of 🩸 for exactly this reason. Found by the four `every icon unique` assertions in the event suites, which are **currently red on this one defect and nothing else** (1510 other checks pass). Left alone deliberately: it is a one-character content change, but *which* of the two events gives up 💀 is a design/content call, and 💀 is arguably more apt for a Soul Market (it is the HUD's own Souls glyph) than for The Offering, which held it first. Not decided inside an unrelated `showMsg` bug fix. |
| Early 2026 | Game concept locked. 5 characters, 4 floors, corrupted companions as bosses. Dice affinity system. Two currencies. |
| Early 2026 | Full card pools for all 5 characters (30 cards each including upgrades). |
| Early 2026 | Enemy roster all 4 floors. Boss debuff system. 18 events. Economy. Consumables. |
| Early 2026 | King Aldric 3-phase fight. True Ending relic system. Two endings locked. |
| Early 2026 | GDD v0.5 → v0.7. Floor hint system. Sir Crimson story arc early design. |
| April 2026 | Sir Crimson full arc locked. Rare relics (10) + Character relics (15) designed. PROGRESS.md created. All 5 hero card pools coded (Barbarian 24, Mage 27, Thief 26, Vampire 28, Gambler 26). All upgrade versions coded for all 5 heroes. Reward pool fix — now shows full card variety. Status display system overhauled — Rage/Weak/Vulnerable/Chill all update card preview numbers with color coding. Status tooltip system added — tap icon to see description. Exhaust pile viewer added to deck overlay. Exhaust cards return to deck after combat. Burn changed to stacks × 1 (matching Poison). Burn/Poison timing split — Burn before enemy acts, Poison after. Chill only ticks on attack turns. Cold Mastery correctly reduces Chill percentage. Entrench block carry fixed. Energy display changed to single number. Time Warp redesigned as draw-focused card. rollDice() updated with full Gambler mechanics. Vulnerable per-turn tick (not per-hit). Card Rarity System — CHAR_REWARD_POOLS restructured into common/uncommon/rare buckets for all 5 characters. Shared cards distributed correctly. curseddice removed. Pity timer added (G.rareOffset, caps at 35%). Elite rewards use separate odds 55/35/10. Scholar's Lens shows 4 reward options. Relic System — Full RELICS object built with 30 relics. hasRelic() helper added. All common and most uncommon/rare hooks wired into combat. healPlayer() routes all healing for pale_contract. shopCost() applies King's Debt multiplier. Deferred: bone_key, shattered_mirror, void_compass. Shop Overhaul — Relics section (2 relics, floor-gated by rarity). Die section (1 die, 80 gold). Card removal service (75 gold). Upgrade Card moved to rest stop only. Die Progression — Die removed from reward screen. Dice now from shop, Magic Door (25% chance, floor-gated), and events only. File Split — castle-run.html split into index.html + css/styles.css + js/data.js + js/combat.js + js/ui.js + js/game.js + js/main.js. GitHub Pages confirmed working. Mobile confirmed playable. |
| April 16, 2026 | Split-build follow-up: mobile selected-card preview fixes; deferred re-render so previews reflect status changes; intent overlap and enemy-intent presentation improvements; boss-intro scrolling and card-lift fixes; dice contrast and compact mobile dice controls; extensive responsive landscape layout, hand spacing, sprite sizing, and short-phone-height work. These changes are Implemented and require continued device/browser verification. |
| July 25, 2026 | Design-only session, nothing implemented. Full redesign of the True Ending / Souls / Cores system after discovering the old floor-reward relic gate was structurally impossible (floors 1-3 are the only relic sources, floor 4 is Aldric — no run could ever hold all 4). New system: 5 per-hero Challenge relics (need 4 of 5, never your own), Cores repurposed as lore + Challenge-unlock triggers, Souls converted from a permanent cross-run tree to an in-run resource spent at a per-floor stat-upgrade screen. All 5 Challenges fully designed (Barbarian, Vampire, Mage, Thief, Gambler). Also resolved this session: confirmed only 5 of the 18 designed events exist in code (PROGRESS.md's old "18 Complete" claim was false); fixed a live `lastFightWasElite` stale-flag bug causing `iron_ration`/`grave_robber` to misfire after boss fights following an elite; corrected the Normal Ending castle-outcome and two stale Hand Size Decision entries; fixed an inaccurate Burn tooltip. |
| August 15, 2026 | Doc sync only, nothing implemented. Propagated the July 25 redesign into `DESIGN_DISCREPANCIES.md`, `PROGRESS.md` (this file), and flagged the affected sections of `GDD.md` for the same treatment. Old True Ending Relics (4, floor-reward), permanent Soul tree, and "Events 18 Complete" all marked superseded/corrected rather than deleted, for history. Later same day: finalized the Soul-spend menu — 8 options (Vitality, Grit, Steady Hand, Second Die, Momentum, Overdraw, Reckless Surge, Gambler's Edge), offer 3 at random per spend window, built around explicit tradeoffs rather than pure stat boosts. Two options considered and cut during design: True Roll (min-roll floor — conflicted with Mage/Gambler/Thief all caring about natural 1s) and Twinned Roll (roll-twice-and-sum — broke Mage's and Vampire's affinity checks in opposite directions, overlapped with existing Twinned Die/Fractured Die relics). Second Die (optional +d2, ~50% parity-flip odds) replaced them. Design-only — Soul-spend screen UI/logic not yet built. |
| August 17, 2026 (Sir Crimson — Echo mimic, closing GDD §5) | **The last unbuilt piece of Sir Crimson's kit: every 3rd turn from fight start, he mimics a random card pulled from the player's own deck instead of his regular rotation move.** Confirmed the pause-not-advance default before writing any code (it was already fully specified, no override needed). **Card audit**: hand-read every entry in `CARDS` — 274 total, including the `'+'`-suffixed upgrades already merged into `CARDS` at script-load time (`Object.entries(CARD_UPGRADES).forEach(...)`, confirmed before starting so "every entry in `CARDS`" genuinely meant the full merged set, not base cards alone) — and classified each as Echo-compatible or utility-only. A card is compatible if its effect calls `dealDamage(g,'enemy',N)`, `gainBlock(g,'player',N)`, strips the target's Block (`g.enemy.block -= N`, the exact Shatter Step pattern), or `applyStatus(g,'enemy',...)`; when Sir Crimson becomes the caster these roles flip (damage lands on the player, Block goes to him, status lands on the player) exactly per GDD's own "block cards give him block, damage cards hit you, status cards apply to you." Anything without one of those four components — pure draw, self-cost, gold/energy/die manipulation, self-heal, a self-buff status on `'player'` (Regen, Rage, Momentum, Fly, every Power), or a stack-multiplier with nothing to multiply (Death Mark/Blood Tide without existing Poison/Regen) — has no mapping and is excluded by simple absence from the new `SIR_CRIMSON_ECHO_POOL` table (`js/data.js`) — there is no separate exclusion list; presence in the table IS the classification. "Full effect" (GDD: Echo "uses it against them at full effect") took the affinity-bonus/strongest branch, ignored hard play-gates (Backstab's first-card-only, Death Rattle's HP threshold) the same way Studied Blow's own numbers are unconditional, combined multi-hit cards into one total `dmg` rather than a hits-count, and used a card's own stated cap (Golden Strike, Betting It All) rather than live Gold/dice-roll for state-dependent scaling — cards with no such stated ceiling and no fixed value at all (Hedge Bet/Wild Card/All In, which scale directly and unboundedly with whichever die is currently equipped) were excluded rather than guessed. 160 of 274 entries ended up compatible. **The request's own shorthand notation (`strip: 8`, `status: 'Weak', stacks: 2`) was translated into the 5b-i engine's actual established field names** (`block` doubles as "amount gained" for a `'block'` move and "amount stripped" for `'attack+strip'`, `status` is the same `{name, stacks}` array shape `applyStatus()` already takes) rather than extending the engine to accept a second notation — flagged here rather than silently building something that looked different from what was typed. **Selection and resolution are two clean, separate steps, exactly as required:** `pickSirCrimsonEchoCard(g)` filters `g.deck` to compatible keys and picks uniformly among them (equivalent to "reroll past utility-only cards" but without an actual rejection loop, and correctly weighted by however many copies of a card the player actually holds, since the filter runs directly over the deck array rather than a de-duplicated set); `resolveSirCrimsonEcho(g)` applies whatever combination of `dmg`/`block`/`strip`/`status` the picked card carries through the exact same `enemyAttackDamage()`/`resolveEnemyAttack()`/`applyStatus()` pipeline every other Sir Crimson move and every other enemy attack already uses — no parallel damage math. **Turn gating required tracing the exact index-advance timing through several hand-worked turn-by-turn traces before the code was right — the first version had a real off-by-one that would have made Echo repeat the just-executed move instead of pausing and resuming the correct upcoming one.** The fix: STEP 10 now branches three ways — if the turn just ending was itself an Echo turn, the current index is left untouched and re-previewed for next turn (this is the resume); otherwise the index advances normally, and *then* a second check decides whether the *upcoming* turn will be an Echo turn, in which case the mimicked card is picked right there (telegraph-time — one turn ahead, matching the standard preview timing every other move already gets for free) and the freshly-advanced index is left waiting rather than executed. Verified by tracing a real 12-turn run turn-by-turn: the sequence lands on exactly Crimson Strike, Iron Guard, [Echo], Shatter Step, Studied Blow, [Echo], Crimson Strike, Iron Guard, [Echo], Shatter Step, Studied Blow, [Echo] — every paused move resumes at exactly the position it was frozen at, nothing skipped, nothing repeated, across two full cycles. **A real, reproducible test-infrastructure issue was tracked down during verification, not papered over with a bigger timeout:** several multi-turn trace suites intermittently appeared to "hang" waiting for a turn to advance. Directly instrumenting every function in the chain (`endTurn`, `startTurn`, `checkCombatEnd`, the new Echo functions) proved every single call always completed successfully with no error and the correct turn number — the hang was never a hang at all: these suites drove 9-12 real turns of Sir Crimson's genuine (now-lethal) damage output with the test player never blocking or healing, which legitimately brought HP to 0 (confirmed directly: HP logged as negative at the exact "stuck" point across multiple repro runs), and `endTurn()`'s STEP 11 correctly refuses to schedule another turn once that happens — a real, working game-over, not a bug, just a test that hadn't accounted for it. Fixed by topping HP back up once per simulated turn in the test harness itself, isolating each turn's damage for measurement exactly as intended; every suite has passed consistently across many repeated runs since. **Verified:** Echo fires on exactly turns 3, 6, 9, 12 and nowhere else, confirmed by a real 12-turn run, not a formula check; the regular rotation resumes at precisely its pre-Echo position every time, confirmed across two full cycles; the telegraphed card name and every one of its stored numbers (damage, Block, status stacks) match what actually resolves, byte-for-byte, checked by capturing the pick at telegraph-time and comparing against the real HP/Block/status deltas after resolution; a compound damage+status pick (Skull Crack, forced deterministically) applies both components, not just one; a compound damage+strip pick (Crushing Blow) correctly strips whatever Block survives its own damage's absorption, matching Shatter Step's own established order; a 500-pick statistical sweep against a deck deliberately salted with several utility-only cards never once selected one; three full Echo cycles with a different random deck each time all resolved consistently with a valid, matching pick and no cross-cycle state corruption. Full regression sweep (19 suites spanning every prior batch this session) re-run clean; two now-superseded assertions from earlier Sir Crimson suites (one in the batch-5b-ii fight suite, one in the restructure suite) are marked retired rather than left silently contradicting the new behavior — both were written before Echo existed and naively sampled the rotation index without accounting for the pause, which is now correct, intended behavior independently confirmed by this batch's own dedicated coverage. Screenshot-checked: the intent box correctly names the mimicked card and shows its exact numbers rather than a generic "Echo" label. **Status: Verified.** This closes GDD §5 — Sir Crimson's full kit (both story beats, the smoke interruption, the shadow-themed Rest-stop, and now every one of his four rotation moves plus Echo) is built and Verified end to end. |
| August 17, 2026 (Aldric — mercy choice + Challenge relic effects) | Two changes to King Aldric's Phase 3, both design-requested: (1) each held Challenge relic now fires a **distinct** beat effect instead of four generic unattributed placeholders — Barbarian (loses all Strength) and Gambler (infinite reroll) keep their existing effects; Vampire, Mage, and Thief got new ones (Vampire drains 8 HP from Aldric to the player — the same mechanic and number he uses against you in his own Challenge, turned back on him; Mage grants persistent +1 card draw for the rest of Phase 3, not a one-time burst; Thief grants a flat +15 Block at that beat). Which relic lands on which of the 4 HP thresholds (100/75/50/25) is no longer fixed — `computeAldricRelicAssignment()` (js/combat.js) applies a fixed priority order (Barbarian, Vampire, Mage, Thief, Gambler), skipping whichever relic isn't held and compacting the rest into the 4 slots in order; if all 5 are held, the current hero's own relic is excluded first, then the same compaction runs. (2) **The True Ending trigger changed from automatic-on-killing-blow to an active choice.** At the 50 HP beat specifically, if the relic gate is open, a mercy prompt now appears alongside that beat's relic effect (`#aldric-mercy-overlay`) — accepting ends the fight immediately (reusing `checkCombatEnd()`'s own win-resolution path rather than duplicating it) and grants the True Ending; declining continues the fight normally toward a real kill, which now only gives the Normal Ending. `showAldricEnding()`'s check moved from `G.aldricHasRelics` (the gate) to a new `G.aldricMercyChosen` flag, set only inside `acceptAldricMercy()` — holding the gate is now necessary but not sufficient. The old `G.aldricStopped` flag/mechanic ("Aldric stops attacking forever") was fully retired since no hero effect in the new system uses it. **Verified** via 4 real Playwright suites, run repeatedly for stability: priority-compaction across every held-relic combination including the 5-of-5 exclusion case; each hero's beat effect firing correctly, with Mage's draw bonus confirmed to persist turn-to-turn (not fire once) across several simulated turns; the mercy prompt appearing only when the gate is open and never otherwise; a full real end-to-end run accepting mercy (True Ending) and a separate full real run declining it (Normal Ending), plus the already-existing gate-closed and loss-before-Phase-3 cases both still correctly giving the Normal Ending. Full regression sweep (26 suites spanning every prior batch this session) re-run clean, including 2 already-known pre-existing failures unrelated to this batch. |
| August 18, 2026 (Consumables — batch 1: inventory model + in-combat use UI) | First runtime implementation of the previously fully-unbuilt Consumable system (10 items designed, 0 built) — scoped to 2 of the 10 items (Health Potion, Block Stone) to establish the inventory model and UI before building out the rest. `G.consumables` (js/game.js) is a new per-run array, capped at 3, initialized empty in `newGame()`'s literal alongside `G.relics` — same lifecycle: persists across every `startCombat()`/`startBossFight()`/`startAldricFight()` within a run, reset only by a fresh `newGame()`. `CONSUMABLES` (js/ui.js, placed next to `RELICS` — confirmed via direct verification to be its real location, not js/data.js as this file's own architecture summary might suggest) holds the two entries, each a real callable `effect(g)` (unlike `RELICS`' string-tag convention) that reuses `healPlayer(g,20)`/`gainBlock(g,'player',15)` directly — no new effect logic needed, so both inherit `healPlayer`'s existing max-HP clamp and Pale Contract healing-tax automatically, and `gainBlock`'s existing Thief-Challenge block-denial. `grantConsumable(key)` mirrors `acquireRelic()`'s push+showMsg pattern but adds the slot-cap rejection check that relics don't need, and no dedupe (consumables stack; relics are one-of-each). `useConsumable(index)` (js/combat.js) is bounds-checked, applies the effect, splices the used item out, and re-renders. New in-combat UI: `#consumable-slots`, a small click-to-use row (renders only held items, no empty placeholders) floated above `.dice-panel` via the same `position:absolute` technique already proven safe for `#soul-die-controls`, anchored on the opposite side (`left:0` vs `right:0`) to avoid collision; `renderAll()` now calls `renderConsumableSlots()` alongside its other per-action re-renders. `js/debug.js` gained a `consumables=<comma-list>` option mirroring the existing `relics=` parsing convention, since no real (non-debug) acquisition path exists yet. **Verified** via 3 real Playwright suites (31 assertions total), each run twice for stability: debug grant → correct slot UI text, unknown-key rejection, 3rd item correctly filling the last slot, 4th item correctly rejected once full without corrupting the existing 3; both items' effects resolving for the exact designed amounts (including the max-HP clamp inherited for free), no Energy cost, single-use removal — including a middle-slot removal case confirmed to shift the array correctly without touching its neighbors; using an empty inventory or an out-of-range/negative index confirmed as true no-ops; playing a real hand card while consumables are held/rendered confirmed zero cross-contamination in either direction; inventory confirmed to survive all three real fight-start functions unchanged and to reset correctly on a fresh `newGame()`. Screenshot-checked at both desktop (1280×800) and mobile-landscape (812×375) — the slot row is legible and does not overlap the hand, dice, or Energy/HP HUD at either width. Full regression sweep (26 suites spanning every prior batch this session) re-run clean, including the 2 already-known pre-existing failures unrelated to this batch. **Status: Verified**, scoped to the 2 items built — the remaining 8 designed items and any real (non-debug) acquisition path (shops, events, magic doors, chests, per the original design) are Deferred to a later batch. |
| August 18, 2026 (The Sleeping Shade — 18th GDD-named event, first real `startEventCombat()` user) | Two calibration questions confirmed with the project owner before implementing: (1) the enemy's proposed HP 85/damage 13/no-special stats were compared against every real Floor 3-4 enemy in `js/data.js` — HP matched Blood Cultist exactly, but damage sat low and, more tellingly, every single named Floor 3-4 enemy carries a `special`, making a special-less guard at this weight the only exception in the late-game roster; decided to keep HP/damage as proposed and add a modest one-time **Night Terror** burst (turn 3 only, +15 bonus damage) in the same established pattern as Blood Cultist's Ritual and Royal Sorcerer's Arcane Overload; (2) since events are picked with zero floor-gating anywhere in the codebase (`rand(EVENTS)`, no `minFloor` concept exists for events at all) while rare relics themselves are floor-gated (`RELIC_RARITY_MIN_FLOOR.rare` is 2), an early encounter or a fully-owned rare pool would otherwise leave a real win granting nothing — decided the victory grant falls back to whatever `offerableRelics()` does offer rather than a wasted win. Built: `SLEEPING_SHADE_ENEMY` (`js/data.js`, event-only, not in any `FLOOR_ENEMIES` pool) and the 18th `EVENTS` entry with 3 choices — Wake it (real fight via `startEventCombat()`, victory grants one relic filtered to `rarity==='rare'` from `offerableRelics()` with the fallback above), Sneak past (true no-op), Leave an offering (-20 Gold, guaranteed `rarity==='common'` relic, refusing without proceeding if unaffordable or fully owned — the same affordability-refusal convention every other event choice already follows). **This is the first real event to call `startEventCombat()`**, so its explicit "delete once a real event supplies its own enemy" throwaway plumbing — `DUMMY_EVENT_ENEMY`, `startDummyEventCombat()`, and `js/debug.js`'s `eventcombat` target — was deleted, and `CLAUDE.md`'s debug target list updated to match; confirmed via direct source grep that nothing else in the repo referenced any of the three names before removing them. **Verified** via a dedicated Playwright suite, run 5 times for stability: icon/title/desc/3-choice rendering; the fight is genuinely playable (normal hand/dice/Energy/card-damage resolution, not a stub); the Night Terror burst fires only on turn 3 (13 → 28 → 13); victory grants exactly one relic while the standard gold/soul reward chain stays untouched; both fallback paths (floor too early, all rares owned) and the absolute-empty-pool case (every offerable relic owned) each verified independently; a loss reaches the game-over screen normally; the offering's happy path, insufficient-gold refusal, and all-commons-owned refusal are all correct. **One real test-authoring pitfall caught mid-verification, not a game bug**: an early draft of the turn-3 burst test assumed the enemy attacks every turn and flaked intermittently — traced to genuine pre-existing behavior (generic non-scripted enemies randomize their next intent 65% attack / 35% defend, `js/combat.js` STEP 10, unrelated to this event), fixed by forcing `G.enemy.intent = 'attack'` before each simulated turn rather than chasing a timing theory that wasn't the real cause. Full cross-batch regression sweep (28 suites, including after the dummy-plumbing deletion) re-run clean. **Two failures surfaced that are unrelated to this batch, one already known and one newly identified**: the already-documented `sircrimson_restructure2.js`/`sircrimson_fight.js` retired assertions, plus a previously-unnoticed stale assertion in `sircrimson_fight2.js` — its reachability check counts literal-text occurrences of `startSirCrimsonFight()` across every script file including two unrelated comment mentions, confirmed via direct grep to predate this session and to be untouched by anything in this batch (neither file it scans was edited here); flagged for whoever next touches that suite rather than fixed as a scope addition. Screenshot-checked at desktop: the event screen and the live fight screen (HP 85/85, `Preparing: Attack 13` with the Night Terror special hint) both render correctly. **Status: Verified**, scoped to this one event — 5 GDD-named events remain entirely unbuilt (The Trapped Knight, The Starving Wolf, The Locked Chest, The Cracked Mirror, The Soul Market) and 5 pre-July-25 generic placeholders still occupy slots in `EVENTS` alongside the 13 GDD-named ones now built; see the Events section's count correction above. |
| August 18, 2026 (Diagnosing the recurring Sir Crimson test failures) | `sircrimson_restructure2.js`, `sircrimson_fight.js` and `sircrimson_fight2.js` had each been flagged as failing in three separate, unrelated sessions (Consumables batch 5, Consumables batch 4, Events batch 5), confirmed "pre-existing" each time, then left alone — three flags against the same feature family with no actual root-cause was treated as suspicious rather than settled. Ran all three in isolation with full output captured, and diagnosed all three as **false-positive failures in already-correct game behavior**, for two distinct reasons — not flakiness, not a live product bug. **`sircrimson_restructure2.js`** and **`sircrimson_fight.js`** were each already self-diagnosed and annotated as retired in their own header comments from earlier sessions (the former: Upgrade/Remove Card correctly now lead into the real 160 HP fight instead of the batch-5a/5b-i instant-resolve stub the assertion still expected; the latter: the rotation-order assertion predates the Echo mimic batch and doesn't account for Echo's turn-3 pause/resume, so `Shatter Step`/`Crimson Strike` legitimately sampling twice in a row is the intended pause signature, not corruption) — both had been correctly root-caused before, but the actual failing assertions were never fixed or removed, so they kept re-triggering and getting re-investigated every subsequent sweep. **`sircrimson_fight2.js`** was a different, previously-undiagnosed failure mode: its reachability check counts literal-text occurrences of `startSirCrimsonFight()` across every script file to verify exactly one real call site exists, but a plain text search can't distinguish a real call from a comment that merely mentions the function name — two such unrelated comments (in `js/ui.js` and `js/combat.js`, discussing `showCombatScreen()` and `extraDraw` banking respectively) pushed the raw count from the assumed 2 to 4, even though ground-truth grep confirms exactly one real call site has always existed. **Fixed all three** rather than re-flagging again: the two stale assertions in `sircrimson_restructure2.js` now check the correct current behavior (`activeScreen === 'combat-screen'`, `G.enemy.isSirCrimson === true`, `enemyHp === 160`, `fought === false`) instead of the retired stub outcome; `sircrimson_fight.js`'s expected sequence now bakes in the Echo pause/resume (`['Crimson Strike','Iron Guard','Shatter Step','Shatter Step','Studied Blow','Crimson Strike','Crimson Strike','Iron Guard']`) instead of a naive clean repeat; `sircrimson_fight2.js`'s reachability check now strips `/* */` and `//` comments before counting, so it measures code-level call sites rather than raw text matches (reports both `rawOccurrences` and the filtered `callSites` for transparency). All three verified passing individually and stable across repeated reruns, then the full cross-batch regression sweep (29 suites) re-run clean, with one unrelated flake reproduced and cleared on rerun in `echo_mechanic.js` (a long multi-turn simulation with pre-existing timing sensitivity, untouched by this session's edits — 3/3 clean on immediate rerun). **The actual lesson**: annotating a stale assertion as "retired" without removing or fixing it doesn't stop it from failing — it just guarantees the same false alarm gets rediscovered and re-investigated indefinitely. All three files are now genuinely green, closing this recurring loop. |
| August 17, 2026 (Sir Crimson batch 5b-ii — his actual fight, final) | **`startSirCrimsonFight()` is now a real, losable fight** — HP 160, his 4-move rotation wired through the 5b-i multi-move engine (`js/combat.js`), replacing the stub that had stood in for it since batch 5a. **The move data uses the 5b-i engine's own established field names, not the request's shorthand notation, and this translation is deliberate and documented in code:** `strip:8`/`status:'Weak', stacks:2` became `block:8` (the engine's existing "amount stripped" field for an `attack+strip` move, reused from how `'block'`-type moves already use `block` for "amount gained") and `status:{name:'😵Weak', stacks:2}` (the exact shape `applyStatus()` already takes everywhere else in the codebase, and the emoji-prefixed key every other status in the game already uses) — the engine was built generic last batch specifically so a new boss's kit is only ever data expressed in its existing schema, not a reason to extend the schema. GDD's "telegraphed one turn early" for Studied Blow becomes `telegraphTurnsEarly: 2` — every move already gets the engine's default one-turn-ahead preview for free, so "one turn earlier than that" is 2 turns out, exactly what `findEarlyTelegraphedMove()` checks for; this is not a reinterpretation, it's the same value the design brief itself specified. The GDD's Echo mimic move ("every 3rd turn, pulls a random card from your deck and uses it against you") is **not built** — outside this batch's explicit move list, flagged rather than silently included or silently dropped. **A real correctness problem was found and fixed before it could ship, not caught by review:** the natural first design — give Sir Crimson `G.inBoss = true` like a real boss fight (GDD: "Full boss-level difficulty") and let his win resolve through the ordinary `checkCombatEnd()` win branch — would have made `checkCombatEnd()` treat his death as an ordinary floor-boss clear: `G.map[G.currentFloor].boss` would resolve to Floor 2's *already-defeated* boss (since `G.currentFloor` is still 2 at this point in the sequence), incorrectly re-pushing that boss's core, re-showing its "Core collected!" toast, granting a second helping of boss gold, fully healing the player, and — critically — incrementing `G.currentFloor` from 2 to 3 immediately, *before the player has even picked their real Floor 2 card reward*, silently corrupting the run's floor progression. Caught by tracing every `G.inBoss` read in the codebase before writing any fight-start code, not discovered by testing afterward. Fixed with a `G.enemy.isSirCrimson` flag checked at the very top of `checkCombatEnd()`'s enemy-death branch, short-circuiting before any of the gold/soul/relic-hook/cores logic runs, then handing off exactly where the stub did (`G._sirCrimsonFought = true; showSirCrimsonOutro();`) — satisfying "no reward, no gold, no heal" *literally*, which is deliberately stricter than how Aldric's own win is already allowed to work (his death still incidentally picks up `bloodsoaked_rag`/`ash_pendant`/`tarnished_coin` and a flat +3 Souls before his own `isFinalBoss` branch takes over later in the same function — a pre-existing, accepted quirk this batch does not touch, since Sir Crimson's requirement is explicit where Aldric's was never stated either way). `G.inBoss` itself stays `true` during his fight for GDD's "full boss-level difficulty" framing (Iron Vambrace/Hollow Throne Block, Ashen Crown energy, Ley Line Crystal/Loaded Coat/House Always Wins resets, and every other per-combat relic hook `startBossFight()` already grants all apply normally, confirmed safe specifically because the `isSirCrimson` short-circuit is what stops it from mattering at combat's end) — this is intentionally *not* the same exclusion Aldric gets (`stageCombatStartBlock`/`applySoulCombatStart` run with their normal `includeRelics`/`includeDraw` defaults, unlike Aldric's documented gap), since nothing in GDD calls for Sir Crimson to deny relics the way the final boss does. `startSirCrimsonFight()` itself moved from `js/ui.js` (where the placeholder stub lived, alongside the other Sir Crimson screen-flow functions) to `js/combat.js`, alongside `startCombat()`/`startBossFight()`/`startAldricFight()` — the file split in `CLAUDE.md` puts fight-setup logic in `combat.js`, and this function earned that classification the moment it became a real fight-start function rather than a two-line placeholder. **Verified by driving the real `checkCombatEnd()` → `showReward()` → confrontation → Rest-stop → fight chain end to end, including a genuine win and a genuine loss, not simulated ones:** HP is exactly 160 and the 4 moves are in exact GDD order (Crimson Strike/Iron Guard/Shatter Step/Studied Blow) with exact numbers (12/14/8+8-strip/15+Weak 2); a full 8-turn rotation driven through real `endTurn()` calls cycles and wraps in that exact order twice over; Studied Blow's early hint is invisible at 3 and 4 turns out, appears at exactly 2 turns out without displacing the intervening move's own main-line preview, and becomes the ordinary main-line preview at 1 turn out; Shatter Step's 8 damage is absorbed by Block first when Block is high enough (confirmed via `resolveMoveTurn()` called directly, after an earlier version of this same check gave a false failure by reading `G.block` *after* the next `startTurn()` had already run its own Block-reset — fixed by inspecting the value at the correct moment instead of after a full turn-cycle wait) and the strip then removes exactly 8 more from what's left, while a 0-Block case shows the 8 damage landing on HP directly with nothing left to strip; a real loss — HP brought down and finished off by Sir Crimson's own actual attacks across real turns, not `G.hp` set to 0 directly — reaches the game-over screen with the smoke overlay, rest-stop screen, confrontation screen, and outro screen all confirmed not left stuck active; a real win (a real killing blow, not a simulated flag flip) leaves Gold, Souls, `G.cores`, HP, and the deck all byte-identical to their pre-kill values, reaches the outro, and dismissing it reveals the exact same 3 reward cards generated before the interruption ever began; and `startSirCrimsonFight()` has exactly one call site in the entire codebase (fetched and grepped the actual served source files directly, after an initial DOM-script-text scan gave a false negative — `document.scripts[i].textContent` is empty for every externally-loaded `<script src>` file, which is 100% of this codebase's scripts) beyond its own definition, Sir Crimson appears in no enemy/elite/boss pool, and 8 normal fights plus a real floor boss never spawn him. Full regression sweep (17 suites) re-run clean after modifying `checkCombatEnd()` again. Two prior sessions' test files are marked retired at the top rather than silently left contradicting the new behavior: `sircrimson.js` (batch 5a, already retired for its Floor 2/3 sections) and `sircrimson_restructure2.js` (this session — its "leads to the outro via the stub fight" assertions were true only while the fight was a stub, and are now correctly superseded by `sircrimson_fight.js`/`sircrimson_fight2.js`'s coverage of the real thing). Screenshot-checked: Sir Crimson renders at 160/160 HP with the correct per-move intent text and the Studied Blow early-telegraph warning, inside a genuinely playable combat screen. **Status: Verified.** This closes out Sir Crimson's mid-run encounter — story beats, trigger placement, the smoke interruption, the Rest-stop variant, and the fight itself are all built and Verified; only the Echo mimic move remains for a future batch, and per `DESIGN_DISCREPANCIES.md`, Sir Crimson's own backstory is still an open GDD TBD. |
| August 17, 2026 (Sir Crimson — Floor 2/3 confrontation restructured, smoke interruption) | **Supersedes the Floor 2/3 half of what batch 5a built — the Floor 1/2 shadow beat is completely untouched.** The confrontation's trigger moved from `proceedOrPath()` (the end of the whole reward chain, after the card, boss relic, and Soul-spend screens had all already run) to `showReward()` itself, interrupting the very moment the real Floor 2 boss's card-reward screen renders — the player briefly sees their actual reward, then the interruption plays out, and only on victory does that same, never-regenerated reward screen become interactive again with the chain proceeding normally from there. **Three structural questions were confirmed before writing any code, not guessed:** (1) the black/red smoke is a fade-transition that masks screen swaps (fade in over whatever's active → swap underneath while fully opaque → fade out), used only for the two moments the build spec names explicitly — entering the interruption and revealing the reward screen on victory — not sprinkled between confrontation → rest → fight, which are plain `showScreen()` swaps exactly like every other screen transition in the game; (2) the shadow-themed Rest-stop variant is a full duplicate (`#crimson-rest-screen`, `showSirCrimsonRest()`/`startSirCrimsonRestPick()`/`cancelSirCrimsonRestPick()`/`renderSirCrimsonRestDeck()`) calling the exact same `healPlayer()`/`upgradeCard()`/`removeCardFromDeck()`-style mechanics as the real Rest room, rather than branching the real functions — zero risk of a Sir-Crimson-specific code path ever touching the real Rest room; (3) `startSirCrimsonFight()` stays the batch 5a/5b-i stub (still resolves immediately into the outro) rather than getting a placeholder move set this session, so the defeat-path requirement is verified structurally (confirming `checkCombatEnd()`'s existing, completely unmodified defeat branch — same `restoreLoadedCoatDie()`/`showGameOver()` calls — would fire normally) rather than through an actual live loss, which the stub cannot currently produce. **A real timing gap was found and closed during implementation, not discovered by review:** the initial design set the smoke overlay's own `pointer-events:auto` only once it became visible, leaving the ~1.2s "let the player register their real reward" delay beforehand fully clickable — a fast Skip Reward or card pick in that window could reach a later screen (boss relic offer, Soul spend) before the smoke ever appeared, breaking "the smoke interrupts THIS render" and risking the reward being claimable twice once revealed again. Fixed by disabling `#reward-choices`' pointer-events and hiding the Skip button *synchronously*, the instant the interruption is scheduled — not gated on the smoke's own visibility — and restoring both explicitly in `dismissSirCrimsonOutro()` once the original screen is revealed. **The "no regeneration" guarantee falls out of `showScreen()`'s own existing behavior for free**: it only toggles which element has `.active`, never touches a screen's rendered DOM, so navigating confrontation → rest → outro and back to `#reward-screen` — without ever calling `showReward()` a second time — naturally reveals byte-identical content with no snapshot/restore logic needed. **Verified by driving the real `checkCombatEnd()` → `showReward()` → interruption chain end to end, not by calling the new functions in isolation**: the exact same 3 card names render before the interruption and after the victory reveal; a real Playwright hit-tested click (not a native DOM `.click()`, which ignores CSS `pointer-events` entirely and gave one false pass during verification before being corrected) genuinely cannot reach a reward card during the pre-smoke delay, and the deck length is unchanged after the attempt; all three Rest-stop options (Rest's exact 30%-of-max-HP heal, Upgrade Card producing the correct `+` key, Remove Card splicing the deck, and Cancel returning to the option grid) behave identically to the real Rest room's own formulas and mechanics; a simulated mid-sequence `G.hp <= 0` still reaches the ordinary, unmodified game-over flow with no smoke left stuck visible; and the Floor 1/2 shadow beat still fires exactly once and resets correctly on a fresh run, unaffected by any of this. Full regression sweep (16 suites spanning every prior relic/HUD/engine batch) re-run clean after touching the shared `showReward()`/`proceedOrPath()` chokepoints, with the same single pre-existing, already-documented House-Always-Wins RNG-timing flake reproduced (unrelated — that suite never sets `G.currentFloor` to a boss-clear context this code path checks). Batch 5a's own test suite is marked retired at the top of its file rather than silently left contradicting the new design — its Floor 1/2 sections still pass, its Floor 2/3 sections encode the superseded behavior on purpose. Screenshot-checked at desktop: the reward screen visibly reddens as the smoke fades in, and the confrontation/rest-stop screens render correctly with the Rest tile correctly disabled at full HP. `newGame()` gained one new field (`_sirCrimsonInterrupting`, guards against re-scheduling the interruption if `showReward()` is somehow re-entered before its delayed trigger fires). **Status: Verified** for the restructure; the fight itself and its real numbers remain 5b-ii's job. |
| August 17, 2026 (Sir Crimson batch 5b-i — generic multi-move boss rotation engine, infrastructure only) | **Every enemy in the game — floor enemies, elites, all 5 corrupted-companion bosses, Aldric — has always had exactly one flat `damage` stat and a binary `intent: 'attack'\|'defend'`.** This batch adds a second, generic path any boss can opt into with an `moves: [{ name, type, damage, block, status, telegraphTurnsEarly }]` array (`type` one of `'attack'\|'block'\|'attack+debuff'\|'attack+strip'`) — built reusable rather than Sir-Crimson-specific, the same way `spendGold()`/`loseHP()`/`checkAffinity()` were, per this batch's own design brief. **No current enemy/boss data has a `moves` array** (confirmed before writing any code — `ENEMIES`/`FLOOR_ENEMIES`/`EASY_ENEMIES`/`ELITES`/`BOSSES` are uniformly one flat number each), so every new branch is gated on `G.enemy.moves` being truthy and non-empty; the pre-existing flat-damage code in `endTurn()` STEP 6/10 and `updateIntent()` is untouched byte-for-byte in its own branch, reached whenever that check is false — which is always, today. **Reused the existing shared damage pipeline rather than writing parallel math**: `enemyAttackDamage(g, consumeChill, baseOverride)` already had a `baseOverride` extension point (previously exercised only by Aldric's per-phase attacks via `aldricAttackProfile()`), so every attack-flavored move type feeds its own `damage` field through that same parameter — Rage, Weak, Chill, Vulnerable, Fly, and Block absorption all apply identically to a moves-based boss's attacks, with zero new damage-modifier code. New `resolveMoveTurn(g, move)` dispatches STEP 6's execution by `move.type`; `'block'` grants `g.enemy.block += move.block` directly (matching the existing convention — confirmed `gainBlock()` has no `'enemy'` branch today, so every current enemy Block grant, e.g. the Defend branch, Castle Guard's Shield Up, Aldric's Stone Heart, already bypasses it the same way); `'attack+debuff'` calls the existing `applyStatus(g, 'player', ...)`; `'attack+strip'` needed an all-new block-strip implementation (confirmed nothing generic existed — only two one-off inline `g.block -=` mutations in Crypt Crawler/Blood Bat's `special.effect` closures) and, absent a GDD-specified order, **deliberately follows that same precedent's timing**: the strip acts on whatever Block survives the move's own attack damage, not Block before it, since both existing analogues fire their similar effect from `special.trigger:'attack'` (i.e. after the attack already resolved) rather than before. **Rotation state** is a fixed cycle per the confirmed default — `G.enemy._moveIndex` advances by 1 and wraps at `STEP 10` (the exact line that previously ran `Math.random() < 0.65 ? 'attack' : 'defend'`), no weighted-random selection. **Generalized telegraph**: `updateIntent()` gained a parallel branch (an early `return`, so the legacy branch beneath it can never be reached when `moves` is set, and is completely unreached — i.e. functionally deleted-and-restored, not modified — when it isn't) that renders per-type text reusing the exact same Rage/Weak/Chill/Vulnerable-aware bold/strikethrough dual-number technique the flat-damage preview already uses, fed the move's own `damage` instead of `e.damage`. **Extra-early telegraph required peeking past the immediately-next move**, since the standard "next turn's move" preview IS just `moves[_moveIndex]` — nothing needed to change for the default case. New `findEarlyTelegraphedMove(moves, moveIndex)` scans forward (bounded by the rotation's own length) for the soonest move whose `telegraphTurnsEarly` is at least as large as its actual turns-until-execution, and — since the single intent box can't show two full lines — renders that as a small secondary hint appended below the main line (the same "supplementary div below the strong tag" pattern the existing `⚡ special ability` hint already establishes), rather than replacing the main line's own standard one-turn-ahead content. **No existing precedent to reuse or stay compatible with**: confirmed by search that "telegraph" appears nowhere in any `.js` file before this batch — Aldric's own HP-threshold pauses (`ALDRIC_RELIC_TRIGGERS`) are discovered and resolved on the same turn they trigger, never previewed a turn ahead, so this is new ground rather than an extension of an existing mechanism. `el.classList.toggle('intent-pulse', ...)` — previously an exact `e.intent === 'attack'` string match — was widened to treat `'attack+debuff'`/`'attack+strip'` as attack-flavored too, inside the new branch only, so a future Sir Crimson debuff/strip move pulses the intent box like any other attack rather than silently losing the animation the way a naive copy-paste of the old check would have. **Verified with a synthetic 2-3(-4) move test-boss set (not Sir Crimson's real kit)**, exactly as scoped so this batch is independently testable ahead of 5b-ii: the rotation cycles `0,1,2,0,1,2,...` and wraps correctly; telegraph text matches each of the four types (plain attack, block, attack+debuff, attack+strip) one turn ahead by default, including the Rage-modifier-aware dual-number preview firing per-move; a move flagged `telegraphTurnsEarly: 2` correctly gains its secondary hint exactly two turns out (not three, not one) while the main line still shows the intervening move normally, and becomes the ordinary main-line preview once it's genuinely one turn away — confirmed the rotation's actual execution order is completely unaffected by the flag (`Strike, Guard, StudiedBlow, Strike, Guard, StudiedBlow`, not reordered to front-load it); resolution applies the correct effect for all four types, including a corrected test assumption during verification — an initial "attack+strip strips N Block" check used a starting Block value high enough that the move's own attack fully absorbed through it first (correct, expected behavior, matching the Block-absorption math every other attack already uses), which looked like a failure until the test's own expected values were fixed rather than the code; a real `endTurn()` call (not just the extracted functions in isolation) confirmed STEP 6 and STEP 10 integrate correctly together; and every existing single-damage enemy, boss, and Aldric — including a fresh `startAldricFight()` call mid-suite — produced byte-identical intent text, resolution behavior, and `processAldricTurn()` dispatch, with `G.enemy.moves` confirmed absent throughout. Full 16-suite cross-batch regression sweep re-run clean after modifying the shared `endTurn()`/`updateIntent()` chokepoints, with one pre-existing, already-documented flake reproduced in the House Always Wins suite (a real-RNG timing race in that suite's own `__advanceTurn()` helper, unrelated to `G.enemy`/intent — none of this batch's new code paths execute without `G.enemy.moves` set, which that suite never does) — not a regression from this work. **Status: Verified** for the engine itself; Sir Crimson's own four moves, numbers, and the Echo mimic remain for batch 5b-ii. |
| August 17, 2026 (Sir Crimson batch 5d — final dialogue, replacing batch 5a's placeholders) | **All three `js/ui.js` dialogue constants (`SIR_CRIMSON_SHADOW_LINE`/`SIR_CRIMSON_CONFRONTATION_LINE`/`SIR_CRIMSON_OUTRO_LINE`) now hold owner-supplied final copy** in place of batch 5a's explicitly-flagged placeholder text — a one-line swap each, exactly as scoped when that gap was raised. **The post-fight line also replaces GDD.md's stale "four pieces" wording** (`GDD.md:289`) with matching text, closing the doc/code disagreement the same way the Cores/True-Ending-gate discrepancy was closed earlier this session — `js/ui.js` and `GDD.md` now read identically for this line rather than the code silently diverging from a doc line already known to reference a relic system the game no longer has. The shadow and confrontation lines remain code-only, matching how GDD's Story Arc table (`GDD.md:259-264`) only ever described *what happens* at those two beats rather than giving a literal transcript — the post-fight line was the only one of the three GDD ever specified text for, so it's the only one with a GDD-side line to update. **Verified:** all three constants match the supplied text exactly, with no leftover trace of the old placeholder/stale strings; re-ran batch 5a's own mobile-landscape (780×380) overflow check against the new — longer — text, since final copy is rarely the same length as placeholder copy: each dialogue box itself renders fully unclipped (`scrollHeight === clientHeight` on the text element in all three cases), and while the confrontation and outro *screens* now overflow the short viewport by a few more pixels than before (20px and 40px respectively, up from batch 5a's placeholder-text numbers), both remain scrollable via the same pre-existing `overflow-y:auto` safety net already documented on `#boss-intro-screen`, with the CONTINUE button confirmed reachable by scroll — not a new regression, the same known short-landscape characteristic carried forward. Desktop confirmed to render the full outro text (the longest of the three) with no `text-overflow:ellipsis` or clipping. Screenshot-checked at both viewports. `DESIGN_DISCREPANCIES.md`'s placeholder-dialogue entry is marked Resolved. **Status: Verified** for dialogue content; the fight itself remains the only unbuilt piece of this arc. |
| August 17, 2026 (Sir Crimson batch 5a — story beats & trigger placement, no fight logic yet) | **Sir Crimson's two mid-run story beats (GDD §5) now exist as scripted, unskippable "surprise encounters" gating the Floor 1-2 and Floor 2-3 transitions** — the first runtime code for a character that until now existed only in `GDD.md`/`PROGRESS.md` design text (confirmed by a full-repo search before starting: no `BOSSES` entry, no `charKey`, no move data anywhere; the only prior trace was an unused `crimson` portrait already sitting in `GAME_IMAGES` and wired into `bossImageMap`, waiting for a fight that didn't exist yet). **Both gates live in a single chokepoint, `proceedOrPath()` (`js/ui.js`) — not inside `checkCombatEnd()` alongside `launchFinalBoss()`'s own trigger, despite the initial framing suggesting that parallel.** Aldric's trigger skips the entire reward flow outright (no card reward, no relic, no Soul spend — "one final door remains"), but the request explicitly required gold/heal/card-reward/relic/Soul-spend to run undisturbed for Sir Crimson's two earlier floors. Since every one of those screens' decline/skip/purchase paths already funnels through the shared `proceedOrPath()` right before `showPathSelect()`, gating there — rather than short-circuiting `checkCombatEnd()` the way Aldric does — was the only way to satisfy "the reward flow still needs to happen alongside them" without duplicating a check across every screen that can lead there. **Shadow appearance** (`showSirCrimsonShadow()`/`dismissSirCrimsonShadow()`): confirmed the modal-not-toast default and built it as its own fixed-overlay shell (`#sir-crimson-shadow-overlay`, structurally identical to `#path-confirm-overlay`/`#loaded-coat-overlay` but a separate selector, matching how those two are already kept separate from each other) rather than the Mirror panel's inject-into-flow pattern — the Mirror panel is door-screen-specific by construction (torn down on every non-door screen change in `showScreen()`), while this beat can fire from whatever screen the reward chain currently has open, so a fixed overlay sitting on top of anything was the safer fit. **Confrontation and post-fight outro** extend the boss-intro-screen shell exactly as asked, but as two independent new screens (`#crimson-confrontation-screen`, `#crimson-outro-screen`) sharing `#boss-intro-screen`'s container CSS and its `.boss-intro-*` classes rather than repurposing the literal `#boss-intro-screen` node — Sir Crimson has no Challenge offer and his button hands off to `startSirCrimsonFight()`, not the real `startBossFight()`, so keeping them separate means nothing here can leak into or be leaked into by the companion-boss Challenge/FACE-THEM wiring that screen already owns. `startSirCrimsonFight()` is a deliberate stub for batch 5b/5c: it sets `G._sirCrimsonFought = true` and jumps straight to the outro, calling no combat-setup function at all, so no `G.enemy`/`G.inBoss` state is left dangling for this batch's placement-only scope. **Both flags are declared directly in `newGame()`'s literal** (`js/game.js`), following the `_vitalityBuys` precedent rather than the per-combat `_voidCompassOffered`/`_bossRelicOffered` reset pattern — those two re-arm every fight by design, but Sir Crimson's beats need to survive every combat within a run and only reset when the run itself does, exactly like `_vitalityBuys`. Confirmed this is correct for the roguelite structure: **"never again on a later floor or a fresh run" in the request's verification list means the flag must not leak stale across runs** (a fresh `newGame()` discards the whole `G` object, so this was never actually at risk), not that Sir Crimson permanently stops appearing after a player's first-ever run — GDD frames both beats as recurring content every run features, the same way every run refights different corrupted companions, so a fresh run correctly sees the shadow and confrontation again. **Placeholder content, flagged rather than silently shipped as final:** GDD.md specifies exactly one line for this entire arc — the post-fight True Ending hint — and `PROGRESS.md` already carried a note (line 196 at the time) that this exact line is stale, written for the pre-July-25-redesign 4-relic True Ending and never rewritten. The shadow-appearance and confrontation lines have no GDD source text at all (Sir Crimson's backstory is an open TBD, `GDD.md:790`). Rather than block this batch on final copy for a request scoped to "story beats & trigger placement," all three lines (`SIR_CRIMSON_SHADOW_LINE`/`SIR_CRIMSON_CONFRONTATION_LINE`/`SIR_CRIMSON_OUTRO_LINE`, `js/ui.js`) are new placeholder text written to match GDD's established tone, each a one-line swap rather than a structural one, called out explicitly in a code comment and here rather than presented as finished writing. **Verified, driving the real `checkCombatEnd()` → reward-chain → `proceedOrPath()` flow end to end, not by calling the new functions in isolation:** a genuine Floor 1 boss win still awards its full Gold reward and full-heals the player before the shadow modal ever appears; the shadow fires exactly once (a forced second pass through the same floor's gate falls straight through instead of re-showing); dismissing it correctly lands on path select for the new floor; a genuine Floor 2 boss win reaches the confrontation screen with the same reward flow intact, and a stray `proceedOrPath()` call while unfought cannot bypass it; `startSirCrimsonFight()` immediately shows the outro with `_sirCrimsonFought` now true; dismissing the outro lands on path select for Floor 3; a genuine Floor 3 boss win afterward shows neither beat again; and a fresh `newGame()` resets both flags, with the shadow confirmed to fire again in the new run at its same trigger point. All thirteen prior regression suites re-run clean with zero page errors after modifying the shared `proceedOrPath()` chokepoint (relic pickup, Soul-spend decline, Void Compass, and every other screen funneling through it were unaffected). Screenshot-checked at desktop and mobile-landscape (780×380): the two new screens share `#boss-intro-screen`'s pre-existing short-landscape overflow behavior exactly (confirmed scrollable, and in this case overflowing *less* than the real boss-intro screen since neither carries a Challenge panel) — not a new regression, the same known characteristic already noted in that screen's own CSS comment. **Status: Partially verified** — trigger placement, gating, and the reward-flow interaction are Verified; the fight itself (GDD §5's HP 160/move rotation/Echo mimic) and final dialogue copy remain for a later batch. |
| August 17, 2026 (Character relics batch 5c — Gambler: Loaded Coat, the 15th and final Character relic — all 5 heroes now 3/3) | **`loaded_coat`** (`rarity:'character'`, `hero:'gambler'`): "once per combat, swap your active die for any die type for the rest of that fight." Closes out Character relics at 15 of 15 — every hero now has a complete 3/3 batch. **New picker UI, modeled visually on the shop's single-die-offer tile but listing all 6 `DICE_TYPES` entries as parallel choices** rather than one random roll: a full-viewport overlay (`#loaded-coat-overlay`/`#loaded-coat-grid`, shell copied from the existing path-confirm popup so a mid-combat choice can appear without leaving the combat screen) renders one tile per die with its emoji, name, and description read straight from `DICE_TYPES`, so the picker cannot drift out of sync with the die data it displays. The currently-active die's tile is visually dimmed and given no `onclick` handler — confirmed non-clickable, not just styled to look disabled. The `#loaded-coat-btn` ("🧥 SWAP DIE") button sits beside the existing Ley Line Crystal/Gambler's-Edge buttons in the dice panel, shown/hidden by the identical `hasCharacterRelic()` + used-flag pattern already established for Ley Line Crystal, wired into the same `renderSoulDiceControls()` pass. **On pick, `applyLoadedCoat()` snapshots the pre-swap `{activeDie, diceMax}` into `G._loadedCoatSnapshot` before overwriting `G.activeDie`/`G.diceMax`**, then sets `G._loadedCoatUsed = true`. No extra work was needed to make the swap persist across every remaining turn of the fight — `startTurn()` already re-derives `G.diceMax` from `G.activeDie` at the top of every turn (`G.currentDieType = getDie(G.activeDie); G.diceMax = G.currentDieType.max;`), so a swap made on turn 2 is still the active die on turn 5 with zero reapplication logic. **Restoring the real die on combat end required mapping every genuine-end path inside `checkCombatEnd()`, not just adding one call at the top.** `checkCombatEnd()` fires on every Aldric phase-kill, not only his final death — Phase 1→2→3 is one continuous combat, and a Loaded Coat swap must survive those transitions rather than being wiped at each one. `restoreLoadedCoatDie()` is therefore called at exactly three points: the defeat branch (`G.hp <= 0`, always a genuine end); inside the `G.isFinalBoss` branch, but only *after* `checkAldricPhaseTransition()` returns `false` (a phase transition returning `true` skips the restore and returns immediately, so the swap rides through to the next phase); and once in the shared win-branch tail that every other genuine win path (floor 1-2 boss, floor-3 boss before Aldric, normal/elite enemy) funnels through, rather than duplicating the call at each of those branches individually. **Once-per-combat reset follows the Ley Line Crystal precedent exactly, for the same stated reason:** this is a manual button-action charge, not a passive stat hook, so `G._loadedCoatUsed = false` resets at all three fight-start functions — `startCombat()`, `startBossFight()`, and `startAldricFight()` — including Aldric, unlike the passive relic-start hooks (Iron Vambrace, Ashen Crown, Torn Page) that are documented as skipped there. **One addition beyond what was literally asked: each fight-start site also calls `restoreLoadedCoatDie()` itself, not just the used-flag reset.** `G._loadedCoatSnapshot` should already be `null` by the time any fight starts — cleared by `checkCombatEnd()`'s own restore when the *previous* combat ended — so this is a genuine defensive safety net rather than flag hygiene: if some future code path ever skipped `checkCombatEnd()`'s restore, this resolves `G.activeDie` back to the real equipped die before the new fight can read it, rather than leaving a corrupted die silently masked by a used-flag reset that never actually fixed the underlying value. Verified this net actually works, not just assumed: manually left a stray snapshot in place (skipping `checkCombatEnd()`), then called `startCombat()` directly, and confirmed `G.activeDie` came back correctly resolved to the real equipped die with the stray snapshot cleared. **No flee/retreat mechanic exists in this codebase** (confirmed by repo-wide search — no matches for flee/retreat/escape), so the "or fleeing if that exists" branch in the request needed no handling. **Verified:** the picker shows all 6 `DICE_TYPES` entries with correct name/desc/emoji, matched directly against the live `DICE_TYPES` object rather than a hardcoded list, so it cannot silently drift if a die's text changes later; exactly one tile is marked current and is the only one with no `onclick`, every other tile is clickable; picking a die changes `G.activeDie`/`G.diceMax` immediately and the swap holds correctly across multiple subsequent turns (checked 2 turns past the pick, not just the same turn); a genuine win restores the real pre-swap die exactly (a Gambler equipped with a prior shop-purchased d12, swapped mid-fight to d4, correctly returns to d12 after `checkCombatEnd()`'s win branch resolves) and a defeat does the same; the charge is consumed on first use, refuses a second use in the same combat with no picker reopening, and is fresh again at the start of the very next fight, including a fresh Aldric fight; the button is shown only while owned and unused and hides the instant the charge is spent; inert off-hero — button hidden and `openLoadedCoat()` refuses to populate/open at all when granted off-pool to a non-Gambler. **The relic's addition to Gambler's now-3-relic pool surfaced the recurring cross-batch test-staleness pattern a sixth time, in a scratchpad-only test file this time rather than a suite already tracked in this doc's history:** a local verification script from the House Always Wins batch had hardcoded an exact-match assertion expecting Gambler's Character pool to be precisely `devils_ledger,house_always_wins` — broke the moment this relic became Gambler's third. Fixed by updating the expectation to the full 3-relic list; `bossrelic.js`'s already-generalized dynamic-derivation fix (from an earlier batch) needed no changes at all and passed unmodified, confirming that fix's design goal held. Boss screen offers exactly all 3 Gambler relics (`devils_ledger`, `house_always_wins`, `loaded_coat`) to a Gambler across 300 rolls, with the usual cross-batch isolation from the other 4 heroes' pools. Screenshot-checked at desktop (1280×800) and mobile-landscape (780×380) — the picker box is scrollable (`max-height:88vh; overflow-y:auto`) and the Cancel button stays reachable at the short viewport height, confirmed by direct bounding-rect checks rather than the screenshot's visual impression alone, which made the margin look tighter than it measured. All regression suites re-run (relicaudit, charrelics, magerelics, mageboss, thiefrelics, vamprelics, vamp4b, ledger, house, ledgerpool, bossrelic, plus the new loadedcoat suite) pass with zero page errors, confirmed stable across 3 consecutive runs of the new suite. `newGame()` unchanged — Loaded Coat's fields are combat-scoped, like every other Character relic's. **This closes the "Character slot has no content" half of the Boss Reward Flow discrepancy for every hero** (see `DESIGN_DISCREPANCIES.md`); the known side effect first flagged for Barbarian — Uncommon relics no longer appearing in boss rewards once a hero's 3 slots are all fillable — now applies to all 5 heroes rather than just one. **Status: Verified.** |
| August 17, 2026 (Character relics batch 5b — Gambler: The House Always Wins) | **`house_always_wins`** (`rarity:'character'`, `hero:'gambler'`): "roll max 2 turns in a row → next card costs 0," GDD's only Character relic to explicitly call for a persistent on-screen tracker — new UI ground this session hadn't needed before. **The streak check reuses `g._naturalDieValue` exactly as directed, and this was the right call: it is stamped once per genuine `rollDice()` call (initial roll or a reroll, both) and is never touched by any forced-set action** — Ley Line Crystal, Gambler's Edge, and Second Die all mutate `g.currentDie` directly and leave the stamp alone — so checking `g._naturalDieValue === g.diceMax` at the turn boundary is sufficient by itself to separate a genuine max roll from a forced one; no additional "was this forced" check was needed on top of what the request already specified. Deliberately did **not** reuse `naturalMaxSuppressed()` despite the similar name and neighborhood — that function is the Gambler's Edge *Soul upgrade's* own downside (suppressing affinity-max bonuses permanently once that upgrade is taken), an unrelated mechanic with different gating (`hasSoulUpgrade`, not this relic) that would have produced wrong results if borrowed here. The check itself sits in `startTurn()`, positioned identically to Midnight Hunger's own turn-transition check from the Vampire batch: read *before* anything resets for the upcoming turn, so it evaluates the turn that just ended, guarded on `G.turn > 0` so the very first turn of a fresh combat — where `_naturalDieValue` would still be carrying a stale value from the previous fight's last roll — is correctly skipped. On reaching 2, the counter resets to 0 immediately (mirroring Gambler's Fallacy's own reset-on-fire convention already in this codebase) rather than continuing to climb, so a third consecutive max roll needs two fresh hits to trigger again, not one. The free-card flag is consumed in `getCardEnergyCost()` as a hard override to 0 — matching Soulbound Gauntlet's "first card free" pattern more precisely than Mana Surge's stacking `-1`, since "next card costs 0" is an override, not a discount that should compose with other reductions. **The tracker** is a pair of die-emoji pips reusing the `.core-icon` dim/lit visual language already established for the Cores HUD display, placed beside the existing Soul-die buttons in the dice panel; it renders directly off `G._maxRollStreak` on every `renderSoulDiceControls()` pass (already called from the existing `renderAll()` chain after every turn transition) rather than keeping any separate copy of the count, so the pips can never show a number the game state disagrees with. Unlike the Ley Line Crystal / Second Die / Gambler's Edge buttons beside it, the tracker stays visible whenever the relic is held rather than hiding once "used" — a streak display has nothing to hide, it just shows the current count, including the reset to 0/2 unlit the instant a miss or a fresh trigger clears it. **Verified:** a genuine max roll advances the streak by exactly 1, and a *reroll* landing on max advances it too (confirmed by forcing a non-max initial roll then a maxed "reroll" via a second `rollDice()` call, since both go through the identical stamping code path); forcing `G.currentDie` to max directly — simulating what Gambler's Edge/Ley Line Crystal do — leaves `_naturalDieValue` at its genuine non-max value and correctly does *not* advance the streak; any single non-max turn resets the streak to 0 immediately, never gradually; hitting 2 sets the free-card flag exactly once, consumed by the very next `getCardEnergyCost()` call (a real `playCard()` spending 0 Energy) with the card immediately after that costing its normal printed amount, not a standing discount; the tracker shows 0/2 unlit before any max roll, exactly 1/2 lit after one consecutive hit, and both pips unlit again the instant a miss resets the streak — confirmed against actual rendered DOM state (`.lit` class), not just the underlying counter; inert on a non-Gambler holder granted the relic off-pool, both for the streak (stays 0 no matter how many max rolls land) and the tracker (stays hidden). **The recurring cross-batch test-staleness pattern surfaced a fifth time, immediately, from last session's own Devil's Ledger suites** — `ledgerpool.js` and three of `bossrelic.js`'s assertions expected Gambler to have *exactly* `devils_ledger` and nothing else, which broke the moment this session's relic became Gambler's second. Fixed by generalizing rather than re-patching to another specific expectation: `bossrelic.js`'s partial-batch check now derives Gambler's expected relic set and count directly from `Object.entries(RELICS).filter(hero==='gambler')` inside the page context instead of a hardcoded list, so it will keep working through Gambler's third relic with no further edits — the fix this whole recurring pattern was actually asking for, rather than a sixth manual patch when the next one lands. All twenty-six suites, the 33-assertion layout matrix, and the `newGame()` snapshot (diff-clean against last session's already-reported `goldSpentThisRun` addition, with no new state leaking into `newGame()`'s own object — this relic's fields are combat-scoped, like every other Character relic's) pass, confirmed stable across repeated runs. **Status: Verified.** |
| August 17, 2026 (new shared helper + Character relics batch 5a — Gambler: Devil's Ledger) | **`spendGold(g, amount)` now centralizes every Gold spend, mirroring `loseHP()`'s role for HP loss and for the identical reason: this session's first Gambler relic needed a running total of Gold spent, and Gold was being decremented independently at 6 call sites** — shop card/item buy, shop relic buy, shop die buy, card removal, card upgrade (`js/ui.js`), and the Mirror path-switch cost (`js/game.js`) — **with no shared function, so adding the tracking at each site individually risked a future 7th spend site quietly skipping it.** All six now route through `spendGold()`, which does the decrement and increments `G.goldSpentThisRun` in one place; every site's own affordability check and surrounding UI-update calls (`updateHUD()`, the `shop-gold-display` text, `acquireRelic()`, etc.) were left completely untouched — only the literal `G.gold -= X` line was swapped for `spendGold(G, X)` at each of the six. **One pre-existing asymmetry was deliberately preserved, not "fixed" in passing:** the card-upgrade site spends a raw `80` while every other shop site wraps its cost in `shopCost()` (which applies King's Debt's 25% markup) — upgrades have never been subject to that markup, and this refactor's job was routing the arithmetic through one function, not correcting unrelated pricing inconsistencies it happened to notice. **[Superseded Aug 18, 2026: that asymmetry was closed — card upgrades now route through `shopCost(80)` like every other shop spend, and the shop's upgrade service was given the `#shop-upgrade-btn` it had never had. See the Aug 18 entries and `DESIGN_DISCREPANCIES.md`.]** **`devils_ledger`** (`rarity:'character'`, `hero:'gambler'`): "every 20 Gold spent this run adds +1 damage, up to +8," recalculated live as `Math.min(8, Math.floor(G.goldSpentThisRun / 20))` inside `calculatePlayerAttackDamage()` rather than tracked via its own incrementing stack — so the bonus can never drift out of sync with the counter it reads. Placed beside the existing Rage bonus, both flat additive terms evaluated *before* Weak's 25% reduction and Vulnerable's 50% increase further down the function — deliberately the same position Rage already occupies, so Devil's Ledger composes with those multipliers exactly the way Rage already does, rather than inventing a new composition rule. `G.goldSpentThisRun` is initialized to 0 in `newGame()` only — no fight-start function touches it, so it survives every combat within a run by simply never being reset outside a fresh run, and it is deliberately excluded from `js/meta.js`'s persistence layer per the GDD's explicit "this run" wording (Souls-style in-run resource, not a cross-run unlock). **A verification item in the request turned out to describe an impossible scenario, and this was confirmed directly rather than assumed benign:** "composes correctly alongside Assassin's Edge if both are somehow held" cannot occur under the hero-lock design at all — `hasCharacterRelic()` requires `relic.hero === g.charKey`, Devil's Ledger is locked to `'gambler'` and Assassin's Edge to `'thief'`, and a single `G` state has exactly one `charKey`, so no combination of `G.relics` can ever make both checks pass simultaneously. Verified by checking both relics' active state as each of three different `charKey` values (Gambler, Thief, a third hero) and confirming at most one is ever active. The underlying arithmetic composition — a flat term added early, then multiplied by a later modifier — was still verified using Gilded Quill (a general, non-hero-locked relic that genuinely can coexist with Devil's Ledger on the same Gambler): holding both, a non-qualifying card shows only Devil's Ledger's flat addition, while the 10th card shows that same addition then doubled by Gilded Quill's own multiplier (9 → 18), proving the composition mechanism is sound even though the specific Ledger+Edge pairing named in the request cannot arise. **Verified:** damage bonus is exactly +0 through 19 Gold spent, +1 at exactly 20, +2 at 40, +7 at 159, +8 at exactly 160, and still capped at +8 at 200 and 1000 spent — never exceeds the cap no matter how much more is spent; all six spend sites drive a real click/call through their actual rendered UI (shop card tile, shop relic tile, shop die tile, the remove-card modal, the upgrade-card modal, and a direct `useMirror()` call) with the Gold drop and the counter increment matching exactly, confirming no site double-charges or under-tracks; a King's Debt-inflated cost is tracked at its actual post-markup value (100, not a nominal 80), so the relic correctly rewards what was genuinely spent; the counter survives `startCombat()`, `startBossFight()`, and `startAldricFight()` unchanged, then resets to exactly 0 on a fresh `newGame()`; inert (flat +0, base damage only) when granted off-pool to a Barbarian even with an artificial 1000-Gold-spent counter. **A fourth recurrence of this session's known test-staleness pattern surfaced immediately, and this time terminally.** Four suites (`vamprelics`, `thiefrelics`, `charrelics`, `bossrelic`) each used Gambler as their "hero with zero Character relics" reference hero; Gambler getting its first built relic this session broke all four at once. Because all five real heroes now have at least one built Character relic, there is no longer any real hero left to serve as that reference — patching to "whichever hero is next in line" would only defer the same failure to the next batch. Fixed permanently by switching all four suites to a synthetic `charKey: 'nobody'`, which structurally cannot match any relic's `hero` field regardless of how many more batches ship. Also added a real (non-synthetic) check that Gambler's boss-reward Character slot currently offers *exactly* `devils_ledger` and nothing else, reaching precisely 10 Common + 9 Rare + 1 Character = 20 distinct relics — the correct partial-batch number, not the full-batch 22 the other four heroes now reach. All twenty-four suites, the 33-assertion layout matrix, and the `newGame()` snapshot (whose only diff is the deliberately added `goldSpentThisRun` key) pass, confirmed stable across repeated runs of every newly-touched suite. **Status: Verified.** |
| August 17, 2026 (Character relics batch 4b — Vampire, batch complete: Crimson Lens & Blood Pact) | **`crimson_lens` and `blood_pact` complete Vampire's Character batch at 3 of 3.** Crimson Lens: "on a non-extreme roll, Vampire Attacks heal 50% of the damage dealt (post-Block)." Blood Pact: "any card-cost HP spend heals back 50%." **A concrete data conflict was found and raised before writing any code, not discovered afterward.** Drain Life ("Heal half damage dealt. Extreme: heal full") and Soul Rend ("Heal equal to damage dealt", unconditional on roll) already implement the exact mechanic Crimson Lens generalizes, built into their own effect() functions. A blanket, no-exceptions hook would have stacked on both — concretely, Drain Life's non-extreme heal would silently become 50%+50%=100% (quietly matching what it already advertises as the *extreme* case), and Soul Rend's would become 100%+50%=150%, exceeding anything either card's printed text describes. Owner chose to exclude the two cards specifically (and their `+` upgrades, `drainlife+`/`soulrend+` — Drain Life+ in particular always heals full damage regardless of roll, making the conflict worse there) rather than ship the blanket version. **The exclusion required new plumbing, since `dealDamage()` has no way to know which card is calling it.** No cardKey parameter exists on `dealDamage(g, target, amount, source, bypassBlock)`, and threading one through 30+ card-effect call sites in `js/data.js` to support one relic's 2-key exclusion list would have been exactly the per-card churn these relic hooks exist to avoid. Instead, `playCard()` now stamps `G._currentCardKey = cardKey` once, at the top of the function, before `card.effect()` runs — a single shared-chokepoint addition, not a per-card one. Crimson Lens reads it back (normalized with the same trailing-`+` strip `CARD_PLAY_CONDITIONS` already uses in `js/data.js`) to skip the two excluded keys; safe for multi-hit cards and Spell Echo repeats since both re-invoke the *same* card's effect without changing the key mid-resolution, and safe against staleness since JS's single-threaded execution means nothing can read it between this assignment and the effect's own `dealDamage()` calls. **No card-type check was needed at all** — audited every Vampire Skill/Power and confirmed none of them ever call `dealDamage(g,'enemy',...)`, so for a Vampire that call happening at all already means an Attack just resolved; checking `card.type==='Attack'` explicitly would have been redundant. Reads `g.currentDie` directly for the roll, since `dealDamage()` isn't passed one either — the same value every Vampire card's own affinity check already uses, so the two can't disagree. Heals off `pen`, the post-Block value already computed in `dealDamage()`'s enemy branch, not the pre-Block `amount` `calculatePlayerAttackDamage()` works with — a fully blocked hit heals nothing. Blood Pact sits at "the opposite branch of Berserker's Scar's existing source check" exactly as directed, inside `loseHP()`: `source !== 'enemy'` catches every direct card-cost call (Dark Blood, Dark Embrace, Blood Bank, Dark Rite, Dark Ascension — none of which pass a `source` at all) plus Cursed Reroll's self-inflicted `dealDamage(g,'player',3,'self')`, which routes to `loseHP()` with `source` dropped entirely by `dealDamage()`'s own fallback branch. Reads the local `amount` variable *after* the `floorAt` clamp reassigns it in place, so a cost that would have dropped the player below its floor and was silently reduced refunds 50% of what was actually paid, never the card's printed number. **Disjointness was verified structurally, not assumed:** grepped every one of Vampire's ~30 cards for one that calls both `dealDamage(g,'enemy',...)` and a non-enemy HP-cost path in the same effect — zero matches, confirming Attacks-that-deal-damage and Skills-that-spend-HP are genuinely disjoint sets in this kit before relying on that fact. **One notable side-effect of both new relics sharing `checkAffinity()`/`loseHP()` with the prior batch's hooks:** if a Vampire holds both Crimson Lens and Midnight Hunger, Crimson Lens's own `checkAffinity(g, g.currentDie, 'extreme')` call also correctly feeds Midnight Hunger's turn-tracking — an extreme hit discovered via Crimson Lens's check counts exactly the same as one discovered via the die's own highlight render, since both paths converge on the identical shared function. Not asked about because it composes correctly by construction (the flag is a boolean, idempotent to a duplicate `true`), not by special-casing. **Verified:** fires on rolls 2-5 on a d6 (exactly floor(6/2)=3 each), and roll 1/6 correctly resolve to Blood Drain's own extreme heal (8) instead, confirming Crimson Lens's `!extreme` gate and not a card-specific fluke; rescales correctly on a Hunter's Die (roll 4 of 8 heals 3, rolls 1 and 8 don't) and a Cursed Die (roll 3 of 4 — the only value reachable at all once `min3` blocks 1-2 — heals 3, roll 4 doesn't); heals off post-Block damage specifically (no Block → 3, partial Block-4 → 1, full/over-Block → 0, not a flat 3 regardless of Block); Blood Pact's clamped-HP case refunds 50% of the actually-spent 2, not the printed 4; Drain Life/Soul Rend and their `+` upgrades produce byte-identical heals with and without Crimson Lens held; all 16 disjoint Vampire cards (9 Attacks, 7 HP-cost Skills) checked with both relics held simultaneously never cross-fire the other relic's toast; both inert when granted off-pool to a Barbarian. Vampire's Character pool now offers all 3 relics to a Vampire across 300 boss-screen rolls with the usual cross-batch isolation. All nineteen suites and the 33-assertion layout matrix pass, confirmed stable across 5 repeated runs of the two new suites given this session's prior history of RNG-dependent test flakiness; `newGame()` unchanged. **Status: Verified.** |
| August 16, 2026 (Character relics batch 4 — Vampire, 1 of 15: Midnight Hunger) | **`midnight_hunger`, `rarity:'character'`, `hero:'vampire'`.** "If you didn't hit affinity this turn, your next roll gets +2." Same hero-lock and pool machinery as the three prior batches. **The hook point (inside `checkAffinity()` itself, per the request) turned out to make "counts an empty turn" work for free, without any extra code.** `checkAffinityHighlight()` calls `checkAffinity(g, roll, 'extreme')` on every roll for a Vampire (since Vampire's own dice affinity IS `'extreme'`) as part of the die's UI highlight — this fires from the turn-start auto-roll whether or not the player plays a single card. Flagging `g._hitExtremeThisTurn = true` inside `checkAffinity()`'s own `'extreme'` branch (gated on `hasCharacterRelic('midnight_hunger', g)`) means a card-free turn is tracked correctly with zero special-casing, and the flag also inherits every existing guard in that function for free — `aldricAffinityDisabled` and `naturalMaxSuppressed()` — so a Gambler's-Edge-forced max roll does not count as "hit extreme," consistent with how forced rolls are already excluded from natural-affinity bonuses everywhere else in the game. The queue-check lives in `startTurn()`, read *before* that function resets the flag for the upcoming turn, so it reflects whichever turn just ended; the bonus itself is consumed inside `rollDice()`, applied via the identical formula Second Die uses on its own bonus (`Math.min(roll + bonus, diceMax)`) but baked in earlier — before `g.currentDie`/`g._naturalDieValue` are stamped — so it becomes part of what the turn's roll IS for every downstream reader, not a later correction. Because Second Die operates on `G.currentDie` afterward as an entirely separate mechanism, the two compose additively with no special-casing: verified on a d20 that a base roll of 11 becomes 13 via Midnight Hunger and then 15 via Second Die's own +2, both bonuses cleanly present. **A sentinel value avoids a turn-1 false positive:** `G._hitExtremeThisTurn` is seeded `true` (not the per-turn `false`) at all three fight-start functions, specifically so the very first `startTurn()` call of a fight — which has no preceding turn to have missed — does not wrongly queue a bonus for the opening roll; verified directly. Unlike Ley Line Crystal (a manual once-per-combat button whose reset needed explicit handling at Aldric because it's fight-start-scoped), Midnight Hunger hooks into `checkAffinity()` and `startTurn()`, both universal functions Aldric already calls unconditionally, so it needed no special exclusion there at all — flagged as the reason those two batches' relics differ in this respect, not an oversight. **Verified:** turn 1 never queues a bonus; a turn that genuinely misses queues +2 for the very next roll and that roll alone (proven directly via mocked `rollDice()` calls: 4→6 on a d6, 11→13 on a d20); the bonus clears immediately on consumption and does not resurface on a third turn even across a forced three-turn all-miss cycle; hitting extreme on the last affinity check of a turn — after two prior misses in the same turn — still correctly cancels the queue, since the flag can only ever be set true and a later miss-check cannot un-set an earlier hit; inert on a non-Vampire hero holding it off-pool, both for the flag-set and the bonus-queue path; boss screen offers exactly `midnight_hunger` to a Vampire across 300 rolls with the usual cross-batch isolation. **A real, reproducible test-suite flakiness was found and fixed during verification, not shipped silently.** Several tests called `startCombat()` and then immediately inspected `G._hitExtremeThisTurn`/`G._nextRollBonus` — but `startCombat()`'s own turn-1 auto-roll uses real, unmocked `Math.random()`, which lands on an extreme value (1 or 6 on a d6) roughly 1 time in 3, and the flag design *deliberately* never resets a `true` back to `false` on a later miss (required for the "last-card hit still cancels the queue" behavior) — so a test could observe a stray `true` left over from real RNG with no way to distinguish it from a genuine bug. Reproduced by running the suite 8+ times in a row rather than trusting one green run. Fixed with a shared in-page `window.__freshCombat()` helper that mocks `Math.random` to force a deterministic non-extreme initial roll for every test that starts a fresh combat and then depends on a clean flag state, confirmed stable across 12 consecutive runs afterward. All seventeen suites and the 33-assertion layout matrix pass; `newGame()` unchanged. **Status: Verified.** Vampire's other two Character relics were not part of this request and remain unbuilt. |
| August 16, 2026 (Character relics batch 3 — Thief, 3 of 15) | **`assassins_edge`, `shadow_wrap`, `venomfang`, all `rarity:'character'`, `hero:'thief'`.** Same hero-lock and pool machinery as the two prior batches. **Assassin's Edge was checked against an existing near-identical mechanic before implementing, and the check ruled out copying it.** Gilded Quill ("every 10th card played this combat deals double damage") already lives in `calculatePlayerAttackDamage()`, but it uses a one-shot flag (`_gildedQuillActive`) that `dealDamage()` consumes with `{consume:true}` on its very first call — so a multi-hit Attack landing on the 10th card only doubles the *first* hit, not all of them. The request's stated default ("doubling every dealDamage() call that lands while that count holds") explicitly wants different behaviour, so Assassin's Edge reads `(g._cardsPlayedThisTurn||0) % 4 === 0` fresh on every call instead of consuming a flag. This is only safe because of two things confirmed by reading the code rather than assumed: `G._cardsPlayedThisTurn` is incremented *before* `card.effect()` runs and never changes again until the next card is played (documented in an Aug 15, 2026 fix comment already in the file), and `dealDamage(g,'enemy',...)` is called *only* from card `effect()` functions — Burn and Poison ticks subtract `g.enemy.hp` directly and never call `dealDamage()` — so the stateless check cannot leak into end-of-turn DoT resolution. **Shadow Wrap surfaced a real fork the request's own framing blurred.** It grouped Iron Vambrace, Ashen Crown and Torn Page together as "the same passive-hook pattern," but they're actually two different patterns: Iron Vambrace/Hollow Throne stage their Block through `stageCombatStartBlock()`'s `G._pendingCombatBlock`, applied in `startTurn()` *after* the turn-1 Block reset — a mechanism that exists specifically because a plain `G.block += N` at fight start used to be silently wiped by that same reset (an Aug 15, 2026 bug fix). Torn Page/Rusted Chain use a plain inline grant, which only works for them because `extraDraw` and enemy statuses aren't touched by that reset. Since Shadow Wrap is a Block grant — the Iron Vambrace category, not the Torn Page one — a literal inline `G.block += 5` would have reproduced the exact bug that was already fixed once. Owner confirmed routing it through `stageCombatStartBlock()` instead; it now sits beside Iron Vambrace/Hollow Throne there and inherits the Aldric skip automatically, since `startAldricFight()` already calls that function with `includeRelics=false`. **Venomfang** adds a flat +1 to the Poison tick, implemented as a separate additive term alongside Poison Master's existing per-stack multiplier rather than folded into it, so the two sum (verified: Poison Master alone 3→6, Venomfang alone 3→4, both together 3→7 — not some other composition). **Verified:** Assassin's Edge deals base damage on cards 1-3/5-7 and exactly double on cards 4/8, doubles *every* hit of a multi-hit Attack (Blizzard's 3×5 becomes 30, not 20), and correctly does nothing extra on a damage-free 4th-card Skill while that Skill's own effect (Block) still lands; Shadow Wrap grants 5 Block on normal, elite, and Floors 1-3 boss fights and exactly 0 before Aldric; Venomfang's sum-not-compound behavior holds at both Poison Master ranks; all three are inert (button/effect no-op) when granted off-pool to a non-Thief hero. Boss screen offers exactly Thief's 3 relics to a Thief across 300 rolls with zero cross-contamination from the Barbarian or Mage batches. **One test-authoring bug caught by the suite itself, not the product:** the first verification run showed Assassin's Edge firing on cards 3 and 7 instead of 4 and 8 — the test was seeding `G._cardsPlayedThisTurn` to the target card number directly, forgetting that `playCard()` itself adds 1 before the effect runs; fixed by seeding `n-1`. A second test bug (an IIFE reassigning `G.charKey` that was then immediately clobbered by a helper function's own hardcoded `G.charKey = 'thief'`) made the "inert on wrong hero" check for Venomfang report a false failure; the helper was parameterized instead. **The usual cross-batch test staleness recurred a third time:** both prior batches' suites had used whichever hero was "next in line" as their empty-pool reference (Mage's suite used Thief, this session's own baseline used Thief too before this batch existed) — repointed to Gambler, the one hero still genuinely unbuilt, rather than loosened. All sixteen suites and the 33-assertion layout matrix pass; `newGame()` unchanged. **Status: Verified.** |
| August 16, 2026 (Character relics batch 2 — Mage, 3 of 15) | **`stone_grimoire`, `frost_seal`, `ley_line_crystal`, all `rarity:'character'`, `hero:'mage'`.** Same hero-lock and pool machinery as the Barbarian batch — `offerableCharacterRelics()` filters on `relic.hero`, `hasCharacterRelic()` re-checks it at every effect site. **Two proposed defaults were validated against the data before building, not assumed.** "Spell" for Stone Grimoire/Frost Seal was defined as `card.affinityBonus === 'high'`; checked against Mage's entire reward pool first and confirmed there is no third category — every card is either `dice:false` with no roll-scaling (Mana Surge, Arcane Boost, Void Channel, Blizzard, Arcane Momentum, Soul Steal, Iron Wall, Cursed Reroll, Cold Mastery, Burning Soul) or `dice:true` with `affinityBonus:'high'`, so the gate cleanly excludes Strike/Defend and every Power with no card-list to maintain. Ley Line Crystal's reset lifecycle raised a genuine fork the request hadn't addressed: passive relic-start hooks (Iron Vambrace, Ashen Crown, Torn Page) are documented as **not** running for the Aldric fight at all, while the Soul-upgrade button pattern it was explicitly modeled on (Gambler's Edge / Second Die) **does** reset for Aldric via `applySoulCombatStart()`, which runs unconditionally there. Owner confirmed the Soul-upgrade precedent — Ley Line Crystal is structurally a manual button action, not a passive stat, so `G._leyLineCrystalUsed` resets at all three fight-start functions (`startCombat`, `startBossFight`, `startAldricFight`), following the relic-flag convention (`_ashenCrownFired`) directly rather than folding into the Soul-upgrade-named helper. **A wording error was caught before implementation, not after:** the initial relic entry described Ley Line Crystal as "set your die to its highest face", which is only true on a d4 or d6 — the actual spec targets exactly 6, clamped only when `diceMax < 6`. Corrected before any test ran; `useLeyLineCrystal()` uses `Math.min(6, G.diceMax)` and was verified to set exactly 6 on a Hunter's Die (d8) and a Legendary Die (d20), not their own max face, and to clamp to 4 on a Cursed Die. It also respects the existing one-forced-value-per-turn rule (`G._dieSetThisTurn`), the same guard Gambler's Edge uses, since nothing in the request's default overrode that stated Core Combat Rule. New button `#ley-line-crystal-btn` ("🔮 SET 6") sits beside the existing Soul dice buttons, shown/hidden by the same `hasCharacterRelic()` + used-flag pattern, wired through the existing `renderSoulDiceControls()` render path so no new render call was needed. **Verified:** Stone Grimoire grants +4 Block on both Mage Attacks and Skills with `affinityBonus:'high'`, regardless of roll, and +0 on Strike, Defend, and dice:false utility Skills — isolated by diffing Block gained with and without the relic on the same card, since several of these cards (Arcane Shield, Defend) already grant their own Block and an early version of this test mistook that for the relic firing; Frost Seal applies exactly 1 Chill on rolls 1-3, nothing on the 4-5 dead zone, and never stacks to 2 when a high roll already triggers Frost Bolt's own Chill; Ley Line Crystal's button appears only when owned and unused, disappears after use, refuses a second use the same combat, refuses when the die was already forced this turn, and resets on every new fight including Aldric; all three are inert (button hidden, function no-ops) when granted off-pool to a non-Mage hero. The boss relic screen offers exactly Mage's 3 relics to a Mage and exactly Barbarian's 3 to a Barbarian across 300 rolls each, with zero cross-contamination. **One test-suite update, not a product change:** the Barbarian batch's regression suite had used Mage specifically as "the hero with no Character relics" — now false. Those assertions were repointed to Thief (still unbuilt) and to disjointness checks between the two live batches, rather than loosened. All fifteen suites and the 33-assertion layout matrix pass; `newGame()` unchanged. **Status: Verified.** |
| August 16, 2026 (Character relics batch 1 — Barbarian, 3 of 15) | **First Character relics exist: `warlords_bandage`, `battle_drum`, `berserkers_scar`, all `rarity:'character'`, `hero:'barbarian'`.** Hero-locking is enforced in two independent places by design: `offerableCharacterRelics()` filters the offer pool on `relic.hero === g.charKey`, and `hasCharacterRelic()` re-checks the same field at every effect site, so a relic granted off-pool (debug, a hand-edited save) stays **inert** on the wrong hero instead of silently working — verified for all three. Character relics are excluded from `offerableRelics()` entirely, so the shop and the Void Compass screen can never offer them (GDD §9: boss reward only); the boss screen concatenates the two pools, which lets `pickBossRelicOffers()` treat `character` as just another rarity bucket. `RELIC_RARITY_MIN_FLOOR.character = 2` gates the slot exactly like Rare — it limits when the slot can fill, never whether the screen appears. **Owner override on Berserker's Scar:** the proposed "all HP loss" default was rejected in favour of **enemy-caused damage only**, so paying HP as a card cost grants nothing. Implemented by adding an optional `source` argument to `loseHP()`; only `resolveEnemyAttack()` and the Vampire Challenge drain pass `'enemy'`, so all ~25 card call sites keep the default and were untouched. Berserker's Oath, three lines above in the same function, deliberately still fires on ANY HP loss — asserted, since the split could easily have caught it. Worth recording: **player-side Burn/Poison never tick** (both tick loops read `G.statuses.enemy` only), so DoT-on-player is not currently a live damage source at all; the `loseHP` comment notes that such a tick should pass `'enemy'` when it lands. **Battle Drum needed care in two places.** It reads LAST turn's roll, so the value is captured before `rollDice(G, true)` overwrites `G.currentDie`; and the Titan's Die `max_draw` branch **returns early**, so the draw is applied in both branches — otherwise the Drum would silently vanish on exactly the turns the player rolled max. The extra card is flagged `{ turnStart: true }` so the Mage Challenge's extra-draw denial treats it as part of the opening hand rather than a card effect. **A bug was caught during verification, not review:** Warlord's Bandage was first placed above `const roll` in `playCard()` and threw a temporal-dead-zone `ReferenceError` on every card play; it now sits after the card resolves, reading the same `roll`/`checkAffinity()` the effect used, and fires once per play rather than once per Spell Echo repeat. **A behavioural consequence worth flagging:** now that Barbarian has all three slots fillable, `pickBossRelicOffers()` pass 1 satisfies the offer outright and the backfill never runs — so **Uncommon relics no longer appear in Barbarian boss rewards** (22 reachable: 10 Common + 9 Rare + 3 Character). That is GDD's literal "1 Common, 1 Rare, 1 Character-specific", and Uncommons remain available from shops and Void Compass, but it is a real change to what a Barbarian sees at Floor 3+ and will repeat per hero as each batch lands. Heroes without Character relics still backfill and reach all 28. **Verified:** Warlord's Bandage heals only on odd-roll Attacks (not Skills, not Powers, not even rolls, any odd value); Battle Drum keyed to last turn's roll and not this turn's fresh one, silent on turn 1; Berserker's Scar stacks 1 Rage per damage instance (3 hits → 3 Rage), nothing on a fully blocked hit, nothing on self-inflicted costs; all three inert on the wrong hero; the Character slot offers exactly the 3 Barbarian relics to a Barbarian and nothing to a Mage across 300 rolls, none before the Floor 3 gate, and none ever in shop or elite pools across 200 rolls each. All thirteen suites pass. **Status: Verified.** |
| August 16, 2026 (new screen — boss relic choice) | **`showBossRelicReward()` fills the slot `proceedAfterCardReward()` had reserved with a NOTE comment: pick 1 of 3 relics after a Floors 1-3 boss, between the card reward and the Soul screen.** Modeled structurally on `showEliteRelicReward()` — same reward screen, same tiles, same bail-out — and drawing candidates from `offerableRelics()`, so ownership filtering, the unimplemented-relic exclusion and floor gating are inherited rather than duplicated. The Floors 1-3 guard is now a shared `isBossRewardWindow()` used by both this screen and the Soul screen, so the two cannot drift onto different floors. Fires once per boss via `G._bossRelicOffered`, reset in `startBossFight()` exactly as `_voidCompassOffered` is reset in `startCombat()`. **GDD's documented composition ("1 Common, 1 Rare, 1 Character-specific") cannot be built as written** — Character relics have zero entries in `RELICS`, and a guaranteed Rare after every floor boss contradicts GDD §9's own Rare = Floor 3+ rule. Resolved per the owner's two defaults: the composition is expressed as an ordered rarity list filled from whatever is eligible, and any slot with no content is **backfilled so the player always sees three real choices** rather than a short screen. Because the composition is a list rather than hardcoded slots, the third slot begins filling itself the moment Character relics are added with `rarity:'character'` and a `RELIC_RARITY_MIN_FLOOR` entry — no change to this screen. Character's "Floor 3+" gates that slot's eligibility, not the screen's appearance, matching how Rare already behaves. **A real bug was found and fixed during verification, not by review:** the first backfill rule ("take from whichever tier has the most unowned options left") **starved Uncommon completely from Floor 3 onward** — pass 1 removes one Common and one Rare, leaving Common and Uncommon tied on remaining count with Common winning every tie-break, so Uncommon became unreachable from boss rewards and the reachable pool was 19 of 28 relics. Backfill is now tier-blind, drawing from the whole remaining eligible pool; Floor 3+ now reaches all 28. A regression assertion guards it. **One decision not among the two questions:** the screen hides the skip button. GDD documents a skip payout for card rewards only, this is a guaranteed boss reward, and the shared button is hardwired to `skipReward()`, which pays +10 Gold and re-enters `proceedAfterCardReward()` — wrong payout and wrong routing here. `showSoulSpend()`, the very next screen in the same flow, hides it for the same reason; `showReward()` restores it for cards, which is asserted. Flagged for reversal if a skip is wanted — it is one line. **Verified by driving the real flow, 300 rolls per floor:** always exactly 3 cards, never duplicated within an offer, never already-owned, never floor-ineligible or unimplemented; Rare absent entering Floor 2 and present from Floor 3; fires on Floors 1-3 and never for Aldric, a non-boss fight, or outside the window; picking grants via `acquireRelic()` and lands on the Soul screen without re-offering; owning every relic advances to the Soul screen instead of stalling; and the screen renders 3 tiles with no horizontal overflow on desktop and mobile landscape. (One initial failure was a stale-DOM artifact in the test — the Aldric check was reading the previous screen's cards because the relic screen correctly never rendered; the assertion now clears the pool and checks the flag.) All twelve suites and the 33-assertion layout matrix pass; `newGame()` unchanged. **Status: Verified.** |
| August 16, 2026 (bug fix — relic offer pools: rarity/floor gating + unimplemented relics) | **Both relic offer pools now share one filter, `offerableRelics()` in `js/ui.js`.** Prompted by a relic audit that asked whether Bone Key was excluded anywhere; it was not, and neither was anything else. `renderShopRelics()` and `showEliteRelicReward()` each inlined their own `Object.entries(RELICS).filter(([k]) => !G.relics.includes(k))`, whose only rule was "not already owned". **Two bugs fell out of that.** (1) **No rarity or floor gating existed at all** — a Floor 1 shop could sell King's Debt or Hollow Throne, against GDD §9's Common/any, Uncommon/Floor 2+, Rare/Floor 3+ design. The shop even carried the comment *"Floor 1-2: common pool. Floor 3+: 50% chance uncommon (not yet added — always common for now)"* sitting directly above code that offered all 30 relics — the comment described an intent the code never had. (2) **Relics with no implementation were fully purchasable.** A full hook audit of all 30 keys against the entire codebase found exactly two dead: **`bone_key`** (no room hook) and **`shattered_mirror`** (no enemy card-copy mechanic). Buying either printed "acquired!" via `acquireRelic()`'s generic branch and then did nothing for the rest of the run. Both are now excluded via a named `UNIMPLEMENTED_RELICS` list carrying the reason per key and an explicit instruction to delete the entry when the behaviour lands, so a finished relic cannot stay unobtainable. **The audit corrected two stale doc claims too:** `void_compass` was listed as deferred but is implemented and drives the elite relic screen; and the audit surfaced that `ivory_die` and `loaded_gauntlet` have no `hasRelic()` call yet are live — they are pickup side-effects inside `acquireRelic()`, which a naive grep would score as dead. **Verified by driving the real `renderShopRelics()` and `showEliteRelicReward()` 300 times per floor each**, not by reading the filter: Floor 1 yielded exactly the 10 Commons, Floor 2 exactly 19 (10 Common + 9 Uncommon), Floors 3-4 exactly 28 (30 minus the two unimplemented) — each run exhausting the full eligible set, so the bounds are exact rather than merely unobserved. Neither pool ever produced `bone_key` or `shattered_mirror` at any floor, and the two pools returned **identical** sets at every floor, which is the drift this shared helper exists to prevent. Degenerate cases checked: owning every relic yields an empty pool and the elite screen bails without throwing. Confirmed repo-wide that **nothing else iterates `RELICS`** — the only other accesses are single-key lookups for display (`acquireRelic`, `showRelicTooltip`, `renderRelics`), which correctly need no gating. All eleven suites and the 33-assertion layout matrix still pass. **Status: Verified.** |
| August 16, 2026 (closeout — die_reward labelling + GDD Magic Door accuracy) | **Two small closeouts from the Magic Door investigation.** **(1) `die_reward` now has proper presentation.** It is a Magic-Door-only room type that was missing from `roomEmoji()`, `roomLabel()` *and* `getMagicHint()`, so it fell through to the `🚪` / "Unknown" defaults — meaning a **revealed** die-reward door was indistinguishable from a hidden one, which on Floors 1-2 (where doors are never hidden) was every one of them. Now `🎲` / "Dice Cache", with the hint "A faint rattle, like dice waiting to be claimed." written in the same one-sentence style as the existing hints. Note the hint belongs to `getMagicHint()`, not `roomLabel()` — the request named the two lookup tables but the hint text lives in the third function, and a label was still needed for the revealed state, which is what the original bug was about. **The label wording was the one value not specified in the request**; "Dice Cache" was picked to sit alongside Battle / Elite Fight / Strange Event / Merchant / Rest Stop and to echo the hint, and is a one-word change if another is preferred. **(2) GDD.md's "Always hidden" claim corrected to match the code.** Resolved as doc-wrong / code-right per owner decision — the early-game reveal is intentional easing, so `isHidden` was left alone. The Room Types row now reads "Revealed on Floors 1-2; on Floors 3-4, 60% chance hidden", backed by a short section that quotes the actual condition (`G.currentFloor >= 2 ? Math.random() < 0.6 : false`), spells out that `G.currentFloor` is zero-indexed so "Floors 3-4" cannot be misread as index 3-4, and describes both branches. The Gambler's Curse override is documented immediately below it — forced hidden *and* hint stripped, one charge per Magic Door actually reached, the only thing in the game that conceals a Magic Door on Floors 1-2 — so the two entries agree with each other and with the code. **Verified by repro on all three states:** a revealed die-reward door renders "🎲 ✨ Magic Door / Dice Cache" with no "Unknown"; a hidden one renders "🚪 / ??? " plus the dice hint and does not leak the label; and a curse-blinded one is still hidden with the hint suppressed and the charge consumed. GDD wording was checked by parsing the threshold and probability straight out of `js/game.js` and asserting the document quotes the same values, rather than eyeballing them. All ten suites and the 33-assertion layout matrix still pass. **Status: Verified.** |
| August 16, 2026 (content fixes — three promise-vs-effect mismatches) | **Closes the three findings flagged during the Magic Door reward investigation. All three were cases of the game promising something it did not deliver.** **(1) Gambler's Curse map blindness now exists.** `G.mapBlind += 3` had been written and never read, so "Risk: Next 3 Magic Doors are hidden" was pure decoration and the offer carried no downside at all. Implemented in `showDoors()` (`js/game.js`). Establishing the baseline first mattered, because it is narrower than the GDD's "Always hidden" wording suggests: `isHidden` requires `G.currentFloor >= 2`, so a Magic Door is **always revealed on Floors 1-2** and only ~60% hidden on Floors 3-4, where hidden doors still show a hint that names the room type fairly plainly ("The scent of candle wax and gold" = Merchant). A blinded door is therefore forced hidden **and** stripped of that hint — confirmed with the project owner as the intended reading. Both halves are load-bearing: forcing hidden is the entire downside on Floors 1-2, while dropping the hint is what stops a charge being spent for nothing on a Floor 3-4 door that was already rolling hidden. Charged **once per Magic Door actually reached** — not per room, not per floor — and stamped on the room object so a re-render cannot burn a second charge. **(2) Cursed Die removed from the unseen die reward.** `giveReward(g,'die')` (one caller: this event) could hand out the d4 Cursed Die, max 4, a strict downgrade from the starting d6, in response to an offer framed as "a powerful reward right now". The pool now excludes d4 alongside the already-excluded d6. Deliberately scoped to the *unseen* grant: the Magic Door die chooser still offers the Cursed Die, because there the player reads its range and bonus before picking, so no promise is broken. **(3) Hidden Cache no longer promises an item it cannot give.** The choice read "Take it all (25 Gold + item)" and the description "a stash of gold and something extra", but the effect granted only Gold. Checked for a reusable grant path first — there is **no consumable or item system in the codebase at all** (Consumables are designed but unbuilt), so per the agreed fallback the text was corrected rather than a new item system invented for one event. Both the choice label and the description were fixed; correcting only the label would have left the description still promising "something extra". **Verified by repro, not code reading:** Floor 1 baseline confirmed revealed; the curse accepted through the real event; three consecutive Magic Doors each hidden with no hint and the counter walking 3→2→1→0; the fourth door back to baseline; a re-render of the same door not double-charging; a plain non-magic room not consuming a charge; 400 die grants containing zero d4 and zero d6 while the visible chooser still offers d4; and Hidden Cache granting exactly 25 Gold with no deck or relic change and a matching toast. All nine other suites and the 33-assertion layout matrix still pass; `newGame()` unchanged. **Status: Verified.** **Flagged in passing, not fixed:** `die_reward` is a valid Magic Door room type but is missing from both `roomEmoji()` and `roomLabel()`, so it falls through to `🚪` / "Unknown". A *revealed* die-reward door (25% of Magic Doors; always revealed on Floors 1-2) therefore renders identically to a hidden one — the same label-does-not-describe-the-content bug as the earlier Sharp Card / Blizzard shop tile. The fix is one entry in each lookup table, but the player-facing wording is a content decision, so it is left for the owner. |
| August 16, 2026 (bug fix — invisible toasts / silent rewards) | **Every `showMsg()` toast fired outside combat was rendering behind the screen and had never been visible.** Reported as "a Magic Door gave me a reward with no confirmation" — the player only worked out they had been given a bigger die when they later rolled a 17. The message was never missing: `giveReward(g,'die')` has always called `showMsg('… equipped!')`. **The real cause is a stacking-order bug.** `.message-log` is `z-index: 30`, while `#door-screen`, `#event-screen`, `#shop-screen`, `#rest-screen`, `#reward-screen`, `#path-screen` and `#boss-intro-screen` are **all `z-index: 50`**, so any toast raised on those screens was painted behind the screen's own content. Only combat toasts were ever visible. Found by screenshotting the actual pixels — an early probe checking `.visible`/opacity said the message was fine, and `elementFromPoint` is useless here because the log is `pointer-events: none`. Raised to `z-index: 550`: above every screen and the map overlay, still below the deliberate full-screen dramatic overlays (600) and the tooltip (99999). **Three supporting fixes on the same path:** `showMsg(txt, ms)` takes an optional duration; the die grant now holds for 5s because it fires on the event screen and `proceedDoors()` swaps screens 800ms later, so a 2.5s toast spent most of its life mid-transition (the log is a body-level fixed element, so it survives the change and stays readable after it); and `.message-log`'s `white-space: nowrap` became `normal` with a `min(90vw, 440px)` cap, because it silently **clipped** anything longer than the viewport — a truncated confirmation being precisely the failure this is meant to prevent. Reward text is now built by a shared `dieGrantMessage()` used by both die-granting paths (the random event grant and the Magic Door chooser) so they cannot describe the same event differently: "🎱 Hunter's Die equipped — rolls 1–8 (was Standard Die, 1–6). Odd rolls deal +2 bonus damage." — name, new range, what it replaced, and the bonus. Verified at desktop, mobile landscape and mobile portrait: shown, unclipped, inside the viewport, and still readable for ~4.2s *after* the screen transition. All nine prior suites and the 33-assertion layout matrix still pass. **Status: Verified.** **Two content findings reported to the project owner and deliberately NOT changed** (they are design decisions, not bugs to silently patch): (1) "The Gambler's Curse" event increments `G.mapBlind += 3` and advertises "Next 3 Magic Doors are hidden", but **`G.mapBlind` is never read anywhere in the codebase** — the downside is completely inert, making the offer pure upside; (2) the same event's "powerful reward" is a random die from every type except d6 and your current one, which **includes the d4 Cursed Die** — it can hand you a strict downgrade while charging you a curse. Also spotted in the same audit: the "Hidden Cache" event's choice reads "Take it all (25 Gold + item)" but grants only Gold — no item is ever given. |
| August 16, 2026 (True Ending gate rewire — path complete) | **Aldric's Phase 3 / True Ending gate now reads the permanent Challenge relic record. The True Ending path is wired end to end for the first time.** One line of logic, `G.aldricHasRelics = G.cores && G.cores.length >= 4` → `hasTrueEndingRelics()`, plus a new `hasTrueEndingRelics()` / `challengeRelicCount()` / `TRUE_ENDING_RELICS_REQUIRED` trio in `js/meta.js` so the 4-of-5 rule lives in one place rather than being counted inline. **Timing deliberately preserved:** the gate is still evaluated exactly once, at `startAldricFight()`, before the first turn, and is never re-checked mid-fight — same as the old check; only the data source changed. **Why the old check was wrong twice over:** Cores are a different system (lore reveal + Challenge unlock), and `G.cores` is a *per-run* count, so simply beating four floor bosses in one ordinary run unlocked the True Ending — exactly the exploit the July 25 redesign existed to remove. That is now the headline regression test: a run carrying 4 in-run Cores and 0 Challenge relics leaves the gate **closed**. `G.cores` itself is untouched and keeps its own job (`renderCores()`, the Challenge-unlock trigger via `META.coresCollected`). **Dead superseded content removed, per the audit and a design decision taken with the project owner this session.** `ALDRIC_RELIC_TRIGGERS` carried THE CROWN / THE SWORD / THE SIGIL / THE VOW — four named relics from the deleted four-relic design. Of the two options offered (strip them entirely vs. leave them commented as inert), a third was chosen deliberately: **keep the four HP-threshold beats and Aldric's own dialogue, drop the relic identities.** Reason: removing the beats outright would have silently made the True Ending fight harder — he currently *stops attacking* at 25 HP and grants infinite rerolls from 50 HP — which is a balance change disguised as a cleanup, and GDD §9 explicitly defers designing what the five Challenge relics do in this fight, so inventing replacements was out of scope. The beats are now unattributed ("THE RELICS PULSE"), the quotes stay because they are Aldric's soul surfacing and were never relic-specific, and the stale `THE CROWN` placeholder in `index.html`'s overlay markup was updated too. **The audit's "Sword's `G.enemy.damage` halving is dead code" finding is confirmed and the line is deleted** — `aldricAttackProfile()` hardcodes `base = 15` for a gate-passed Phase 3 and never reads `G.enemy.damage`, so the halving could never reach the damage path (a previous session had already documented this in a comment without removing it). Its 75 HP beat remains, since pausing Aldric's attack for that turn was its only real contribution. The victory text's "The Vow glows" was rewritten for the same reason. **`js/debug.js`:** `cores=N` no longer unlocks Phase 3 and now says so; a new `crelics=` option seeds the permanent Challenge relic record — either a count (`crelics=4`) or a hero list (`crelics=mage,thief,gambler,vampire`) — **in memory only, never `saveMeta()`**, matching how `cores` and `challenge` already avoid writing into a real player's save. The Aldric jump's console line now reports relics held against the requirement instead of Cores. **Verified — including the full chain with no debug seeding of `META` at all:** four Challenge relics earned through the real two-run flow per hero (collect the Core via `checkCombatEnd()`, reload, meet them again, opt in at the boss intro, win under Challenge conditions), persisted to `localStorage`, then a fresh Aldric fight reads the gate **open** while that run's `G.cores` is empty — proving the gate no longer touches Cores. Also verified: the gate boundary at 0/1/2/3/4/5 relics (opens only at 4, and a 5th cannot occur since you can never Challenge your own hero); with 4 relics Phase 3 is reached, all four threshold beats fire with the shared unattributed banner, the 50 HP beat grants infinite rerolls, the 25 HP beat stops him attacking, and the True Ending screen shows; with 3 relics the gate is closed, Phase 3 still runs as the Unbreakable wall, **no** threshold beats fire, Aldric still attacks, and the Normal Ending shows. **No regression to the Weak/Chill/Rage routing fixed earlier this session** — re-tested through `aldricAttackProfile()`/`enemyAttackDamage()`: Weak still reduces (15 → 11), Rage still raises (15 → 21), Phase 2 is still a 3-hit volley. All eight prior suites and the 33-assertion layout matrix still pass. **Status: Verified.** **What remains is design, not wiring** (both recorded in `DESIGN_DISCREPANCIES.md`): (1) GDD §9 still defers what the five Challenge relics individually DO in the Aldric fight — they are earn-and-store only today, and the four unattributed beats are placeholders holding the pacing; (2) GDD §1's "at 50 HP, choose to use the relics instead of the killing blow" **is not implemented** — the True Ending fires on the killing blow with the gate passed. The path is complete and reachable; it is not yet told the way the GDD describes it. |
| August 16, 2026 (new system — Challenge-mode fights) | **Challenge relics can now actually be earned. This closes the second of the three True Ending pieces; only Aldric's gate rewire remains.** Six design questions were settled with the project owner before any code was written, and two of the answers changed the build: the attempt is **opt-in** (a prompt on the existing `showBossIntro()` screen, matching the GDD's "pick which 4 challenges to chase"), and an **already-earned Challenge is never re-offered** — so eligibility checks `!hasChallengeRelic()` as well. **Eligibility** (`isChallengeEligible()`, js/combat.js) requires all of: a Challenge exists for that hero; the boss is not the player's own hero; their Core was collected in an **earlier** run; and the relic is not already earned. The Core check reads `META.coresCollected`, never `G.cores` — `G.cores` is rebuilt empty each run, so using it would let a companion qualify inside the very run that first beat them, which is not what "in a future run" means. **Enforcement.** The two Denial rules are suppressed at their **choke point** rather than gated per card: `gainBlock()` for Thief, `drawCards()` for Mage. That was chosen over the `CARD_PLAY_CONDITIONS` dim-and-warn pattern after checking the card pool — draw and Block are frequently *affinity-conditional* ("Odd: gain 7 Block + draw 1"), so a static card list would dim Shadow Step even on rolls where it grants no Block at all, and would need hand-maintaining against `js/data.js` forever. One guard each covers every card, plus relic/status draws (Lucky Coin, Lucky Streak), by construction. **A softlock was avoided here:** the turn-start deal of 5 cards runs through the same `drawCards()`, so blanket suppression would have left the player with no hand at all — `drawCards()` now takes `{ turnStart: true }` from the two turn-start call sites, and the Challenge denies only *extra* draws, which is what GDD §9 actually says. Gambler's denial disables the **reroll button** (relabelled "🚫 NO REROLL", with `useReroll()` guarded as a backstop); card-driven rerolls (Risk Taker, Wild Combo) deliberately still work, since they cost a card and Energy and are a different resource from the free per-turn reroll. The two Escalation rules tick from `tickChallengeEscalation()`, called at the same point in `endTurn()` (STEP 5) where every other turn-triggered enemy ability fires, gated on `G.turn`: Barbarian gains +1 Strength on turns 2/4/6 — implemented as enemy `'💢Rage'`, which *is* Strength for enemies in this codebase, rather than a parallel stat — and Vampire drains 8 HP on turns 3/6/9, with a `G.hp <= 0` check straight after so a lethal drain resolves before the enemy also acts. **Earning** is on the win: `checkCombatEnd()` calls the new `recordChallengeRelicEarned()` (js/meta.js), which mirrors `recordCoreCollected()` exactly — first-time-only, returns true only then, writes through `saveMeta()`. Losing simply never reaches that line, which is the whole of "a failed attempt costs nothing". `G._challenge` is cleared by `startCombat()` and `startAldricFight()` but deliberately **not** by `startBossFight()`, since the intro sets it immediately before; `showBossIntro()` also resets it on every showing, so an accepted-then-abandoned Challenge cannot leak. **Verified end-to-end, per hero, through the real two-run flow:** collect the Core in run 1 via the real `checkCombatEnd()`, **full page reload**, new run with a different hero, confirm the offer appears, opt in, then drive real turns through `endTurn()` and watch enforcement land turn-by-turn — Barbarian's Strength traced 0,0,1,1,2,2,3,3 across turns 1-8 (firing on 2/4/6, never on odd turns) and Vampire's boss HP traced 5000 → 5008 → 5016 (firing on 3/6, with the player losing exactly 8 across the turn-3 tick) — then win and confirm the relic is recorded, written to `localStorage`, and **still present after another full reload**. Also confirmed: not offered when the Core was never collected; not offered for the player's own hero; not re-offered once earned; declining leaves Block/draw/reroll working normally; losing earns nothing and reaches the game-over screen without a softlock; and the flag cannot leak into a normal, elite or Aldric fight. `js/debug.js` gains `challenge=1` (or `challenge=<heroKey>`) so an eligible fight can be reached in one command instead of two real runs — it seeds `META` **in memory only, never `saveMeta()`**, so testing cannot write into a real player's save, matching the existing `cores` option's design. **Status: Verified.** **Two notes.** (1) The Challenge relics currently have **no in-fight effect** — they are earn-and-store only. GDD §9 explicitly leaves their Aldric-fight effects to be redesigned against the 5-relic pool. (2) `newGame()`'s reset surface was re-snapshotted and is still byte-identical (44 keys), Aldric's gate is untouched, and all seven prior suites plus the 33-assertion layout matrix still pass. **Pre-existing issue found, not fixed:** the boss-intro screen already overflowed short landscape viewports before this work — "FACE THEM" sits below the fold at 340px and under, reachable only by scrolling the screen's own `overflow-y: auto` (measured 423px of content in a 340px viewport with the panel hidden). The Challenge panel adds ~8px after being given a compact treatment at that breakpoint, so it does not meaningfully worsen it, but the screen wants a proper short-landscape pass. |
| August 16, 2026 (new infrastructure — cross-run persistence) | **Castle Run now has a save system. New file `js/meta.js`, loaded first, holding `META` — the only state that survives a run.** Before this, nothing at all persisted between page loads, which an independent audit correctly identified as the real blocker on the True Ending path: GDD §1/§9 require that beating a corrupted companion unlocks their Challenge *in a later run*, and that Challenge relics accumulate *across runs*, none of which is expressible without storage. Confirmed first that no `localStorage` / `sessionStorage` / cookie usage existed anywhere in the split build, so there was no key convention to match; the key is `castleRunProgress`. **Schema is versioned from day one:** `{ version: 1, coresCollected: [charKey…], challengeRelicsEarned: [charKey…] }`, using the same hero `charKey` strings `G.cores` and `BOSSES` already use. **Deliberately minimal — exactly two permanent facts and nothing else.** No run statistics, achievements or settings. Souls, deck, relics held, Gold, HP, floor/room progress and hero pick all remain per-run; `newGame()` was not touched at all, and that was verified rather than asserted (see below). **The "first time ever" distinction is the whole point and is why this is not a mirror of `G.cores`:** `G.cores` is rebuilt from empty every run, so it can say "collected this run" but never "collected before". `recordCoreCollected(charKey)` writes only on a companion's first-ever Core and returns `true` only then, so re-beating a companion in a later run is a no-op with no duplicate and no redundant write. Hooked in at the existing collection point in `checkCombatEnd()` (`js/combat.js`), immediately beside the existing `G.cores.push(...)`. **One guard was needed there:** Aldric sets `G.inBoss` *as well as* `G.isFinalBoss`, so that branch also runs when he dies — with `G.currentFloor` still 3, meaning `boss` is the floor-3 companion. The persistent write is therefore guarded on `!G.isFinalBoss`. (Observed in passing and left alone as out of scope: the pre-existing `G.cores.push` in that branch is *not* guarded, so killing Aldric pushes a duplicate of the floor-3 companion into the in-run list — measured going 1 → 2. It is invisible today because `renderCores()` uses `includes()` and the Phase 3 gate `G.cores.length >= 4` is evaluated at fight *start*. Worth fixing when that gate is revisited.) **Loading is defensive and was tested against hostile input, not just happy path:** non-JSON garbage, `[]`, `null`, a bare string, a number, an empty string, a missing `version`, a *newer* `version`, a `version: 1` save with `coresCollected` of the wrong type, one with no fields at all, and one padded with duplicates/numbers/nulls/objects — all 12 degrade to a valid empty `META` with no thrown error and no console error. Two deliberate details: `loadMeta()` **never writes a repaired copy back**, so opening the game in an older build cannot destroy a save it does not understand (verified the `version: 99` blob is still byte-identical on disk afterwards); and a fresh browser leaves no key at all until the player actually earns something. Storage access itself is inside the `try`, not just the parse, so private-mode/disabled-storage browsers that throw on `getItem` still boot — a failed save is logged and the unlock simply holds for the session. **Verified end-to-end, not by asserting `setItem` was called:** beat a real floor boss through the real `checkCombatEnd()` path → record written; **full page reload** → still recorded; `newGame()` with a different hero → permanent record intact while `G.cores`, Souls, Gold, relics, Soul upgrades, deck, HP, floor and `runSouls` all reset normally. Then five sequential runs with a full reload between each, deliberately re-beating already-collected companions on runs 4 and 5: the permanent list accumulated to exactly the 3 distinct companions and stopped, no duplicates. Also confirmed `js/debug.js`'s `G.cores` pre-grant (`?debug=aldric&cores=4`) does **not** touch `META`, so testing cannot pollute a real save. **`newGame()`'s reset surface was proven unchanged** by snapshotting all 44 keys of `G` plus every scalar and array value after `newGame('barbarian')`, then `git stash`-ing this change and snapshotting again on the original code: byte-identical, zero page errors both ways. **Status: Verified.** **NOT DONE, and this must not be mistaken for the True Ending path being unblocked:** `challengeRelicsEarned` is a **reserved, unwritten slot**. Nothing earns a Challenge relic, because Challenge-mode fight logic — detecting that a Challenge attempt is being made and enforcing its constraint (per-hero, GDD §9) — does not exist in code at all. The field is parsed, normalized and written back on every save so it round-trips safely; wiring it up later means pushing a charKey onto it and calling the existing `saveMeta()`, with no schema migration. Aldric's Phase 3 gate (`G.cores.length >= 4` in `js/combat.js`) was **not** touched, per scope — it still reads the per-run Core count, not Challenge relics, and rewiring it to `META.challengeRelicsEarned` is a follow-up that only makes sense once relics can actually be earned. Remaining order of work: Challenge fight logic → Challenge relic earning + save → Aldric gate rewire. `CLAUDE.md` updated with the new file, the new load order, and a Cross-run persistence section. |
| August 16, 2026 (bug fix — mobile card preview) | **The mobile card preview now actually renders. Closes the first of the two pre-existing bugs flagged in the mobile-landscape layout row below.** Root cause was as diagnosed there: base CSS carries `.mobile-card-preview { display: none }` (correct — the preview must never appear on desktop, where cards are full size and carry their own text), and the landscape block that styles it never set `display` back. Every other rule in that block — border, gradient, shadow, cost badge, emoji, name, type, condition, description, bonus, hint — was already authored and correct, so `renderHand()` had been populating and marking `.active` an element with a 0×0 box since the split build. One-line re-enable, scoped to the landscape block only; desktop keeps the base `display: none` (re-asserted by test, plus a check that cards there are still uncompressed at 112px and still play on a single click). **Surfacing it exposed a positioning bug that had never been observable.** The preview was anchored `bottom: calc(var(--combat-bottom-hud-height) + 8px)` — measured from the bottom of `#combat-screen`, which assumes the hand row sits flush with the viewport bottom. It does not: `#combat-screen` is a column flex packed to `flex-start`, so when its content is shorter than the viewport the slack collects *below* the hand row — measured **13.7px at 915×352 and 47.7px at 844×390** — and the preview settled that far down, overlapping the very cards it describes. 800×340 escaped by 0.3px, which is exactly the kind of margin that reads as "fine" until a different device. Fixed by anchoring to the hand row itself rather than the screen: `ensureMobileCardPreview()` now parents the element to `#hand-area` (which is already `position: relative`) and the CSS uses `bottom: calc(100% + 8px)` / `+ 6px`, so it is always a fixed gap above the cards whatever the slack. Verified no overlap with the hand row, either sprite, or either HP/Block column, and fully inside the viewport, at 915×352 / 844×390 / 800×340 / 740×320 / 667×300. It sits in the empty centre gap between the two combatants, is `pointer-events: none` so taps pass through, and `z-index: 34` keeps it above the arena. **Content was audited rather than assumed correct, per the "never been seen" risk — and it was not correct.** Damage and Block numbers check out: the preview's description is the same `displayDesc` string the tile uses, and its roll comes from `G.currentDie || 1`, the identical expression `playCard()` uses, so affinity branches cannot drift. Confirmed by playing each card and comparing the resolved delta against the displayed figure — Heavy Blow even→16 / odd→10, War Shout even→10 Block, and with Rage 5 (→21), Weak 2 (→12) and enemy Vulnerable 3 (→24) all matching. **The Energy cost did not match.** `renderHand()` reproduced only the Mana Surge branch, while `playCard()` applies three discounts, so a card made free by **Soulbound Gauntlet** (first card each turn) or **Shadow Artist** (2nd/4th card of the turn) displayed its printed cost. This was not merely cosmetic: the same figure feeds `canPlay`, so a genuinely free card was marked `.unplayable` and the second tap refused to play it — **a free card could not be played at all** at 0 Energy. Fixed by extracting the cost to a single `getCardEnergyCost(g, cardKey, { consume })` in `js/combat.js`, following the existing `calculatePlayerAttackDamage(g, amount, { consume })` precedent: `renderHand()` calls it read-only for the tile, the preview and `canPlay`; `playCard()` calls it with `{ consume: true }` to spend the one-shot flags. The discount badge also now lights for any discount rather than Mana Surge alone. **Scope note — this one shared-logic fix necessarily reaches desktop**, since the card tile uses the same figure. Measured: with no discount active desktop is unchanged (costs 2/1/1/1/1, no badge, 112px tiles, single-click play, printed cost charged); the only difference is that a card that really is free now reads **0 instead of its printed cost and is no longer greyed out** — the previous desktop behaviour was simply wrong. `playCard()`'s pre-existing order, which consumes the one-shot discounts *before* the affordability check, was deliberately preserved so this stays a refactor; it is now much harder to reach, because the UI no longer offers an unaffordable card. **Verification:** 16 value assertions comparing preview figures to resolved effects, 12 assertions on the free-card/Shadow-Artist/unaffordable/hard-play-gate paths (Backstab out of position correctly shows "⚠ Must be the first card played this turn" and the `condition-unmet` styling), the full 11-viewport layout matrix from the row below re-run with **every metric byte-identical** (card overlap, hand overflow, arena height, both sprite heights, HUD centre offset), and desktop checked at 1440×900 and 1280×800. **Verified** at the rendered-pixel level in headless Chromium at approximated Android landscape sizes; as with the row below, not re-checked on a physical device. **Still open:** the second flagged item — the short-landscape block's `#combat-screen { --combat-bottom-hud-height: 92px }` remains dead, losing on specificity to `#combat-screen.active { … 100px }` in the wider block. The preview no longer depends on that variable, but `.hand-area` still sizes from it, so the 92px value has never taken effect. |
| August 16, 2026 (bug fix — mobile landscape layout) | **Three reported mobile-landscape defects fixed in `css/styles.css`; every change sits inside a `@media (max-width: 1100px)` block, so desktop is structurally untouched.** Reproduced at real viewport sizes in headless Chromium (Playwright, `deviceScaleFactor: 2`, touch, `?debug=combat`) before any CSS was read for blame — 915×352, 844×390, 800×340, 740×320, 667×300, plus tablet 1024×700 and desktop 1440×900 / 1280×800 as controls. **(1) Card text truncation was not a text problem at all — it was overlap.** Every text node measured `scrollWidth === clientWidth`; nothing was internally clipped. `.card.mobile-compressed` carried `margin-left: -14px` (wide-landscape block) and `-12px` (short-landscape block) plus `z-index: calc(10 + var(--card-index))`, so each card painted over the right 12-14px of the one before it. Only the **last** card in hand was ever fully readable — which is exactly what the reported "Heavy Blo…", "War Shou…", "Iron Bash" pattern shows. Measured hand-row width at the narrowest tested viewport is 494px against 5×56px of cards, so the fan bought no space; removed both negative margins and gave `.card-stage` a real gap (0.25rem / 0.2rem). Card width, height and font sizes are unchanged. The z-index ladder stays — it still raises a tapped card. **(2) Enemy sprite oversized/clipped: an ID-specificity collision.** The short-viewport block sets `.combatant-sprite { height: clamp(58px, 9vw, 76px) !important; max-height: 76px }`, but `#player-sprite`/`#enemy-sprite` in the wider landscape block are ID-level, `!important`, and set `max-height: none` — ID (1-0-0) beats class (0-1-0), so the cap never applied and the sprites stayed 96px/104px on every phone. The enemy column, which also carries the intent box, then exceeded the arena and `#combat-screen.active { overflow: hidden }` sheared its top: measured overflow **18.3px at 915×352, 30.3px at 800×340, 50.3px at 740×320, 70.3px at 667×300**. (The player was clipped too on the shortest screens — 26.3px at 740×320 — just less visibly, which is why only the enemy was reported.) Both sprites now share one box: 100px in the wide-landscape block, and a viewport-tracking `--combat-sprite-h: clamp(40px, calc(100dvh - 260px), 88px)` (with a `100vh` fallback declaration first) in the short-landscape block. Emoji enemies are rendered as text, so `font-size` is derived from the same variable and shrinks with the box. **(3) HUD icon row off-centre: an asymmetric grid.** Desktop uses `grid-template-columns: minmax(0,1fr) auto minmax(0,1fr)`; the mobile block used `… auto auto`, letting the left track absorb all slack and pushing the centre row right by a measured **249.9px on tablet and 195.4 → 71.4px across phone widths**. Restored the symmetric desktop track list — offset is now 0.0px at every width, with no right-column overflow. **One further change, not in the report:** the fix in (2) initially forced sprites down to 32px on a 300px-tall viewport, because `.combatant-info` reserves height rather than sizing to content and was holding ~16px of permanently idle space beneath each column. Confirmed idle by applying a full status set (weak/chill/rage/poison/burn + vulnerable) and re-measuring — `.combat-stat-row` stayed 43.5px and column heights did not move, so status icons already fit inside the existing box. Reclaimed 12px (player 92→80, enemy 108→96), keeping a few px of slack and **preserving the deliberate 16px enemy/player offset that is what keeps the two HP bars and Block readouts on the same line** (measured content-bottom delta stays 0.1px, with and without statuses). Sprites are now 40-88px instead of 32-72px. **Verification:** an 11-viewport matrix asserting zero card overlap, zero hand overflow (checked at 5 **and** 8 cards), zero sprite clipping, identical player/enemy sprite box, centred HUD and no document overflow — all pass, including boss fights with image sprites, the Aldric fight, and a second hero. Card selection and the raised/selected tile re-checked by actually tapping a card. Desktop diffed metric-by-metric before vs after: card width, overlap, arena height, both sprite heights, HUD offset and overflow are **byte-identical**. **Verified at the rendered-pixel level in headless Chromium at the reported class of viewport; not yet re-checked on the reporter's physical Android device** — screen sizes were approximated from standard Android landscape geometry with browser chrome subtracted, since the exact device viewport was not stated. **Two pre-existing issues found and deliberately not fixed** (both confirmed present before this session's changes by re-running the same test against the stashed original stylesheet): the mobile card preview never renders — `.mobile-card-preview { display: none }` is set in base CSS and no landscape rule re-enables it, so `renderHand()` populates and marks `.active` an element with a 0×0 box, contradicting CLAUDE.md's "selecting a mobile card should show an unobstructed preview"; and the short-landscape block's `#combat-screen { --combat-bottom-hud-height: 92px }` is dead, losing to `#combat-screen.active { … 100px }` in the wider block on specificity. Both are reported for a design call rather than changed under a layout-bug brief. |
| August 16, 2026 (UI tweak) | **Turn indicator switched from icon+number to plain text — now reads "Turn N".** Player reviewed the live build and preferred a text label over any icon, matching how the HUD labels HP as text rather than a glyph. One-line change in `index.html`: `🔄 <span>` became `Turn <span>`. (Note for the record: the chip was still 🔄 — the ⏳ hourglass variant mentioned in the brief was never applied to the code, so nothing needed undoing.) Position in the HUD centre row, the inherited `.hud-resource` class, the `.hud-turn` colour rule, `updateHUD()`'s render path and the per-combat resets in `startCombat()`/`startBossFight()`/`startAldricFight()` are all untouched — confirmed by diff. **Crowding checked with measured estimates rather than assumed:** "Turn 6" is ~28px at the mobile breakpoint versus ~31px for the 3-digit Gold chip the row already carries in normal mid-run play, so the text form does not create a new worst case; the whole centre row grows ~8px (4.8%), and the row is `white-space: nowrap` inside a grid whose left column carries `overflow:hidden`, so pressure lands on the floor/room/cores area rather than wrapping or pushing the arena. Two-digit turns ("Turn 12", ~33px) become the row's widest chip, which is the case worth watching. A shorter "T.N" form (~23px) is the fallback if it reads tight in practice. The 20-assertion turn-display suite still passes unchanged, as do all eleven other suites. **Implemented** — text rendering and live update re-verified headlessly; the narrow-landscape appearance of the wider label still wants the same eyes-on pass as the rest of this session's HUD work. |
| August 16, 2026 (UI) | **Turn indicator added to the combat HUD.** `G.turn` was previously write-only — incremented in `startTurn()` and read by nothing, so no turn number had ever been displayed. **Placement: the combat HUD's centre resource row**, appended after Souls as `🔄 N`. Chosen because that row is already the home for icon+number readouts (Gold, Energy, Draw, Discard, Souls), so the chip reuses the existing `.hud-resource` class and inherits every responsive rule — including the 0.62rem mobile font and tightened gaps — **without a single new `@media` rule, absolute position, or layout box**. That matters given the still-unverified layout risk from the Second Die / Gambler's Edge dice buttons: this deliberately adds no new positioned element, and the HUD row is a flex line in a grid cell whose left column already carries `overflow:hidden`, so it absorbs the extra width rather than pushing the arena. Rejected the enemy/intent area (AGENTS.md flags intent overlap, and past sessions fixed exactly that) and the hand area (tightest vertical space at short landscape heights). Styled `var(--text3)` — the quietest colour in the row — so it does not compete with HP/Block/Energy/dice/intent. Icon+number matches the neighbouring chips' wordless convention; the full "Turn N of this combat" wording lives in the `title` tooltip. **One state change was required despite this being framed as display-only:** `G.turn` only ever incremented across an entire run, so without a reset the counter would have shown stale numbers on every fight after the first. It now resets in `startCombat()`, `startBossFight()` and `startAldricFight()` — three lines, and safe because nothing else reads `G.turn` (verified: still exactly one `G.turn++` site). Aldric resets once at fight start, **not** per phase transition, so his three phases read as one continuous fight; `G.aldricTurns` still resets per phase, unchanged. Leakage outside combat needed no logic: the chip lives inside `#combat-screen`, so screen switching hides it structurally. Verified by a 20-assertion test: displayed turn walks 1→2→3→4→5→6 through real `endTurn()`/`startTurn()` cycles, restarts at 1 for the next battle, boss fight and Aldric fight, keeps counting across a phase-2 transition, and `updateHUD()` calls from the shop/reward/rest paths neither throw nor render junk. **Implemented — not Verified: no browser check yet, and the short-landscape width cost of a 6th HUD chip is exactly the thing that needs eyes-on rather than assertions.** |
| August 16, 2026 (dev tooling) | **Dev jump tool added (`js/debug.js`) — testing convenience, not a player feature.** Verifying floor- or enemy-specific behaviour previously meant replaying from Floor 1; this drops straight into a target state. Checked first that no debug/dev-mode infrastructure already existed (none did — no flags, no admin panel, nothing parsing the URL). Six targets: `aldric`, `boss`, `bossintro`, `floor`, `combat`, `elite`. Console `dbg('aldric')` / `dbg('boss', {floor:2})` (aliases `__debugJumpTo`, `__debug.jump`) or URL `?debug=aldric&floor=4`. Options: `hero`, `floor` (clamped 1-4), `gold`, `souls`, `hp`, `cores` (4 unlocks Aldric's Phase 3 path), `relics=a,b`, `upgrades=a,b`, plus pre-applied status stacks — `weak`/`chill`/`rage`/`poison`/`burn` on the enemy and `vulnerable` on the player, which makes the status maths fixed earlier this session spot-checkable in one command. Deliberately **not a special test mode**: it calls the real `newGame()` and the real fight-start functions, so the resulting combat is an ordinary one started from a shortcut. **Isolation:** the file is self-contained and loaded last; no gameplay file references it (`grep` for `dbg`/`__debug` across `data/game/combat/ui/main.js` returns nothing), the only edit outside it is one `<script>` tag, no UI entry point exists, and it does nothing at all unless `?debug=` is present or a console global is called. Self-tested with a 50-assertion suite: the Aldric jump produces a fully playable fight (full HP, Energy, 10-card deck, 5-card hand, die rolled, Stone Heart and enemy turns resolving, cards playable), boss jumps work on all four floors, floor/combat/elite jumps work, every option is honoured, bad input (unknown target/hero/relic, out-of-range floor) is handled without throwing, and a normal New Game is byte-for-byte unaffected — Floor 1, 30 Gold, 0 Souls, no cores/relics/upgrades leaked, no enemy pre-spawned. Documented in `CLAUDE.md` and in a header block at the top of the file. **Implemented and self-tested** — browser-verified behaviour of the `?debug=` URL path specifically still relies on `window.location`, which the headless harness stubs. |
| August 16, 2026 (bug fix — shop card tiles) | **Shop card-purchase tiles now show real card detail; "Sharp Card" mislabel fixed.** Three of the four top-row tiles showed a generic "Add X to deck" line instead of card info, and one was titled "Sharp Card" while its body advertised Blizzard. **Diagnosis: neither a per-slot generic heading nor a mismatched card lookup — there was no card lookup at all.** Each `SHOP_ITEMS` entry hardcoded its own `name`/`emoji`/`desc` strings, with the actual card key buried inside its `effect` closure, so the title and body were two independent literals and one had gone stale ("Sharp Card" was a leftover label; the card sold is `blizzard`, real name **Blizzard**). The emoji was wrong too — 🗡️ on the tile versus the card's own 🌨️. Fixed by making card stock data-driven: the three entries now declare only `{ card:'<key>', cost }`, and `showShop()` reads name, emoji, type, rarity, Energy cost and effect text live from `CARDS`, so a tile can no longer disagree with what it sells. The rarity lookup that `showReward()` had inline was extracted to a shared `getCardRarity()` used by both screens — with a fallback across all hero pools, since the shop can sell a card outside the current hero's pool (Blizzard is a Mage card any hero can buy) and the hero-scoped rule alone would have mislabelled it. Purchase now uses the same grant the reward screen performs. Result: Blizzard "Attack · Common · ⚡2 — Deal 5 dmg to enemy 3 times", Life Leech "Attack · Common · ⚡2 — Deal 9 dmg. Extreme: drain 12 Block", Iron Wall "Skill · Uncommon · ⚡2 — Gain 14 Block". **Scope confirmed:** Hunter Die and Healing Potion keep their original `name`/`desc`/`effect` shape and rendering path untouched (both re-verified working), and the Dice, Relic and card-removal sections were not modified. Swept the full shop rendering code for other uses of the generic pattern — there were none; the only remaining "added to deck" string is a `showMsg` toast, not a tile. One new CSS class (`.shop-item-type`) mirrors `.reward-card-type`. Verified by a 35-assertion test rendering the shop 70+ times across heroes: every card tile shows real name/type/rarity/Energy/effect, title and body agree, no placeholder survives, buying grants exactly the declared key, and non-card stock still functions. **Implemented** — in-app visual check of the tile layout still recommended. |
| August 16, 2026 (doc rule — no code change) | **DoT-kills-bypass-HP-triggers promoted from implicit step-ordering consequence to a stated `GDD.md` rule. Ordering deliberately unchanged.** Written into §5 Status Effects with a cross-reference in the AI Development Guidelines' Status Timing Rules, since it silently governs every `trigger:'hp'` ability rather than just Undying. Scope was established by inspection and test, not assumed: **exactly four abilities are affected** — Reassemble (Skeleton), Undying (Cursed Knight), Dark Blessing (Corrupted Priest), Undying+ (Cursed Knight+) — and **all four are self-preservation effects; none damage the player**, so the rule is uniformly a player advantage that can never let the player dodge a punish. Two precisions the rule needed and now states: the bypass is **lethal-only** (a Skeleton dropped 12 → 7 by Burn still Reassembles to 15 that turn; a Corrupted Priest pushed below its 50% line by Poison still heals), and **card killing blows always trigger normally**, making DoT the sole route around these abilities. **On the Bone Golem / Collapse question specifically: no such interaction exists.** Collapse belongs to **Void Colossus** (Floor 4 elite) with `trigger:'attack'`; Bone Golem's live ability is Bone Wall (`trigger:'skill'`). The "Bone Golem: Collapse, deals 20 damage when broken below 50% HP" pairing existed only in the pre-sync GDD roster removed in the row below, never in code — so there is no free pass on Collapse damage via poison/burn. Killing an enemy before it attacks denies Collapse regardless of damage type, which is ordinary racing. Flagged for the future: if an HP-threshold ability that harms the player is ever added, this rule stops being harmless and needs revisiting — noted in both the GDD text and `DESIGN_DISCREPANCIES.md`. Verified by a 10-assertion test covering the lethal/non-lethal boundary, card-vs-DoT contrast, and a scan confirming no hp-triggered special damages the player. |
| August 16, 2026 (doc sync — no code change) | **`GDD.md` enemy roster re-synced to `js/data.js`. This was a stale-doc correction, NOT a rebalance — no enemy stat, ability or pool assignment was changed in code.** Follow-up to the investigation in the row below. All four floors were rewritten wholesale rather than patched row by row, because the drift touched nearly every entry: **Floor 1 and Floor 4 turned out to be just as stale as Floors 2-3** (Floor 1's documented Armored Guard / Dungeon Hound / Cursed Statue / Skeleton Archer and Floor 4's Royal Guard / Corrupted Paladin / Shadow Demon / Castle Shade exist nowhere in code, and every elite pair differed), so Floor 4 was included despite the original request naming only 1-3 — leaving it stale would have left the doc self-contradictory, since Cursed Knight+ and its Undying+ ability live there and are directly tied to the Undying wording being corrected. As directed: Cursed Knight stays on Floor 2 with Undying; Skeleton Warrior and Hexed Blade are removed as confirmed dropped; Undying's revive is corrected from "at 1 HP" to **15 HP** (and Undying+ documented as twice at 20 HP). 19 phantom enemies and 6 phantom abilities that never existed in the shipped build are gone from the doc. A **Block** column was added since several code enemies carry non-zero Block (Cursed Knight 8, Armored Knight 12, Death Knight 5, Cursed Knight+ 8) that the doc never recorded, and the elite-to-floor pairing is now stated explicitly (`startCombat()`'s `floorEliteMap`). Checked first that none of the 19 removed names were referenced anywhere else in `GDD.md` — they were confined to the roster section. Tables were generated from `js/data.js` rather than transcribed, then **verified by a script that parses the rewritten doc back out and diffs every name, HP, Block, damage, ability name and ability description against the live data — 0 mismatches**. One deliberate deviation: the Void Colossus row mirrors the code's ability text verbatim, with the `bypassBlock` detail moved to a footnote so the table stays a faithful mirror. The `DESIGN_DISCREPANCIES.md` roster entry is updated from OPEN to RESOLVED. |
| August 16, 2026 (investigation — no code change) | **Floor 2 "Cursed Knight" identity mismatch investigated: doc-sync issue, not a code bug. Undying's once-per-combat cadence confirmed correct.** A player reported a Floor 2 enemy (Cursed Knight, 75 HP, 13 dmg, Undying) matching no single `GDD.md` entry. Checked the data before assuming a side was wrong: `js/data.js` `FLOOR_ENEMIES[2]` defines exactly that enemy, with name, HP, damage and ability all from one coherent definition — nothing mis-wired, and the player fought precisely what the code specifies. The drift is doc-vs-code and far wider than one enemy: the GDD's Floor 2 and Floor 3 rosters barely overlap with the code's, `Skeleton Warrior` and `Hexed Blade` do not exist in code at all, the GDD's Cursed Knight is a Floor 3 standard (58 HP, Hexed Blade) rather than a Floor 3 *elite* as the report assumed, and even the one shared name (Bone Archer) has different stats and a different ability on each side. Recorded as an OPEN entry in `DESIGN_DISCREPANCIES.md` rather than reconciled, because the direction is ambiguous — the Floor 2 Cursed Knight / Floor 4 Cursed Knight+ pairing looks like a deliberate rebalance, so rewriting ~20 GDD roster rows needs confirmation first. **Undying: Verified.** Gated on a per-instance `_revived` flag; observed 1st killing blow → revives at 15 HP with the fight continuing, 2nd → dies for real and the win resolves, 3rd/4th → stays dead; flag survives turn boundaries and does not leak into the next combat; Cursed Knight+ allows exactly two revives at 20 HP each. Note the GDD says Undying revives "at 1 HP" while code uses 15 HP. **Sub-finding, not fixed:** Burn/Poison killing blows bypass Undying entirely — DoT subtracts HP in `endTurn()` STEPS 1/7 and `checkCombatEnd()` runs at STEPS 4/8, before STEP 9's `hp` trigger, so the enemy dies with `_revived` never set. Same ordering skips any `trigger:'hp'` special. Logged for a design call. No runtime code was changed in this session entry. |
| August 15, 2026 (bug fix — Aldric debuffs) | **Aldric's damage now routes through the shared `enemyAttackDamage()` helper, so Rage / Weak / Chill apply to him like any other enemy.** Closes the OPEN item raised by the status-cadence verification in the row below. Every phase previously called `resolveEnemyAttack()` with a hardcoded number (15 / 8×3 / 20 / 15), bypassing the shared modifier path — yet `endTurn()` STEP 6b still drained a Weak stack per Aldric turn, so the player watched Weak tick down against the final boss while it did nothing. The correctness argument, not the balance one, decided it: visible stacks that do nothing are the game lying to the player. Checked `GDD.md`'s Aldric section first — nothing there assumes independent damage calculation, and the one intentional immunity (Phase 3 "Unbreakable") is expressed by clearing `G.statuses.enemy`, which is preserved. A new `aldricAttackProfile(g)` supplies each phase's base damage and hit count and is shared by `processAldricTurn()` and `updateIntent()`, so his intent cannot drift from the volley he throws — the same unification applied to regular enemies earlier this session; the phase-2 preview now also advertises the 3-hit volley as "×3", which it never did. Measured before → after: **Phase 1** 15 → 11 with Weak, 21 with Rage 6, 15 with Rage 6 + Weak; **Phase 2** per-hit 8 → 6 with Weak, Poison/Burn amplification preserved (8 → 12), and a 3-hit volley consumes **one** Chill stack rather than three (matching the multi-hit rule established for Weak); **Phase 3** no-relics still a flat 20 through Weak+Chill with statuses wiped as before, relic branch 15 → 11 with Weak. Player Vulnerable still amplifies him (15 → 22) and composes correctly with Weak (→ 16). Verified by headless test (30 assertions, stable across 3 runs) including phase-mechanic guards: Stone Heart still restores block, Grieving Ground still adds its curse, phase-2 boss dice still rolls, and the phase-3 relic trigger still fires at the 50 HP threshold and skips that turn's attack. **Noted, not changed:** the Sword relic trigger halves `G.enemy.damage`, but the phase-3 relic branch has always used an explicit 15 and never read it — dead code belonging to the superseded True Ending relic system; and `GDD.md` says Fractured Strike "doubles" with Poison/Burn while the code uses ×1.5. Both recorded in `DESIGN_DISCREPANCIES.md`. **Verified.** |
| August 15, 2026 (verification — status cadences) | **Chill's attack-conditional tick confirmed correct — no behaviour change needed.** Verification task, not a bug report, and the verification came back clean: Chill has exactly one decrement site (`enemyAttackDamage(g, consumeChill)`, with `consumeChill` true only on `endTurn()` STEP 6's attack branch). Observed across 15 assertions: 3 stacks unchanged over 4 consecutive defend turns (`3 → 3 → 3 → 3 → 3`), intact after 20 defend turns, and exactly 3 stacks consumed by an interleaved 7-turn run containing 3 attacks; the attack that consumes a stack is correctly reduced 25%; intent previews do not consume stacks. **Weak and Vulnerable confirmed to use the different, plain per-turn cadence — as the GDD specifies and as expected, not a bug:** on defend turns, enemy Weak `4 → 3 → 2 → 1`, player Vulnerable `4 → 3 → 2 → 1`, player Weak `3 → 2 → 1`. Chill is the only action-conditional status in the game. **One real inconsistency found and fixed:** the in-game Chill tooltip claimed "Ticks down each turn," contradicting both the GDD and the verified behaviour — corrected to "Ticks down only when the enemy attacks." Text only; no combat code touched. Two adjacent findings recorded as OPEN design questions in `DESIGN_DISCREPANCIES.md` rather than changed: (1) damaging enemy *specials* neither consume Chill nor have their damage reduced by it (only the basic attack action counts), making Chill weak against special-heavy enemies; (2) Aldric bypasses `enemyAttackDamage()` entirely, so Rage/Weak/Chill never modify his damage — yet enemy Weak still drains a stack per Aldric turn while doing nothing. **Verified** (Chill/Weak/Vulnerable cadences); tooltip fix is Implemented pending an in-app read. |
| August 15, 2026 (bug fix — player Vulnerable) | **Player Vulnerable wired up — closes the Weak/Vulnerable migration gap opened in the split-build history.** The exact mirror of the enemy-Weak defect fixed in the row below: Cursed Hound's rabid bite applies Vulnerable to the player, but nothing read it, so it neither increased damage taken nor decayed — it stuck forever doing nothing. Confirmed scope first: exactly one source in the entire game (`js/data.js`, Cursed Hound's `trigger:'attack'` special) and zero reads anywhere in the combat loop. **Historical record:** `git log -S` shows both halves of this pair were lost in the *same* migration — commit `90c74a0` carried a `getModifiedIncomingDamage()` helper that read player Vulnerable and Fly, and it did not survive the re-split in `bb85760` ("Re-split castle-run.html into separate CSS and JS files"), the same commit family that dropped the enemy-Weak branch. So this was one migration loss with two symptoms, now both closed. Fixed via `applyPlayerVulnerable()` in `js/combat.js`, applied inside the shared `resolveEnemyAttack()` pipeline so it covers basic attacks, enemy specials and Aldric — the same breadth enemy Vulnerable gets from `calculatePlayerAttackDamage()` — and ticked in **STEP 6b**. **Timing was re-derived, not assumed:** STEP 2b (where player Weak lives) would be wrong, because player Vulnerable modifies the enemy's STEP 6 attack, so ticking it earlier would strip a 1-stack application before the attack it amplifies. STEP 9's attack-trigger specials run after the tick, so Cursed Hound's fresh stack correctly survives to hit the following turn. **Multiplier ×1.5, matching the one rule the game states in both places it is defined** (GDD.md's status table "takes 50% more damage" and the shared in-game tooltip) and the live enemy-side convention; the lost helper's ×1.25 already disagreed with the GDD, so it was not restored — flagged in `DESIGN_DISCREPANCIES.md` in case that asymmetry was once deliberate. `updateIntent()` now composes both helpers, so the previewed number includes Vulnerable and cannot drift from the landed hit the way Rage did. Verified by headless test (40 assertions, stable across 5 consecutive runs): stacks `5 → 4 → 3 → 2 → 1` then removed at 0; a 20-damage hit lands for 30 while Vulnerable and returns to 20 on expiry; Vulnerable 1 amplifies the very next attack then expires; the Cursed Hound sequence deals 10 then 15; intent equals damage dealt across six status combinations (Vuln, +enemy Weak, +Rage, +Chill, +Rage+Weak, +player Weak); enemy specials are amplified too; Block absorbs the amplified hit correctly; and an unafflicted hit is unchanged. One flaky assertion surfaced and was fixed in the harness, not the game — `endTurn()` randomizes the next intent (65% attack), so any damage measured after a prior turn read 0 on a defend turn; measured turns now force `intent='attack'`. **Verified.** |
| August 15, 2026 (bug fix — enemy Weak) | **Enemy Weak fixed: it never ticked down AND never did anything. Reported as a regression from this session — it is not; it predates the session and was proven so.** Evidence: this session's `git diff` of `js/combat.js` touches no line of `endTurn()`, and re-running the repro against `git show HEAD:js/combat.js` (the committed code, before any session work) reproduces it exactly — stacks `6 → 6 → 6 → 6 → 6 → 6` and a Weak-6 enemy still hitting for its full 20. **The previously-resolved "Weak timing" fix in `DESIGN_DISCREPANCIES.md` also did not regress** — STEP 2b is present, correct, and running; it only ever decremented *player* Weak, and `calculatePlayerAttackDamage()` still correctly refrains from decaying the stack (a 3-hit card consumes exactly 1 stack at end of turn, re-verified). The real defect is that **enemy** Weak was read nowhere in the combat loop: 12 card effects apply it (Skull Crack, Iron Roar, War Call, Cheap Shot, Cripple, Bluff + upgrades), and nothing consumed it or applied its 25% reduction, so it stuck at its peak forever as a purely cosmetic status while the player spent cards on a no-op. `git log -S` shows the enemy-side Weak branch existed in commit `90c74a0`'s damage helper and was lost when the monolith was split into `js/*.js` — a migration gap, not a recent break. Fixed by extracting `enemyAttackDamage(g, consumeChill)` as the single source of truth for a basic enemy attack (Rage, then Weak ×0.75, then Chill), used by both `endTurn()` STEP 6 and `updateIntent()` so displayed intent and resolved damage cannot drift; and by adding **STEP 6b**, which ticks enemy Weak *after* the enemy acts. The placement is deliberate and not symmetric with player Weak: player Weak reduces the player's own attacks (already resolved by then, so STEP 2b is right), whereas enemy Weak reduces the enemy's STEP 6 attack — ticking it in STEP 2b would strip a Weak 1 before the very attack it was played to weaken. **Also fixed in passing:** `updateIntent()` previously read raw `e.damage` and applied only Chill, so it understated incoming damage whenever the enemy had Rage; intent now matches the landed hit in all six tested combinations. Verified by headless test (22 assertions) with before/after stack trails — enemy Weak `6 → 5 → 4 → 3 → 2 → 1` then removed at 0; a 20-damage enemy hits for 15 under Weak; Weak 1 still weakens the next attack then expires; intent equals damage dealt for plain / Weak / Rage / Rage+Weak / Chill / Weak+Chill; previewing the intent does not consume a Chill stack; player Weak, enemy Vulnerable, and the multi-hit no-drain rule all unchanged. **Still open, deliberately not changed — same defect, mirror image:** player Vulnerable (applied by Cursed Hound's rabid bite) is likewise never read — it neither increases damage taken nor decays. Making it live would raise difficulty, so it is a design call, not a correction. **Verified.** |
| August 15, 2026 (bug fix — rejection refunds) | **Loaded Die and Death Rattle rejection refunds fixed — same defect class as the Backstab Energy-refund bug in the row below, not a new one.** All of these cards refund your Energy when their hard gate rejects the play, but each refunded a *hardcoded printed cost* rather than the cost actually paid: `g.energy += 1` (Loaded Die / Loaded Die+) and `g.energy += 2` (Death Rattle / Death Rattle+), exactly as Backstab did with its `+= 1`. Whenever the printed and paid costs differ — Shadow Artist making the 2nd/4th card of a turn free, or Mana Surge discounting it — a rejected play returned more Energy than it consumed, minting Energy on demand. All four sites now call the same `refundCardCost()` helper added with the Backstab fix, which returns `G._lastCardCostPaid`. Six rejection paths in `js/data.js` now share one refund path and zero hardcoded amounts remain (Time Warp's `g.energy +=` is a genuine card effect, not a refund, and was left alone). Verified by headless test (48 assertions in the play-condition suite): every gated card is Energy-neutral on rejection both at full cost and while Shadow Artist makes it free. Test sensitivity was confirmed by re-installing the pre-fix effects and re-running the same scenario — the old code mints +1 (Loaded Die) and +2 (Death Rattle), so the assertions genuinely detect the defect rather than passing vacuously. **Still open, deliberately not changed:** Void Channel and Arcane Boost reject with no refund at all, so a blocked play there costs full Energy for nothing. That is a different question from refunding the wrong *amount* — adding a refund where none existed is a design call, not a correction, so it is flagged rather than fixed. **Verified.** |
| August 15, 2026 (UI) | **Unplayable-condition indicator in hand — additive UI, no play logic touched.** Cards with a hard play gate now dim and show a red border plus a "⚠ <reason>" line while their condition is unmet, instead of looking identical to a playable card until the player wastes the play on a rejection. Built generally, not just for Backstab: `CARD_PLAY_CONDITIONS` in `js/data.js` declares each gate as `{ met(g), reason }`, keyed by BASE card key so `+` upgrades inherit it and future cards get the treatment by adding one line. `getCardBlockReason()` resolves it; `renderHand()` applies a `.condition-unmet` class, the reason label, and a `title` tooltip for desktop hover, and swaps the mobile preview's hint slot to the reason (the compressed tile hides its text, so that is where a phone player reads it). **Scope sweep found 5 gated families, not 1** — Backstab (first card only), Death Rattle (below 50% HP), Loaded Die (die already set this turn), Void Channel (needs 2 other cards in hand), Arcane Boost (needs another card to discard) — 10 card entries with upgrades. Roll-conditional bonus branches were deliberately excluded; they always resolve and the dynamic preview already covers them. The indicator is a **warning, not a block**: the card keeps its click handler and `effect()` still delivers the real rejection, so no new invariant. Distinct from the existing `.unplayable` (cannot afford) state, which dims harder and removes the handler. Hand-side checks are off by one from their effect-side twins — the card is still in hand when they run — so `wouldBeFirstCardThisTurn()` was added alongside the verified `isFirstCardThisTurn()` rather than modifying it, and the pairing is documented in both files plus `CLAUDE.md`'s card checklist (new item 7). Verified by headless test (36 assertions), the central one being that the indicator agrees with reality: for all 10 gated cards, in both states, "flagged" matches "actually rejected on play". Also verified: Backstab clear at turn start → flagged the instant another card is played → clear again next turn, across turns 2-5; no false positives across the entire `CARDS` pool; Void Channel / Arcane Boost count the card itself correctly; Void Channel discard mode rendering untouched. **Mobile:** the label is `display:none` on `.mobile-compressed` tiles (registered in the existing compressed rules, no new breakpoints), so compressed-tile layout is structurally unchanged — but this is a structural guarantee, not a device check; short landscape heights still want an eyes-on pass. **Implemented** (not Verified — no in-app render yet). |
| August 15, 2026 (bug fix — Backstab) | **Backstab's "first card this turn" check fixed. Pre-existing, and a DIFFERENT failure class from the July 23 stale-flag bugs (House Edge / Gambler's Fallacy) despite looking like one.** Reported as a stale per-turn flag; it is not. `G._cardsPlayedThisTurn` resets correctly in `startTurn()` — measured 0 at the start of turns 1-4 and at combat start. The actual cause is ordering inside `playCard()`: the counter is incremented (`js/combat.js`, before the Gilded Quill / Soulbound Tome / Lethal Rhythm hooks) but `card.effect()` does not run until ~40 lines later, so **by the time any effect executes the counter already includes its own card**. Backstab checked `(g._cardsPlayedThisTurn||0) > 0`, which is therefore always true — it rejected itself on every turn of every fight and has never once dealt damage. Measured before the fix: 0 damage on turns 2, 3 and 4 played as the literal first card, both base and `+`. Fixed with an `isFirstCardThisTurn(g)` helper in `js/combat.js` that encapsulates the off-by-one, used by both Backstab versions, with a comment directing future "first card" effects to it rather than to the raw counter. **Second defect in the same effect**: the rejection path refunded a hardcoded `g.energy += 1` (the printed cost) rather than the cost actually paid — with Shadow Artist making the 2nd card free, playing Backstab out of position as card 2 cost 0 and refunded 1, minting Energy (reproduced: 3 → 4). `playCard()` now records `G._lastCardCostPaid` and a `refundCardCost()` helper returns exactly that. **Cross-checked every other card-position effect for the same hazard — all clean, and now covered by tests across turns 2-4**: Shadow Artist base (2nd & 4th card free — reads the counter *before* the increment and adds 1, correct), Shadow Artist+ (`_shadowArtistDiscount`, reset per turn), Lethal Rhythm (every 2nd card), Soulbound Tome (3rd card, once per turn), Soulbound Gauntlet (`_firstCardFree`, reset per turn), Gilded Quill (combat-scoped counter). Arcane Barrage reads `_spellsThisTurn` but is an Attack, and that counter only increments for Skill/Power, so it never counts itself. Backstab was the only effect reading a per-turn counter from inside `effect()`. Verified by headless repro (27 assertions): rejection still fires correctly when genuinely out of position; as the first card it now deals 10 (even) / 14 (odd), and Backstab+ 13 / 18, on three consecutive turns each. Note: a rejected Backstab is still spent (it goes to the discard pile and consumes its play) — unchanged, and consistent with the card text. **Verified.** |
| August 15, 2026 (bug fix — deck integrity) | **Exhaust card-duplication bug fixed. Pre-existing and unrelated to the Soul-menu work in the rows below — it predates that system entirely and shares no code with it.** Player report: one reward pick of Berserker's Oath became 4 copies in the deck, and the Deck viewer's header total disagreed with its pile counts. Traced, not assumed — cards are plain string KEYS and `G.deck` is a persistent master list that playing or exhausting a card never removes from; `G.exhaustedPile` is only a this-combat exclusion list. Three linked defects found: **(1)** `showReward()` did `G.deck.push(...G.exhaustedPile)` before clearing the pile — the "return to deck" step re-added cards that had never left, so every exhaust permanently added a duplicate copy. **(2)** `playCard()` pushed Powers onto `G.exhaustedPile` even though ~25 Exhaust cards in `js/data.js` already push their own key inside `effect()` — so a Power was exhausted twice per play, and defect 1 then added **2** copies per fight (measured: 1 → 3 → 5 → 7 → 9 across four wins). **(3)** self-exhausting non-Powers (Spell Echo, Jackpot, Betting It All, Loaded House) landed in the exhausted pile AND the discard pile, so they could be redrawn during the very combat they "exhausted" — 1 duplicate per fight. Fixes: the return step now just clears the pile; `playCard()` detects (by key count taken before `effect()`) whether the card exhausted itself and places it in exactly one pile, trimming surplus entries if Spell Echo repeated an Attack's effect; a Power whose effect does not self-place still exhausts. Separately, the reshuffle inside `drawCards()` filtered the discard pile with `!exh.includes(k)`, which deleted **every other copy** of an exhausted card's key from circulation while `G.deck` still counted them — this was the deck-viewer count mismatch, and it now recycles the discard pile as-is (an exhausted card can no longer be in it). `shuffleDeck()` keeps an instance-accurate `excludeExhausted()` helper as a guard. Verified by headless repro (32 assertions): deck size and per-card copy counts unchanged across 4 and 10 consecutive wins, exhausted cards still unavailable for the rest of their own combat (including after a forced mid-combat reshuffle) and available again the next fight, normal cards still discard, two copies of the same Exhaust card behave independently, boss-fight path clean, and `G.deck.length === draw + hand + discard + exhausted` holds at every step. **Verified** for the card-count invariant; in-app Deck viewer read-through still recommended. |
| August 15, 2026 (bug fix) | **Start-of-combat Block bug fixed — this is a pre-existing relic bug, NOT a Soul-menu balance change.** `startTurn()` zeroes `G.block` on the first turn of every combat, and it runs *after* the combat-start hooks in `startCombat()`/`startBossFight()`. Every start-of-combat Block grant was therefore wiped before the player ever saw it: **Iron Vambrace** ("Start every combat with 6 Block") granted nothing, and **Hollow Throne** ("Start every combat with 20 Block. Lose 8 max HP") shipped as pure downside — the player paid 8 max HP for Block that never arrived. Confirmed by direct test, then fixed: `stageCombatStartBlock()` stages the total into `G._pendingCombatBlock`, and `startTurn()` applies it immediately after its turn-1 reset. Iron Vambrace (6), Hollow Throne (20) and the Soul upgrade Grit (5) all route through it and stack correctly (31 with all three). **Read the relic numbers as restored-to-spec, not buffed** — 6 and 20 are the values their descriptions have always advertised, and if start-of-combat Block ever looks strong in testing, that is these two relics finally working, not Grit being overtuned. Grit's own 5 is unchanged from the finalized design. The Aldric fight still skips relic start-of-combat hooks entirely (a separate pre-existing gap — `torn_page`, `rusted_chain` and `ashen_crown` are missing there too); Grit does apply there, since the Floor 3 spend window precedes that fight. Thief's `shadow_wrap` (5 Block) is still unbuilt and should be routed through the same field when it lands. |
| August 15, 2026 (build) | **Soul-spend menu built** (split build) — the first implementation pass on the July 25 in-run Soul redesign. **Income converted to the GDD §15 flat rate**: 1 regular win / 2 elite / 3 floor boss, +1 per battle with Ash Pendant, replacing the per-enemy `souls` values in `js/data.js` (bosses paid 15, which would have made a 3-8 Soul menu meaningless); the data fields are left in place but no longer read. `G.souls` already reset to 0 per run via `newGame()` — confirmed, not changed. **8 upgrades added** as `SOUL_UPGRADES` in `js/data.js` (Vitality, Grit, Steady Hand, Second Die, Momentum, Overdraw, Reckless Surge, Gambler's Edge) at the finalized costs. **Spend screen** (`showSoulSpend()`, `js/ui.js`) reuses the shared reward screen and its `.reward-card` tiles, the same way the Void Compass relic pick does; it offers 3 of 8 chosen by the existing shuffle-then-slice pattern, never duplicates, never re-offers an owned non-repeatable, dims what you can't afford, and always exposes a "Keep your Souls" decline so a broke player is never stuck. Fires from `proceedAfterCardReward()` after a Floor 1-3 boss only — Aldric is excluded, and the Floor 4 boss never reaches the reward flow at all. Because the **boss relic-choice screen is still unbuilt**, the Soul screen currently follows the boss *card* reward; when the relic screen lands it slots in between. `buySoulUpgrade()`/`soulUpgradeCost()`/`soulUpgradeOffer()` live in `js/game.js`; Vitality's rebuy price escalates through `G._vitalityBuys` (3→4→5…) while every other upgrade is once per run. **Reroll converted from a boolean to a charge counter** (`G.rerollsLeft`, base allowance from `rerollAllowance()` = 1 per turn) with `G.rerollUsed` kept in sync as "no charges left at all" for existing call sites. **Steady Hand is a separate combat-long pool** (`G._bonusRerolls`), granted once at combat start and deliberately NOT refreshed at turn boundaries — one scarce extra reroll for the whole fight, per GDD §15's "+1 reroll charge per combat". The base per-turn reroll cadence is untouched, and `useReroll()` spends the per-turn charge first so the bonus is only consumed once the normal reroll is gone. Unspent bonus does not carry or stack into the next combat. **Two new dice controls** float above the dice panel, visible only while owned and unspent this combat: Second Die (+d2, sum capped at the die's max face) and Gambler's Edge (force any value, respects the one-forced-value-per-turn rule via `_dieSetThisTurn`). Both reset every combat, not every run. Gambler's Edge's downside suppresses affinity on a **natural** max only — `rollDice()` stamps `G._naturalDieValue`, so any card that forces the die afterwards makes the stamp disagree and the roll counts normally; applied in `checkAffinity()`, Lucky Streak, Lucky Coin, and the max-face half of Vampiric Form's extreme check, while cards' own `Max:` bonus branches are untouched. Also noted: the Soul Market event's "buy a soul — permanent +5 max HP" flavor still describes the dead permanent-Soul design (GDD-side only — no such event exists in `js/data.js`, which has just 5 generic events). Verified with a headless harness (106 assertions) driving the real `js/*.js` against a DOM stub: income across a full floor, run reset, offer distinctness across 400 draws, Vitality rebuy escalation, can't-afford dismissal, per-combat flag resets, natural-vs-forced max suppression, and Grit/Overdraw/Momentum reaching turn 1. **Implemented — in-app device/browser verification (especially the new dice buttons at landscape phone heights) still outstanding.** |
| July 23, 2026 | Card/combat consistency + systems session (split build). **7 missing GDD cards implemented** (base + upgrade, added to hero reward pools): Thief's Gambit, Gut Punch, Golden Strike (Thief); Cursed Veins (Vampire); Wild Combo, Press Your Luck, Jackpot (Gambler). **8 dormant Power-card hooks wired up**: Berserker's Oath (Block on HP loss), Burning Soul (+dmg per Burn stack), Poison Master (+dmg per Poison stack), Lethal Rhythm (dmg every 2 cards played), House Edge (min-roll floor), Gambler's Fallacy (guaranteed max after N non-max rolls), Vampiric Form (auto-Fly on extreme roll), and Shadow Artist base (2nd & 4th card each turn cost 0) — the base variant was previously a no-op. **2 bugs found + fixed along the way**: (1) House Edge / Gambler's Fallacy leaked across combats via persistent `_minRoll` / `_fallacyThreshold` flags — converted to status-gated so the effect clears with the exhausted card at combat end; (2) Fly's damage-halving was defined only as a tooltip and never wired into the enemy-attack calc — now applied (halves the hit, then clears). **Enemy-damage pipeline unified**: extracted `resolveEnemyAttack()` (Fly → Block → `loseHP` → on-HP-loss effects) shared by regular enemies AND Aldric (Aldric previously used a separate `dealDamage('player')` path, so it missed Fly); enemy specials (Ritual, Arcane Overload, Collapse) tagged enemy-direct and routed through the same pipeline so Fly halves them too (self-damage/DoT deliberately excluded). Collapse additionally got an opt-in `bypassBlock` flag on `resolveEnemyAttack()`: its damage equals the player's current Block, so the normal Block step made it net ~0 HP every time — bypassBlock sends it straight to HP (Fly still halves it first), so it now deals real damage that punishes defensive play. The flag is per-attack — only Collapse passes it; basic/Aldric/Ritual/Arcane Overload still respect Block. Survive-killing-blow relics (Rabbit Foot, Phylactery) centralized in `loseHP` so they now cover the basic attack, not just `dealDamage` sources. Berserker's Oath now fires on ALL player HP loss (enemy attacks, enemy specials, and self-damage cards) through the shared `loseHP` chokepoint. Also fixed an earlier draw-order bug where a card's own draw effect ran before the card left the hand, so in-turn draws hit the hand cap one slot early (e.g. Blood Price drew 0 at a full hand). **Hand size rule changed**: split the single `handLimit` into `startingDrawCount` (5) and `maxHandSize` (8) — turn-start draw stays exactly 5, while in-turn draw effects can now fill the hand up to 8 (excess draws blocked). **`curseddice` restored + renamed**: display name → Cursed Reroll / Cursed Reroll+ to resolve a collision with the d4 die also named "Cursed Die" (key unchanged); added to every hero's uncommon reward pool; its previously-missing `+` upgrade added (reroll, take 1 damage instead of 3). **Void Compass implemented**: after a non-boss elite, if the relic is held, offers a choice of 3 relics (via a shared `acquireRelic()` grant path reused by the shop); fires once per fight, otherwise elites behave as before (card reward only). **UI fixes**: relic display added (`renderRelics()`), shop "Leave Shop" sticky-footer fix, card width adjustment. All code changes are Implemented and were unit-tested via a headless harness against the real `js/data.js` / `js/combat.js` / `js/ui.js`; in-app device/browser verification still recommended. |
| August 21, 2026 | **Energy "next card free/cheaper" ordering fixed.** `playCard()` applied three of the seven such effects — Shadow Artist+ (`_shadowArtistDiscount`), Disappear (`_disappearCount`, also written by Cursed Veins' non-extreme branch) and Cursed Veins' free Skill (`_freeSkillCount`) — as post-payment refunds *below* the `G.energy < actualCost` gate, so they could only ever help when the card was already affordable at full price. All three moved into `getCardEnergyCost()` alongside the four that were already correct (Mana Surge, Soulbound Gauntlet, The House Always Wins, Shadow Artist base), and the refund blocks deleted rather than duplicated. Because `getCardEnergyCost()` also feeds `renderHand()`, this fixes the display too: affected tiles printed full price and were greyed unplayable. **Two further bugs the reported symptom hid, found by characterising the old behaviour before editing:** Shadow Artist+ consumed a stack for *zero* benefit (it reduced the local `actualCost` after payment, never refunding energy) and shrank a Disappear refund by 1 when both applied; and a card whose `effect()` refuses (Death Rattle above 50% HP) *minted* energy under Disappear, refunding the same payment twice via `_lastCardCostPaid` (measured 2 -> 4). Curse-surcharge ordering re-confirmed unchanged: the tax still applies to the base cost above every discount, so a free-card effect cannot be resurrected by a surcharge. **A third, separate bug found while testing the moved code:** the self-exclusions tested the raw `cardKey`, but upgrades are registered as `CARDS[key + '+']`, so `cardKey !== 'disappear'` never matched 'disappear+' — **Disappear+ and Mana Surge+ each consumed their own discount the moment they were played** (Cursed Veins was the only one that had spelled out both keys). All three now test the normalized `baseKey` already computed in that function for the curse surcharges, so one test covers both spellings and any future upgrade. Note this also fixes Mana Surge, one of the four effects previously believed correct. Verified with 22 headless assertions against the real `js/*.js` (including regression guards on all four already-correct effects, both self-exclusions, and that display-only calls consume nothing across repeated renders) plus 7 browser assertions on the rendered tile's printed cost and unplayable state. |
| August 21, 2026 | **Mirror "Step Through" SyntaxError fixed — the feature was 100% non-functional.** `showDoors()` built the button as `onclick="' + useMirrorCall + '"` where `useMirrorCall` came from `JSON.stringify()`, which emits double quotes. The HTML parser therefore ended the attribute at the first one, storing only `useMirror(` — confirmed by reading the parsed attribute in a real browser — and clicking compiled that truncated fragment into `SyntaxError: Unexpected end of input`. Because `js/data.js`'s `window.onerror` replaces `document.body.innerHTML` with the "Failed to load" screen, one click destroyed the whole page. Fixed by `.replace(/"/g, '&quot;')` on the assembled call: the entity decodes back to `"` before the value reaches the JS engine, so `JSON.stringify()`'s string literals still parse at call time and only the HTML-level collision is removed. **Measured, not assumed:** the break was universal — all 12 floor x path combinations produced the identical truncated attribute and error screen, and the spilled arguments were absorbed as junk *attributes* on the tag (e.g. `a",70,"mirror_used_2_b",7)"`, lowercased by the parser) rather than as visible text, which is why the panel still looked correct. Verified with 82 browser assertions on a fresh page per case (the error screen otherwise poisons later cases): attribute integrity and handler compilation across every floor/path, then a real click switching path, charging exactly the offered cost, setting the target room index, persisting `mirror_used_<floor>_<path>` so the Mirror is not re-offered, and clearing the panel reference; plus the 0-gold refusal path and the sibling "Walk Away" button, both unchanged. Audited for recurrence: this is the only string-built inline `onclick` in the entire `js/` tree, and the other `JSON.stringify()` call site (`js/meta.js`, localStorage) touches no HTML. |
